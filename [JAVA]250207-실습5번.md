### Bonus.java

```java
package javaexam2.company;

public interface Bonus {
    public void incentive(int pay);
}

```

### Employee.java

```java
package javaexam2.company;

public abstract class Employee {
    private String name;
    private int number;
    private String department;
    private int salary;

    public Employee() {}
    public Employee(String name, int number, String department, int salary) {
        this.name = name;
        this.number = number;
        this.department = department;
        this.salary = salary;
    }

    public abstract double tax();

    public String getDepartment() {
        return department;
    }

    public void setDepartment(String department) {
        this.department = department;
    }

    public int getSalary() {
        return salary;
    }

    public void setSalary(int salary) {
        this.salary = salary;
    }

    public int getNumber() {
        return number;
    }

    public void setNumber(int number) {
        this.number = number;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }
}

```

### Secretary.java

```java
package javaexam2.company;

public class Secretary extends Employee implements Bonus{

    public Secretary() {}
    public Secretary(String name, int number, String department, int salary) {
        super(name, number, department, salary);
    }

    @Override
    public void incentive(int pay) {
        setSalary(getSalary() + (int)(pay*0.8));
    }

    @Override
    public double tax() {
        return getSalary()*0.1;
    }
}

```

### Sales.java

```java
package javaexam2.company;

public class Sales extends Employee implements Bonus{

    public Sales() {}
    public Sales(String name, int number, String department, int salary) {
        super(name, number, department, salary);
    }

    @Override
    public void incentive(int pay) {
        setSalary(getSalary() + (int)(pay*1.2));
    }

    @Override
    public double tax() {
        return getSalary()*0.13;
    }
}

```

### Company.java

```java
package javaexam2.company;

import java.util.HashMap;
import java.util.Map;

public class Company {
    public static void main(String[] args) {
        Map<Integer, Employee> map = new HashMap<Integer, Employee>();
        // 1번의 데이터를 기반으로 객체를 생성 하여 HashMap에 넣는다.
        // HashMap에 넣을 때 키 값은 각 객체의 Number로 한다.

        Secretary e1 = new Secretary("Hilery", 1, "secretary", 800);
        Sales e2 = new Sales("Clinten", 2, "sales", 1200);

        map.put(e1.getNumber(), e1);
        map.put(e2.getNumber(), e2);

        // 모든 객체의 기본 정보를 출력 한다(for문을 이용하여 출력 한다.)
        System.out.printf("%-15s%-20s%-9s\n", "name", "department", "salary");
        System.out.println("-------------------------------------------");
        for (Integer key : map.keySet()) {
            Employee emp = map.get(key);
            System.out.printf("%-15s%-20s%-9s\n", emp.getName(), emp.getDepartment(), emp.getSalary());
        }
        System.out.println();
        // 모든 객체에 인센티브 100씩 지급하고 다시 HashMap에 넣는다.
        e1.incentive(100);
        e2.incentive(100);
// 강사님 풀이
//        for(Integer i : map.keySet()) {
//            if(map.get(i) instanceof Secretary) {
//                Secretary sec = (Secretary) map.get(i);
//                sec.incentive(100);
//                map.put(sec.getNumber(), (Employee)sec);
//            } else {
//                Sales sec = (Sales) map.get(i);
//                sec.incentive(100);
//                map.put(sec.getNumber(), (Employee)sec);
//            }
//        }

        System.out.println("인센티브 100 지급");
        // 모든 객체의 정보와 세금을 출력 한다 (for문을 이용하여 출력 한다.)
        System.out.printf("%-15s%-20s%-9s%-9s\n", "name", "department", "salary", "tax");
        System.out.println("---------------------------------------------------");
        for (Integer key : map.keySet()) {
            Employee emp = map.get(key);
            System.out.printf("%-15s%-20s%-9s%-9s\n", emp.getName(), emp.getDepartment(), emp.getSalary(), emp.tax());
        }
        System.out.println();
    }
}

```
