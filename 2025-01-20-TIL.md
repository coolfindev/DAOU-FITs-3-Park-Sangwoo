### 서브쿼리

- SQL 문 안에 포함된 또 다른 SQL 문
- 메인 쿼리에 값을 제공하거나, 조건을 지정하는 역할
- 일반적으로 괄호로 감싸져 있으며, SELECT, INSERT, UPDATE, DELETE 문으로 사용 가능
- 연관 여부에 따른 분류
    - 비연관/연관
- 위치에 따른 분류
    - 스칼라 서브쿼리/인라인 뷰/중첩 서브쿼리
- 반환 값 유형에 따른 분류
    - 단일 행 서브쿼리, 다중 행 서브쿼리, 다중 열 서브쿼리

### 연관 여부에 따른 분류

서브쿼리 독립적으로 실행할 수 있는지 구분하자

- 비연관 서브쿼리
    - 서브쿼리가 독립적으로 실행
    - 메인쿼리, 서브쿼리 간 상호작용 없음
- 연관 서브쿼리
    - 서브쿼리가 메인 쿼리의 각 행마다 실행
    - 메인 쿼리의 데이터를 서브쿼리에서 참조

### 위치에 따른 분류

- 스칼라 서브쿼리
    - 서브쿼리가 컬럼 값으로 변환되어 SELECT 절에서 사용
    - 주로 집계 값이나 계산 결과를 포함할 떄 활용
- 인라인 뷰
    - FROM 절에서 서브쿼리 결과를 테이블처럼 사용
- 중첩 서브쿼리
    - WHERE절, HAVING절에서 조건을 설정하기 위해 사용

### 반환 값 유형에 따른 분류

- 단일 행 서브쿼리
    - 서브쿼리가 단일 행과 단일 열을 반환
- 다중 행 서브쿼리
    - 서브쿼리가 여러 행을 반환
    - 다중 행 연산자(IN, ANY, ALL)와 함께 사용

### 집합 연산자

- 두 개 이상의 SELECT 결과를 결합하여 하나의 결과 집합을 생성
- 데이터베이스에서 집합 이론을 기반으로 작동
- UNION
    - 중복 제외 합집합
    - 결과는 정렬된 상태로 오름차순 반환
- UNION ALL
    - 중복 포함 합집합
    - 결과가 정렬되지 않음
- INTERSECT
    - 교집합
    - 중복된 행은 하나만 반환
- MINUS
    - 차집합
    - 중복된 행은 하나만 반환

### 실습

- 서브쿼리

```sql

-- 지점 이름이 "Location 1"에 해당하는 지점에서 대출을 받은 고객의 이름을 출력하시오.
SELECT NAME
FROM CUSTOMERS
WHERE CUSTOMER_ID IN (SELECT CUSTOMER_ID
    FROM LOANS
    WHERE BRANCH_ID IN (SELECT BRANCH_ID
        FROM BRANCHES
        WHERE NAME LIKE 'Location 1'
    )
);

-- 특정 고객(CUSTOMER_ID = 100)이 소유한 모든 계좌에서 발생한 거래를 출력하시오.
SELECT *
FROM TRANSACTIONS
WHERE ACCOUNT_ID IN (
    SELECT ACCOUNT_ID
    FROM ACCOUNTS
    WHERE CUSTOMER_ID = 100);
    
-- 각 고객이 보유한 계좌 중 가장 높은 잔액을 가진 계좌를 조회하시오.
SELECT C.NAME, T.MAX_BALANCE
FROM  CUSTOMERS C,(SELECT MAX(BALANCE) MAX_BALANCE, CUSTOMER_ID
    FROM ACCOUNTS
    GROUP BY CUSTOMER_ID) T
WHERE C.CUSTOMER_ID = T.CUSTOMER_ID
ORDER BY T.MAX_BALANCE DESC;
```

- 집합연산자

```sql
--대출을 받지 않은 고객의 ID를 출력하시오.
SELECT CUSTOMER_ID FROM CUSTOMERS
MINUS
SELECT CUSTOMER_ID FROM LOANS;

--거래 테이블과 계좌 테이블에 공통적으로 존재하는 계좌 id를 출력하시오.
SELECT ACCOUNT_ID FROM ACCOUNTS
INTERSECT
SELECT ACCOUNT_ID FROM TRANSACTIONS;

--대출 내역이 없는 지점id를 조회하시오.
SELECT BRANCH_ID FROM BRANCHES
MINUS
SELECT BRANCH_ID FROM LOANS;
```

- 실전문제
    1. 직원과 부서 정보를 조합하여 직무와 급여 통계
    
    ```sql
    SELECT (SELECT DEPARTMENT_NAME FROM DEPARTMENTS WHERE DEPARTMENT_ID = E.DEPARTMENT_ID) 부서명,
        JOB_ID "직무 ID", ROUND(AVG(SALARY), 2) "평균 급여", MAX(SALARY) "최대 급여"
    FROM EMPLOYEES E
    WHERE DEPARTMENT_ID IS NOT NULL
    GROUP BY DEPARTMENT_ID, JOB_ID
    HAVING AVG(SALARY) >= 8000
    ORDER BY AVG(SALARY) DESC;
    ```
    
    1. 5년 이내에 입사한 직원들의 급여와 전체 직원 급여 비교
    
    ```sql
    SELECT (SELECT DEPARTMENT_NAME FROM DEPARTMENTS WHERE DEPARTMENT_ID = E.DEPARTMENT_ID) 부서명,
        ROUND(AVG(SALARY)) "평균 급여"
    FROM EMPLOYEES E
    WHERE DEPARTMENT_ID IS NOT NULL
    GROUP BY DEPARTMENT_ID
    HAVING AVG(SALARY) < (
        SELECT AVG(SALARY)
        FROM EMPLOYEES
        WHERE HIRE_DATE >= ADD_MONTHS(SYSDATE, -60));
    ```
    
    1. 특정 지역의 지점에서 대출을 받은 고객 조회
    
    ```sql
    SELECT C.NAME, T.AMOUNT, T.STATUS
    FROM CUSTOMERS C,
        (SELECT CUSTOMER_ID, AMOUNT, STATUS
        FROM LOANS
        WHERE BRANCH_ID IN
            (SELECT BRANCH_ID FROM BRANCHES WHERE LOCATION = 'Location 4')
            AND AMOUNT >= 5e4) T
    WHERE C.CUSTOMER_ID = T.CUSTOMER_ID
    ;
    ```
    
    1. 대출 상태가 'PENDING'인 고객과 'REJECTED'인 고객의 교집합
    
    ```sql
    select name, email, phone
    from customers
    where customer_id in (select customer_id
    from loans
    where status = 'PENDING'
    intersect
    select customer_id
    from loans
    where status = 'APPROVED');
    ```
    
    1. 대출 상태와 계좌 잔액 통계 병합
    
    ```sql
    SELECT C.NAME 고객이름, T.BALANCE 금액
    FROM CUSTOMERS C, (SELECT CUSTOMER_ID, BALANCE
        FROM ACCOUNTS A
        WHERE BALANCE >= 50000
        ) T
    WHERE C.CUSTOMER_ID = T.CUSTOMER_ID
    UNION
    SELECT T.NAME 고객이름, L.AMOUNT 금액
    FROM LOANS L, (SELECT CUSTOMER_ID, NAME
        FROM CUSTOMERS C)T
    WHERE L.CUSTOMER_ID = T.CUSTOMER_ID
    ORDER BY 고객이름;
    ```
    

### 윈도우 함수

- sql에서 행을 기준으로 범위를 정의하여 작업하는 함수
- 집계함수와 함께 데이터를 그룹화하지 않고도 계산 가능
- PARTITION BY
    - 데이터를 특정 그룹으로 나눔
    - 그룹 별로 윈도우 함수가 각 작동함
- ORDER BY
    - 데이터를 정렬하여 윈도우 함수의 동작 순서 지정
    - 누적 계산에 주로 사용
- 용도
    - 순위
        - RANK()-OVER (대상)
        - DENSE_RANK()-OVER (대상) → 중복 순위 존재
    - 이동 평균
    - 누적 합
        
        ```sql
        select employee_id, department_id, salary, sum(salary) over (partition by department_id order by salary) as dept_total
        from employees;
        ```
        
    - 그룹 별 최대 값 최소 값
        
        ```sql
        select employee_id, department_id, salary, max(salary) over (partition by department_id)
        as dept_max from employees;
        ```
        
        - where절을 사용하기 위해서는 쿼리 실행 순서에 따라 select의 alias를 사용할 수 없으므로 서브쿼리를 사용해서 해결하자
    - 옵션
        - ROWS
            - 물리적 행을 기준, 범위 지정
        - RANGE
            - 논리적 값을 기준, 범위 지정
        - (UNBOUNDED) PRECEDING
            - 윈도우 시작 범위를 첫 번째 행으로 지정
        - CURRENT ROW
            - 현재 행을 기준 윈도우 설정
        - FOLLOWING
            - 현재 행 이후의 특정 행 범위를 설정
