## 파일

- 문제1 - 은행 계좌 시스템 관리 구현
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <errno.h>
    
    #define FILE_NAME "accounts.txt"
    #define NEW_FILE "new_accounts.txt"
    
    typedef struct {
        int account;
        char name[20];
        double balance;
    } Account;
    
    void addAccounts() {
        FILE* f = fopen(FILE_NAME, "a");
        if (f == NULL) {
            printf("파일 열기 실패\n");
            return;
        }
    
        Account a;
        printf("계좌ID: ");
        scanf("%d", &a.account);
        printf("계좌 소유자 이름: ");
        scanf("%s", a.name);
        printf("초기 잔액: ");
        scanf("%lf", &a.balance);
    
        fprintf(f, "%d %s %.2lf\n", a.account, a.name, a.balance);
        fclose(f);
        printf(">> 계좌가 성공적으로 개설되었습니다.\n");
    }
    
    void printAccounts() {
        FILE* f = fopen(FILE_NAME, "r");
        if (f == NULL) {
            printf("파일 열기 실패\n");
            return;
        }
        char buffer[100];
        while (fgets(buffer, sizeof(buffer), f) != NULL) {
            printf("%s", buffer);
        }
        fclose(f);
    }
    
    void searchAccounts() {
        FILE* f = fopen(FILE_NAME, "r");
        if (f == NULL) {
            printf("파일 열기 실패\n");
            return;
        }
    
        int account;
        printf("검색할 계좌: ");
        scanf("%d", &account);
    
        char buffer[100];
        int cnt = 0;
    
        while (fgets(buffer, sizeof(buffer), f) != NULL) {
            Account a;
            sscanf(buffer, "%d %s %lf", &a.account, a.name, &a.balance);
            if (a.account == account) {
                printf("ID: %d | 이름: %s | 잔액: %.2lf원\n", a.account, a.name, a.balance);
                cnt++;
                break;
            }
        }
    
        if (cnt == 0) {
            printf(">> 일치하는 계좌가 없습니다.\n");
        }
    
        fclose(f);
    }
    
    void changeAccount(int sign) {
        FILE* f = fopen(FILE_NAME, "r");
        if (f == NULL) {
            printf("파일 열기 실패\n");
            return;
        }
    
        int amount, acc;
        if (sign == 1) {
            printf("출금할 계좌: ");
        }
        else {
            printf("입금할 계좌: ");
        }
        scanf("%d", &acc);
    
        char buffer[100];
        int cnt = 0;
        Account a;
        FILE* f2 = fopen(NEW_FILE, "w");
        if (f2 == NULL) {
            printf("새 파일 열기 실패\n");
            fclose(f);
            return;
        }
    
        while (fgets(buffer, sizeof(buffer), f) != NULL) {
            sscanf(buffer, "%d %s %lf", &a.account, a.name, &a.balance);
            if (a.account == acc) {
                printf("ID: %d | 이름: %s | 잔액: %.2lf원\n", a.account, a.name, a.balance);
                cnt++;
                if (sign == 1) {
                    printf("출금할 금액: ");
                    scanf("%d", &amount);
                    if (a.balance < amount) {
                        printf(">> 잔액이 부족합니다.\n");
                    }
                    else {
                        a.balance -= amount;
    					printf(">> %d번 계좌에 %.2lf원이 출금되었습니다.\n", a.account, amount);
                    }
                }
                else {
                    printf("입금할 금액: ");
                    scanf("%d", &amount);
                    a.balance += amount;
    				 printf(">> %d번 계좌에 %.2lf원이 입금되었습니다.\n", a.account, amount);
                }
            }
            fprintf(f2, "%d %s %.2lf\n", a.account, a.name, a.balance);
        }
    
        if (cnt == 0) {
            printf(">> 일치하는 계좌가 없습니다.\n");
        }
    
        fclose(f);
        fclose(f2);
    
        // 새 파일을 원래 파일로 덮어쓰기
        remove(FILE_NAME);
        rename(NEW_FILE, FILE_NAME);
    }
    
    void transferAccount() {
        FILE* f = fopen(FILE_NAME, "r");
        if (f == NULL) {
            printf("파일 열기 실패\n");
            return;
        }
        int amount, acc, acc2;
        printf("송금할 계좌: ");
        scanf("%d", &acc);
        printf("수취할 계좌: ");
        scanf("%d", &acc2);
        char buffer[100];
        int cnt = 0;
        Account a;
        FILE* f2 = fopen(NEW_FILE, "w");
        if (f2 == NULL) {
            printf("새 파일 열기 실패\n");
            fclose(f);
            return;
        }
        while (fgets(buffer, sizeof(buffer), f) != NULL) {
            sscanf(buffer, "%d %s %lf", &a.account, a.name, &a.balance);
            if (a.account == acc) {
                printf("ID: %d | 이름: %s | 잔액: %.2lf원\n", a.account, a.name, a.balance);
                cnt++;
                printf("이체 금액: ");
                scanf("%d", &amount);
                if (a.balance < amount) {
                    printf("이체 실패!\n");
                }
                else {
                    a.balance -= amount;
                    printf(">> 송금 후 잔액\n");
                    printf("ID: %d | 이름: %s | 잔액: %.2lf원\n", a.account, a.name, a.balance);
                }
            }
            else if (a.account == acc2) {
                printf("ID: %d | 이름: %s | 잔액: %.2lf원\n", a.account, a.name, a.balance);
                cnt++;
                a.balance += amount;
                printf(">> 수취 후 잔액\n");
                printf("ID: %d | 이름: %s | 잔액: %.2lf원\n", a.account, a.name, a.balance);
            }
            fprintf(f2, "%d %s %.2lf\n", a.account, a.name, a.balance);
        }
        if (cnt == 0) {
            printf(">> 일치하는 계좌가 없습니다.\n");
        }
    
        fclose(f);
        fclose(f2);
    
        // 새 파일을 원래 파일로 덮어쓰기
        remove(FILE_NAME);
        rename(NEW_FILE, FILE_NAME);
    }
    
    int main() {
        char choice;
    
        while (1) {
            printf("\n[은행 계좌 시스템]\n");
            printf("1. 신규 계좌 개설 (N)\n");
            printf("2. 계좌 목록 출력 (L)\n");
            printf("3. 계좌 검색 (S)\n");
            printf("4. 입금 (D)\n");
            printf("5. 출금 (W)\n");
            printf("6. 계좌 이체 (T)\n");
            printf("7. 종료 (Q)\n");
    
            printf("선택: ");
            scanf(" %c", &choice);
            switch (choice) {
            case 'N': case 'n': addAccounts(); break;
            case 'L': case 'l': printAccounts(); break;
            case 'S': case 's': searchAccounts(); break;
            case 'D': case 'd': changeAccount(0); break;
            case 'W': case 'w': changeAccount(1); break;
            case 'T': case 't': transferAccount(); break;
            case 'Q': case 'q': return 0;
            default: printf("잘못된 입력입니다.\n");
            }
        }
    
        return 0;
    }
    
    ```
    
- 문제2 - 거래 기록 관리 시스템 구현
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <errno.h>
     /*[문제2] 고객 금융 거래 기록을 관리하는 시스템을 작성하기.
     고객의 거래 정보를 파일에 저장하고, 새로운 거래를 추가하거나, 기존 거래 정보를 조회할수 있는 기능을 제공합니다. 
     
     1) 기능정의서
    - 고객 거래 기록 저장 : 새로운 거래가 발생하면 이를 파일에 기록합니다.파일에는 거래 일자, 거래 종류(입금 / 출금), 거래 금액이 기록됩니다. 
    - 고객 거래 기록 조회 : 사용자가 고객의 거래 내역을 조회할 수 있어야 합니다. 
    - 고객 거래 기록 추가 : 기존 거래 기록에 새로운 거래를 추가할 수 있어야 합니다.
    
    2) 요구 사항
    - 거래 기록은 transactions.txt라는 파일에 저장됩니다. 
    - 각 거래는 "거래 일자(YYYY-MM-DD), 거래 종류(입금/출금), 거래 금액" 형식으로 저장됩니다. 
    - 새로운 거래는 파일에 추가되며, 조회 시 기존의 거래 기록을 모두 보여줍니다. 
    */
    
    typedef struct {
    	char date[11];
    	char type[10];
    	int amount;
    } Transaction;
    
    //void saveTransaction() {}
    void printTransaction() {
    	FILE* f = fopen("transactions.txt", "r");
    	if (f == NULL) {
    		printf("파일 열기 실패\n");
    		return;
    	}
    
    	char buf[100];
    	printf("거래 내역:\n");
    	while (fgets(buf, sizeof(buf), f) != NULL) {
    		printf("%s", buf);
    	}
    }
    
    void addTransaction() {
    	FILE* f = fopen("transactions.txt", "a");
    	if (f == NULL) {
    		printf("파일 열기 실패\n");
    		return;
    	}
    
    	Transaction t;
    	printf("거래 일자(YYYY-MM-DD) : ");
    	scanf("%s", t.date);
    	printf("거래 종류(입금/출금) : ");
    	scanf("%s", t.type);
    	printf("거래 금액 : ");
    	scanf("%d", &t.amount);
    
    	fprintf(f, "%s, %s, %d\n", t.date, t.type, t.amount);
    
    	printf("거래가 추가되었습니다 : %s %s %d\n", t.date, t.type, t.amount);
    	fclose(f);
    }
    int main() {
    
    	while (1) {
    		printf("\n1. 거래 추가\n");
    		printf("2. 거래 내역 조회\n");
    		printf("3. 종료\n");
    
    		int num;
    		printf("선택 : ");
    		scanf("%d", &num);
    
    		switch (num) {
    		case 1: addTransaction(); break;
    		case 2: printTransaction(); break;
    		case 3: return 0;
    		}
    	}
        return 0;
    }
    
    ```
    
- 문제3 - 특정 거래 내역 검색 및 출력
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <errno.h>
    
    /*[문제2] 고객 금융 거래 기록을 관리하는 시스템을 작성하기.
     고객의 거래 정보를 파일에 저장하고, 새로운 거래를 추가하거나, 기존 거래 정보를 조회할수 있는 기능을 제공합니다.
    
     1) 기능정의서
    - 고객 거래 기록 저장 : 새로운 거래가 발생하면 이를 파일에 기록합니다.파일에는 거래 일자, 거래 종류(입금 / 출금), 거래 금액이 기록됩니다.
    - 고객 거래 기록 조회 : 사용자가 고객의 거래 내역을 조회할 수 있어야 합니다.
    - 고객 거래 기록 추가 : 기존 거래 기록에 새로운 거래를 추가할 수 있어야 합니다.
    
    2) 요구 사항
    - 거래 기록은 transactions.txt라는 파일에 저장됩니다.
    - 각 거래는 "거래 일자(YYYY-MM-DD), 거래 종류(입금/출금), 거래 금액" 형식으로 저장됩니다.
    - 새로운 거래는 파일에 추가되며, 조회 시 기존의 거래 기록을 모두 보여줍니다.
    */
    
    typedef struct {
        char date[11];
        char type[10];
        int amount;
    } Transaction;
    
    void searchTransaction(char* choice) {
        FILE* f = fopen("transactions.txt", "r");
        if (f == NULL) {
            printf("파일 열기 실패\n");
            return;
        }
    
        printf("검색 결과:\n");
        char buf[100];
        while (fgets(buf, sizeof(buf), f) != NULL) {
            Transaction t;
            sscanf(buf, "%[^,], %[^,], %d", t.date, t.type, &t.amount);
            if (strcmp(t.type, choice) == 0) {
                printf("%s, %s, %d\n", t.date, t.type, t.amount);
            }
        }
        fclose(f);
    }
    
    int main() {
        char choice[10];
        printf("검색할 거래 유형을 입력하세요 (입금/출금) : ");
        scanf("%s", choice);
    
        searchTransaction(choice);
        return 0;
    }
    
    ```
    
- char 입력 받을 때 한 칸 띄워서 `" %c"`로 받는 이유
    - 이전 입력에서 남아 있는 개행 문자가 무시되고, 사용자가 입력한 실제 문자를 변수에 저장할 수 있음

**텍스트 파일 입력/출력**

- 파일을 저장하기 위해서는 쓰기 모드로 연 뒤, 이를 저장하는 과정이 필요함
    - 이 과정에서 `fprintf()`가 사용됨
- fseek를 통해 동일한 파일 내 값을 변경할 수 있음
    - 단, 고정된 크기의 파일 특성 상 숫자와 같은 자료의 단위가 바뀔 시 버그가 발생할 수 있음
- 하나의 파일 포인터를 사용하기 위해서는 이전에 사용하던 파일 포인터를 모두 읽는 것이 좋음

**파일의 끝**

- feof(파일)
    - feof() 함수는 주어진 *stream*에 대해 파일의 끝 플래그가 설정되는지 여부를 표시함
    - 파일의 끝 플래그는 파일의 끝을 표시하기 위해 몇 개의 함수에 의해 설정됨
    - 파일의 끝 플래그는 이 스트림에 대해 rewind(), fsetpos(), fseek() 또는 clearerr() 함수를 호출하여 지.

**파일 저장(csv, json, …)**

- **fwrite**(&t, sizeof(Transaction), 1, file);
    - 데이터 시작 주소부터 저장된(블록크기 * 블록갯수)바이트의 데이터를 파일 포인터 변수에
    연결된 이진 파일에 쓴 후 파일에 쓴 블록의 개수를 반환
- **fread**(&t, sizeof(Transaction), 1, binFile)
    - 파일 포인터 변수에 연결된 파일에서 (블록 크기 * 블록 개수) 바이트의 데이터를 읽어서
    시작 주소에 저장한 후 블록 개수를 반환
    - 블록크기는 바이트 단위로 블록개수는 정수로 지정
    - 첫 인수인 데이터 저장 시작 주소에 해당하는 기억장소는 (블록크기 * 블록개수)바이트의
    데이터를 저장하기 위해 충분한 기억장소여야 함
