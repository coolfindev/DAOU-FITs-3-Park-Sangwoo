## 오답

- ALU(산술논리연산유닛)
    - 값 저장을 위해서 보다는 계산을 위한 장치
- char[4] = “야구”의 경우 실제 5바이트기 때문에 char[5]로 바꿔주는 것이 적절함

## 포인터

- *p를 사용하면 p가 가리키는 주소에 저장된 값을 가져올 수 있음
- &a를 사용하면 변수 a의 메모리 주소를 얻을 수 있음
- 배열은 스택영역에 데이터가 저장되지만 변수명은 저장되지 않음
    - 반면 포인터는 데이터 영역에 데이터가 저장되고 변수 또한 스택영역에 저장
- 실습예제 26
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    int main (){
    	int sum = 10, score[] = { 99,89,91,78,85 };
    	int* psum, * pscore1, * pscore2;
    	psum = &sum;
    	pscore1 = score;
    	pscore2 = score;
    
    	++pscore2;
    	pscore2++;
    
    	printf("변수 sum의 값 : %d\n", sum);
    	printf("포인터 변수 *psum의 값 : %d\n", *psum);
    	printf("포인터 변수 *psum+1의 값 : %d\n", *psum + 1);
    	printf("포인터 변수 ++ *psum의 값 : %d\n", ++ * psum);
    	printf("포인터 변수 *(psum+1)의 값 : %d\n", *(psum + 1));
    	printf("포인터 변수 *++psum의 값 : %d\n", *++psum);
    	
    	printf("=====================================================\n");
    
    	printf("포인터 변수 *pscore1의 값 : %d\n", *pscore1);
    	printf("포인터 변수 *pscore1+1의 값 : %d\n", *pscore1 + 1);
    	printf("포인터 변수 *(pscore1+1)의 값 : %d\n", *(pscore1 + 1));
    	printf("포인터 변수 *pscore2-*pscore1의 값 : %d\n", *pscore2 - *pscore1);
    	printf("포인터 변수 pscore2-pscore1의 값 : %d\n", pscore2 - pscore1);
    	printf("포인터 변수 pscore2의 값 : %d\n", *pscore2);
    	printf("포인터 변수 pscore1의 값 : %d\n", *pscore1);
    
    }
    
    ```
    
- 문제 1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    #define SIZE 5
    int main (){
    	//1. 배열과 포인터를 활용하여 고객의 계좌 잔액을 업데이트하고 최대 / 최소 잔액을 찾는 프로그램을 완성하시오.
    	int balances[SIZE] = { 1000, 5000, 3000, 7000, 2000 }; // 고객 계좌 잔액
    	int* p = balances;
    	int max = 0;
    	int min = 7000;
    
    	printf("현재 계좌 잔액 : ");
    	for (int i = 0; i < SIZE; i++) {
    		printf("%d ", *(p + i));
    	}
    
    	printf("\n");
    
    	for (int i = 0; i < SIZE; i++) {
    		int money;
    		if (i % 2 == 0) {
    			printf("%d번 고객 입금할 금액 : ", i + 1);
    			scanf_s("%d", &money);
    			*(p + i) += money;
    		}
    		else {
    			printf("%d번 고객 출금할 금액 : ", i + 1);
    			scanf_s("%d", &money);
    			*(p + i) -= money;
    		}
    	}
    
    	printf("업데이트된 계좌 잔액 : ");
    	for (int i = 0; i < SIZE; i++) {
    		printf("%d ", *(p + i));
    
    		if (max < *(p + i)) {
    			max = *(p + i);
    		}
    
    		if (min > *(p + i)) {
    			min = *(p + i);
    		}
    	}
    
    	printf("\n최대 잔액 : %d\n", max);
    	printf("최소 잔액 : %d\n", min);
    }
    
    ```
    
- 문제 2
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    #define SIZE 5
    int main (){
    	//2. 배열과 포인터를 사용하여 계좌별 비밀번호를 저장하고, 로그인 검증을 수행하는 프로그
    	//	램을 작성하시오.
    	
    	int acc[SIZE] = { 102, 105, 106, 107, 108 };
    	int pw[SIZE] = { 1234, 4321, 5678, 9876, 1111 };
    	
    	int acc;
    
    	printf("계좌번호 입력 : ");
    	scanf("%d", &acc);
    
    	int i;
    	for (i = 0; i < SIZE; i++) {
    		if (acc == acc[i]) {
    			printf("비밀번호 입력 : ");
    			scanf("%d", &pw);
    			if (pw == pw[i]) {
    				printf("로그인 성공!\n");
    			}
    			else {
    				printf("비밀번호 오류!\n");
    			}
    		}
    	}
    
    }
    
    ```
    
- 문제 3
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    #define SIZE 5
    int main (){
    	int stu[SIZE][SIZE] = { 0, };
    
    	int* p = stu;
    
    	printf("학생들의 성적을 입력하십시오 (각 학생당 5과목):\n");
    	for (int i = 0; i < SIZE; i++) {
    		printf("학생 %d 성적 입력: ", i+1);
    		scanf_s("%d %d %d %d %d", p + i * SIZE, p + i * SIZE + 1, p + i * SIZE + 2, p + i * SIZE + 3, p + i * SIZE + 4);
    	}
    
    	printf("학생들의 평균 성적: \n");
    	for (int i = 0; i < SIZE; i++) {
    		int sum = 0;
    		for (int j = 0; j < SIZE; j++) {
    			sum += *(p + i * SIZE + j);
    		}
    		printf("학생 %d 평균 성적: %.1f\n", i + 1, (double)sum / SIZE);
    	}
    }
    
    ```
    
- 문제 4
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    int main (){
    	printf("3x3 행렬 입력: \n");
    	int matrix[3][3];
    	for (int i = 0; i < 3; i++) {
    		for (int j = 0; j < 3; j++) {
    			scanf_s("%d", &matrix[i][j]);
    		}
    	}
    
    	printf("\n\n");
    
    	printf("제곱된 행렬:\n");
    	int* ptr[3] = { matrix[0], matrix[1], matrix[2] };
    	for (int i = 0; i < 3; i++) {
    		for (int j = 0; j < 3; j++) {
    			ptr[i][j] = ptr[i][j] * ptr[i][j];
    			printf("%2d ", ptr[i][j]);
    		}
    		printf("\n");
    	}
    
    	printf("\n\n");
    
    	printf("대각선 반전된 행렬 (전치 행렬):\n");
    	for (int i = 0; i < 3; i++) {
    		for (int j = 0; j < 3; j++) {
    			printf("%2d ", ptr[j][i]);
    		}
    		printf("\n");
    	}
    }
    
    ```
    
- 실습예제 27
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int max(int, int);
    int min(int, int);
    
    int main() {
    	int (*fpmm)(int, int);
    	int num1, num2, flag;
    	printf("두 개의 숫자를 입력 : ");
    	scanf("%d %d", &num1, &num2);
    	printf("원하는 값을 입력(1:큰값, 2:작은값) : ");
    	scanf("%d", &flag);
    
    	if (flag == 1) {
    		fpmm = max;
    		printf("두 수 중 큰 값은 : %d\n", fpmm(num1, num2));
    	}
    	else if(flag == 2){
    		fpmm = min;
    		printf("두 수 중 작은 값은 : %d\n", fpmm(num1, num2));
    	} 
    	else {
    		printf("잘못된 숫자 입력입니다.");
    	}
    }
    
    int max(int x, int y) {
    	if (x > y) return x;
    	return y;
    }
    
    int min(int x, int y) {
    	if (x < y) return x;
    	return y;
    }
    
    ```
    

이중포인터

- 문제1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    
    int main() {
    	//2. 이중 포인터를 이용하여 문자열 배열을 결과화면가 같이 나오게 해 보세요. * 요구사항
    	//	문자열 3개를 저장하는 2차원 배열을 만든다.이중 포인터를 사용하여 문자열을 입력받고, 모든 문자를 대문자로 변환한다.이중 포인터를 사용하여 문자열을 길이순으로 정렬한 후 출력한다.
    	//	* 결과화면
    	//	문자열 3개 입력 :
    	//hello
    	//	world
    	//	Cprogramming
    	//	대문자로 변환된 문자열 :
    	//HELLO
    	//	WORLD
    	//	CPROGRAMMING
    	//	문자열 길이순 정렬 :
    	//WORLD
    	//	HELLO
    	//	CPROGRAMMING
    
    	char str1[100], str2[100], str3[100];
    	char* str[3] = { str1, str2, str3 }; //
    	char** arr = str;
    	char* temp;
    
    	printf("문자열 3개 입력 : \n");
    	for (int i = 0; i < 3; i++) {
    		scanf("%s", arr[i]);
    		
    	}
    
    	printf("대문자로 변환된 문자열 : \n");
    	for (int i = 0; i < 3; i++) {
    		for (int j = 0; j < strlen(str[i]); j++) {
    			if (str[i][j] >= 'a' && str[i][j] <= 'z') {
    				str[i][j] -= 32;
    			}
    		}
    		printf("%s\n", str[i]);
    	}
    
    	printf("문자열 길이순 정렬 : \n");
    
    	//정렬
    	for (int i = 0; i < 3; i++) {
    		for (int j = 0; j < 3 - i - 1; j++) {
    			if (strlen(str[j]) > strlen(str[j + 1])) {
    				temp = str[j];
    				str[j] = str[j + 1];
    				str[j + 1] = temp;
    			}
    		}
    	}
    
    	for (int i = 0; i < 3; i++) {
    		printf("%s\n", str[i]);
    	}
    
    	return 0;
    }
    
    ```
    

**함수 포인터**

함수 포인터는 함수의 주소를 저장하는 포인터

이를 이용하면 함수를 매개변수로 전달하거나 콜백(callback) 함수를 구현할 수 있음

- 함수 포인터 사용 예제

```c
#include <stdio.h>
// 함수 정의
int add(int a, int b) {
return a + b;
}
int main() {
int (*funcPtr)(int, int); // 함수 포인터 선언
funcPtr = add; // 함수 주소 할당
printf("10 + 20 = %d\n", funcPtr(10, 20)); // 함수 포인터를 통해 호출
return 0;
}
```

- 문제1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    
    int add(int a, int b) {
    	return a + b;
    }
    
    int sub(int a, int b) {
    	return a - b;
    }
    
    int mul(int a, int b) {
    	return a * b;
    }
    
    int div(int a, int b) {
    	return a / b;
    }
    int main (){
    	/*1. 사용자가 선택한 연산을 수행하는 간단한 계산기를 작성하십시오. + , -, *, / 연산을 수
    		행하는 함수들이 각각 존재합니다.이들 함수에 대한 함수 포인터를 배열로 저장하고, 사용자가 원하는 연산을 선택하면 해당 연산이 수행되도록 하세요.결과화면
    		1. 덧셈
    		2. 뺄셈
    		3. 곱셈
    		4. 나눗셈
    		선택 : 1
    		두 숫자를 입력하세요 : 4 5
    		결과 : 9*/
    	
    	int (*fp)(int, int);
    
    	printf("1. 덧셈\n");
    	printf("2. 뺄셈\n");
    	printf("3. 곱셈\n");
    	printf("4. 나눗셈\n");
    
    	int num;
    	printf("선택 : ");
    	scanf("%d", &num);
    
    	int a, b;
    	printf("두 숫자를 입력하세요 : ");
    	scanf("%d %d", &a, &b);
    
    	if (num == 1) {
    		fp = add;
    	}
    	else if (num == 2) {
    		fp = sub;
    	}
    	else if (num == 3) {
    		fp = mul;
    	}
    	else if (num == 4) {
    		fp = div;
    	}
    
    	printf("결과 : %d\n", fp(a, b));
    }
    
    ```
