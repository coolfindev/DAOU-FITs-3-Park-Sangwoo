## 파일

- fread(&customer, sizeof(CUSTOMER), 1, fp) == 1 조건에서 개수를 1개씩 읽어오므로 while 안에 해당 조건을 넣는 것은 읽을 내용이 있냐라는 의미가 됨
- 문제1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    
    typedef struct person {
        char name[7];
        char gender[3];
        int age;
    } PERSON;
    
    void displayUsers(FILE* fp, int size) {
        PERSON user;
        puts("---------------------------------");
        puts("번호  이름   성별  나이");
        puts("---------------------------------");
        rewind(fp); // 파일의 시작위치로 이동
        for (int i = 0; i < size; i++) {
            fread(&user, sizeof(PERSON), 1, fp);
            printf("%3d %-8s %-3s %4d\n", i + 1, user.name, user.gender, user.age);
        }
        puts("---------------------------------");
    }
    
    void searchUser(FILE* fp, int size) {
        char searchName[7];
        PERSON user;
        int found = 0;
        printf("검색할 이름을 입력하세요: ");
        scanf("%s", searchName);
        puts("---------------------------------");
        puts("번호  이름   성별  나이");
        puts("---------------------------------");
        rewind(fp); // 파일의 시작위치로 이동
        for (int i = 0; i < size; i++) {
            fread(&user, sizeof(PERSON), 1, fp);
            if (strcmp(user.name, searchName) == 0) {
                printf("%3d %-8s %-3s %4d\n", i + 1, user.name, user.gender, user.age);
                found = 1;
            }
        }
        if (!found) {
            printf("사용자를 찾을 수 없습니다.\n");
        }
        puts("---------------------------------");
    }
    
    void addUser(FILE* fp) {
        PERSON user;
        printf("\n이름 : "); scanf("%s", user.name);
        printf("성별 : "); scanf("%s", user.gender);
        printf("나이 : "); scanf("%d", &user.age);
        fseek(fp, 0, SEEK_END); // 파일 끝으로 이동
        fwrite(&user, sizeof(PERSON), 1, fp);
        printf("새 사용자가 삽입되었습니다.\n");
    }
    
    void deleteUser(FILE** fp, int* size) {
        int no;
    
        if (*size == 0) {
            printf("삭제할 사용자가 없습니다.\n");
            return;
        }
    
        PERSON* users = (PERSON*)malloc(*size * sizeof(PERSON));
        if (users == NULL) {
            printf("메모리 할당 실패\n");
            return;
        }
    
        rewind(*fp);
        fread(users, sizeof(PERSON), *size, *fp);
    
        printf("삭제할 사용자의 번호는? ");
        scanf("%d", &no);
    
        if (no < 1 || no > *size) {
            printf("잘못된 번호입니다.\n");
        }
        else {
            for (int i = no - 1; i < *size - 1; i++) {
                users[i] = users[i + 1];
            }
    
            (*size)--;
    
            fclose(*fp);
            *fp = fopen("user.bin", "wb+");
            if (*fp == NULL) {
                printf("파일을 다시 여는 데 실패했습니다.\n");
                free(users);
                exit(1);
            }
    
            fwrite(users, sizeof(PERSON), *size, *fp);
            printf("사용자가 삭제되었습니다.\n");
        }
    
        free(users);
    }
    
    void viewAllUsers(FILE* fp, int size) {
        if (size == 0) {
            printf("등록된 사용자가 없습니다.\n");
        }
        else {
            displayUsers(fp, size);
        }
    }
    
    int main() {
        FILE* fp;
        int size = 0;
        char choice;
        fp = fopen("user.bin", "r+b");
        if (fp == NULL) {
            fp = fopen("user.bin", "wb+");
            if (fp == NULL) {
                printf("파일 생성 오류\n");
                exit(1);
            }
        }
        fseek(fp, 0, SEEK_END);
        size = ftell(fp) / sizeof(PERSON);
    
        while (1) {
            printf("\n메뉴:\n");
            printf("1. 전체 사용자 보기\n");
            printf("2. 사용자 검색\n");
            printf("3. 사용자 추가\n");
            printf("4. 사용자 삭제\n");
            printf("5. 종료\n");
            printf("선택하세요: ");
            scanf(" %c", &choice);
            switch (choice) {
            case '1':
                viewAllUsers(fp, size);
                break;
            case '2':
                searchUser(fp, size);
                break;
            case '3':
                addUser(fp);
                size++;
                break;
            case '4':
                deleteUser(&fp, &size);
                break;
            case '5':
                fclose(fp);
                printf("프로그램을 종료합니다.\n");
                return 0;
            default:
                printf("잘못된 선택입니다. 다시 시도하세요.\n");
            }
        }
        return 0;
    }
    ```
    

### **OCI를 활용한 CRUD 구현**

✅ **DB 연결**

- `OCIEnvCreate()` → `OCIHandleAlloc()` → `OCIServerAttach()` → `OCILogon2()`
- `OCI_ATTR_USERNAME`, `OCI_ATTR_PASSWORD`를 이용한 사용자 인증

✅ **SELECT 문 실행**

- `OCIStmtPrepare()` → `OCIStmtExecute()` → `OCIStmtFetch2()`
- `OCIDefineByPos()`를 활용해 결과값을 `C` 변수에 바인딩
- `while` 문을 사용해 데이터를 한 행씩 가져옴

✅ **INSERT 문 실행**

- `OCIStmtPrepare()` → `OCIBindByPos()` → `OCIStmtExecute()`
- `OCI_COMMIT_ON_SUCCESS` 옵션으로 실행 즉시 `COMMIT`

✅ **UPDATE 문 실행**

- `UPDATE test_table SET NAME = :1 WHERE ID = :2`
- 기존 `OCIBindByPos()`를 재사용하여 SQL 실행

✅ **DELETE 문 실행**

- `DELETE FROM test_table WHERE ID = :1`
- `OCIBindByPos()`를 통해 `삭제할 ID`를 바인딩 후 실행

✅ **핸들 해제 및 연결 종료**

- `OCIHandleFree()`를 호출하여 메모리 해제
- `OCILogoff()`로 DB 연결 종료
- 활용 문제
    
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    #include <oci.h>
    
    void check_error(OCIError* errhp) {
        text errbuf[512];
        sb4 errcode = 0;
        OCIErrorGet(errhp, 1, NULL, &errcode, errbuf, sizeof(errbuf), OCI_HTYPE_ERROR);
        printf("Oracle Error: %s\n", errbuf);
    }
    
    int main() {
        OCIEnv* envhp;
        OCIError* errhp;
        OCISvcCtx* svchp;
        OCISession* usrhp;
        OCIServer* srvhp;
        OCIStmt* stmthp;
        OCIDefine* def1 = NULL, * def2 = NULL, * def3 = NULL, * def4 = NULL;
        sword status;
    
        // DB 로그인 정보
        char* username = "C##COOLFIN";
        char* password = "1234";
        char* dbname = "localhost:1521/xe"; // Oracle 서비스 이름
    
        // 환경 핸들 초기화
        OCIEnvCreate(&envhp, OCI_DEFAULT, NULL, NULL, NULL, NULL, 0, NULL);
        OCIHandleAlloc(envhp, (void**)&errhp, OCI_HTYPE_ERROR, 0, NULL);
        OCIHandleAlloc(envhp, (void**)&srvhp, OCI_HTYPE_SERVER, 0, NULL);
        OCIServerAttach(srvhp, errhp, (OraText*)dbname, strlen(dbname), OCI_DEFAULT);
        OCIHandleAlloc(envhp, (void**)&svchp, OCI_HTYPE_SVCCTX, 0, NULL);
        OCIAttrSet(svchp, OCI_HTYPE_SVCCTX, srvhp, 0, OCI_ATTR_SERVER, errhp);
        OCIHandleAlloc(envhp, (void**)&usrhp, OCI_HTYPE_SESSION, 0, NULL);
        OCIAttrSet(usrhp, OCI_HTYPE_SESSION, username, strlen(username), OCI_ATTR_USERNAME, errhp);
        OCIAttrSet(usrhp, OCI_HTYPE_SESSION, password, strlen(password), OCI_ATTR_PASSWORD, errhp);
        OCISessionBegin(svchp, errhp, usrhp, OCI_CRED_RDBMS, OCI_DEFAULT);
        OCIAttrSet(svchp, OCI_HTYPE_SVCCTX, usrhp, 0, OCI_ATTR_SESSION, errhp);
    
        printf("✅ Oracle DB 연결 성공!\n");
    
        /*** 데이터 삽입 (INSERT) ***/
        char* insert_sql = "INSERT INTO test_customers (CUSTOMER_ID, NAME, PHONE, BIRTH_DATE) VALUES (:1, :2, :3, :4)";
        OCIHandleAlloc(envhp, (void**)&stmthp, OCI_HTYPE_STMT, 0, NULL);
        OCIStmtPrepare(stmthp, errhp, (text*)insert_sql, strlen(insert_sql), OCI_NTV_SYNTAX, OCI_DEFAULT);
    
        int insert_id = 2;
        char insert_name[50] = "John";
        char insert_phone[20] = "123-456-7890";
        char insert_birth_date[20] = "1990-01-01";
        OCIBind* bnd1 = NULL, * bnd2 = NULL, * bnd3 = NULL, * bnd4 = NULL;
    
        // 바인딩 변수 설정 (INSERT)
        OCIBindByPos(stmthp, &bnd1, errhp, 1, &insert_id, sizeof(insert_id), SQLT_INT, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
        OCIBindByPos(stmthp, &bnd2, errhp, 2, insert_name, sizeof(insert_name), SQLT_STR, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
        OCIBindByPos(stmthp, &bnd3, errhp, 3, insert_phone, sizeof(insert_phone), SQLT_STR, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
        OCIBindByPos(stmthp, &bnd4, errhp, 4, insert_birth_date, sizeof(insert_birth_date), SQLT_STR, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
    
        if (OCIStmtExecute(svchp, stmthp, errhp, 1, 0, NULL, NULL, OCI_COMMIT_ON_SUCCESS) != OCI_SUCCESS) {
            check_error(errhp);
        }
        else {
            printf("✅ 데이터 삽입 완료!\n");
        }
    
        /*** 데이터 수정 (UPDATE) ***/
        char* update_sql = "UPDATE test_customers SET NAME = :1, PHONE = :2, BIRTH_DATE = :3 WHERE CUSTOMER_ID = :4";
        OCIHandleAlloc(envhp, (void**)&stmthp, OCI_HTYPE_STMT, 0, NULL);
        OCIStmtPrepare(stmthp, errhp, (text*)update_sql, strlen(update_sql), OCI_NTV_SYNTAX, OCI_DEFAULT);
    
        char updated_name[50] = "John Updated"; // 수정할 이름
        char updated_phone[20] = "987-654-3210"; // 수정할 전화번호
        char updated_birth_date[20] = "1991-02-02"; // 수정할 생년월일
        int update_id = 2; // 수정할 ID
    
        // 바인딩 변수 설정 (UPDATE)
        OCIBindByPos(stmthp, &bnd1, errhp, 1, updated_name, sizeof(updated_name), SQLT_STR, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
        OCIBindByPos(stmthp, &bnd2, errhp, 2, updated_phone, sizeof(updated_phone), SQLT_STR, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
        OCIBindByPos(stmthp, &bnd3, errhp, 3, updated_birth_date, sizeof(updated_birth_date), SQLT_STR, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
        OCIBindByPos(stmthp, &bnd4, errhp, 4, &update_id, sizeof(update_id), SQLT_INT, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
    
        // SQL 실행
        if (OCIStmtExecute(svchp, stmthp, errhp, 1, 0, NULL, NULL, OCI_COMMIT_ON_SUCCESS) != OCI_SUCCESS) {
            check_error(errhp);
        }
        else {
            printf("✅ 데이터 수정 완료!\n");
        }
    
        /*** 데이터 삭제 (DELETE) ***/
        char* delete_sql = "DELETE FROM test_customers WHERE CUSTOMER_ID = :1"; // ID를 기준으로 삭제
        OCIHandleAlloc(envhp, (void**)&stmthp, OCI_HTYPE_STMT, 0, NULL);
        OCIStmtPrepare(stmthp, errhp, (text*)delete_sql, strlen(delete_sql), OCI_NTV_SYNTAX, OCI_DEFAULT);
    
        int delete_id = 2; // 삭제할 ID 값 (예: 2번 ID)
    
        // 바인딩 변수 설정 (DELETE)
        OCIBindByPos(stmthp, &bnd1, errhp, 1, &delete_id, sizeof(delete_id), SQLT_INT, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
    
        // SQL 실행
        if (OCIStmtExecute(svchp, stmthp, errhp, 1, 0, NULL, NULL, OCI_COMMIT_ON_SUCCESS) != OCI_SUCCESS) {
            check_error(errhp);
        }
        else {
            printf("✅ 데이터 삭제 완료!\n");
        }
    
        /*** 데이터 조회 (SELECT) ***/
        char* select_sql = "SELECT CUSTOMER_ID, NAME, PHONE, BIRTH_DATE FROM test_customers";
        OCIStmtPrepare(stmthp, errhp, (text*)select_sql, strlen(select_sql), OCI_NTV_SYNTAX, OCI_DEFAULT);
        OCIStmtExecute(svchp, stmthp, errhp, 0, 0, NULL, NULL, OCI_DEFAULT);
    
        int id;
        char name[50];
        char phone[20];
        char birth_date[20];
        OCIDefineByPos(stmthp, &def1, errhp, 1, &id, sizeof(id), SQLT_INT, NULL, NULL, NULL, OCI_DEFAULT);
        OCIDefineByPos(stmthp, &def2, errhp, 2, name, sizeof(name), SQLT_STR, NULL, NULL, NULL, OCI_DEFAULT);
        OCIDefineByPos(stmthp, &def3, errhp, 3, phone, sizeof(phone), SQLT_STR, NULL, NULL, NULL, OCI_DEFAULT);
        OCIDefineByPos(stmthp, &def4, errhp, 4, birth_date, sizeof(birth_date), SQLT_STR, NULL, NULL, NULL, OCI_DEFAULT);
    
        printf("✅ 테이블 조회 결과:\n");
        printf("---------------------------------------------\n");
        printf("| CUSTOMER_ID | NAME                | PHONE         | BIRTH_DATE |\n");
        printf("---------------------------------------------\n");
    
        while ((status = OCIStmtFetch2(stmthp, errhp, 1, OCI_DEFAULT, 0, OCI_DEFAULT)) == OCI_SUCCESS || status == OCI_SUCCESS_WITH_INFO) {
            printf("| %11d | %-18s | %-12s | %-10s |\n", id, name, phone, birth_date);
        }
    
        printf("---------------------------------------------\n");
    
        /*** 연결 종료 및 리소스 해제 ***/
        OCIHandleFree(stmthp, OCI_HTYPE_STMT);
        OCILogoff(svchp, errhp);
        OCIHandleFree(usrhp, OCI_HTYPE_SESSION);
        OCIHandleFree(svchp, OCI_HTYPE_SVCCTX);
        OCIHandleFree(srvhp, OCI_HTYPE_SERVER);
        OCIHandleFree(errhp, OCI_HTYPE_ERROR);
        OCIHandleFree(envhp, OCI_HTYPE_ENV);
    
        printf("✅ 연결 종료\n");
        return 0;
    }
    
    ```
