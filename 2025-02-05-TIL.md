### JDK 주요 실행파일

- `javac`: 자바 컴파일러, 소스코드 → 바이트코드로 컴파일
- `java` :자바 인터프리터, 컴파일러가 생성한 바이트코드를 해석하고 실행
- `javadoc`: 자동문서 생성기, 소스코드에 있는 주석을 이용하여 Java API와 같은 형식의 문서를 만들어 줌
- `jar`: 압축프로그램, 클래스파일과 프로그램의 실행에 관련된 파일들을 하나의 파일로 압축하거나 해제할 때 사용됨

### Variable

- 강타입 언어라 빠르게 작성할 수 있으나 유연성은 떨어짐
- 데이터 타입
    - primitive type(원시타입)
    - reference type(참조타입)
  
![image](https://github.com/user-attachments/assets/24454cbb-eece-4ced-b03f-438c7e157594)


- boolean은 묵시적 형 변환이 일어나지 않기에 다른 기본형과의 연산이 불가능
- static은 현재로서는 필드와 메소드 앞에 붙을 수 있음.
    - 모든 객체가 공유할 수 있도록 사용이 가능해짐
    - 과다한 사용한 재활용성과 유지보수성이 떨어지게 됨

### 객체지향

- 클래스
    - 네이밍은 한글도 가능하나(유니코드 사용), 권장하지 않음
    - 객체 모델링 수단
    - ADT
- 인스턴스
    - 필드와 메소드를 사용하기 위해 메모리에 일정 공간 확보
    - 자바 프로그램은 인스턴스를 가지고 동작
    - 멤버
        - 인스턴스가 독립적으로 메모리 공간을 확보하는 field, method로 이용해야 사용 가능함

### 메모리 구조

- Register
- Runtime Constant Pool
- Method Area
    - 클래스들의 정보가 들어감
- Heap
- Call Stack( 일반적으로 Stack이라고 함 )
    - 메소드 공간, 지역변수 할당;

### package/import

- 효율적으로 소스코드를 관리, 재사용하는 방법
- 패키지를 지정하지 않으면 default package에 포함됨
- package 명은 중첩되어 사용 가능하고, package 안에는 바이트코드만을 포함시킬 수도 있음
- 타 패키지의 클래스를 가져올 수 있음

```java
//aaa 패키지의 TestA 클래스를 bbb패키지에서 가져올 때
package bbb;
import aaa;
aaa.TestA a = new aaa.TestA();
```

### 접근제어자

- public
    - 어디에서나 접근 가능한 field, method, class 선언에 사용
    - 컴파일 했을 때 클래스 단위로 .class파일(바이트코드)이 생성됨
    - 클래스 파일 내에서는 최대 1개의 public 클래스만 컴파일 가능함
        - 가독성, 유지보수 측면
    - public 클래스를 타 패키지에서 불러올 때 내부 생성자, 필드, 메소드는 캡슐화를 할 수 있음
    
    ```java
    public class PackTest {
    	//instance variable
    	int k; // 다른 패키지에서 접근이 불가능함 -> default기 때문
    	
    	public PackTest() {} //기본 생성자
    	
    	public static void main(String[] args) {
    	}
    }
    ```
    
- default
    - 같은 패키지 내 다른 클래스에서 접근 가능함
- protected
    - default와 같지만 상속관계에 있는 클래스에서도 사용할 수 있도록 함
- private
    - 같은 클래스 내에서만 접근 가능
    - 외부로부터 데이터를 보호하기 위해 사용
    - setter, getter로 데이터를 control 할 수 있다(직접적인 접근은 불가능)
        - 필드는 특별한 이유가 없는 한 private임
        - 필드명 A에 대해 setter → setA() , getter → getA()

### 실습문제

```java
//1
public class Test01 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n1 = sc.nextInt(); //분자
        int n2 = sc.nextInt(); //분모

        if((double)n1%(double)n2 > 1) {
            System.out.println("나머지가 1보다 크다!");
        } else {
            System.out.println("나머지가 1보다 작거나 같다!");
        }
    }
}

//2
public class Test02 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();

        int res = 1;
        for(int i=2; i<=n; i++) {
            res *= i;
        }

        System.out.println(res);
    }
}

//3
public class Test03 {
    public static void main(String[] args) {
        for(int inx=0; inx<=6; inx++) {
            for(int jnx=0; jnx<=inx; jnx++) {
                System.out.print("*");
            }
            System.out.print("@");
        }
    }
}

//4
public class Test04 {
    public static void main(String[] args) {
        for(int i=1; i<=6; i++) {
            for(int j=1; j<=6; j++) {
                for(int k=1; k<=6; k++) {
                    if ((i*j*k) % 3 == 0) System.out.println(i+" * "+j+" * "+k + " = "+i*j*k);
                }
            }
        }
    }
}

//5
public class Test05 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int m = 10;
        int M = 0;
        for(int i=0; i<3; i++) {
            int num = sc.nextInt();
            if (M < num) M = num;
            if (m > num) m = num;
        }
        System.out.println("최대값: "+ M);
        System.out.println("최소값: "+ m);
    }
}

```

### **static block**

static block은 class가 로드된 후 main thread에 의해서 main method가 호출되기 이전에 실행됩니다. 

### 상속

- 부모 클래스 내용을 자식 클래스가 물려받는 것
- 부모 클래스(parent, super)
    - 상위 클래스
- 자식 클래스(child, sub)
    - 하위 클래스
- JAVA에서는 단일 상속만 허용
- 유지보수성이 좋음
- 생성자는 상속되지 않음
- 하위클래스의 객체를 만들면 상위(부모) 클래스의 생성자가 먼저 실행됨
- 서로 다른 패키지에 있다면 default 접근제어자로는 엑세스 불가능
