### 제어문 문제

- 1번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	/*1. 자신의 가산점을 확인할 수 있는 프로그램을 작성하시오
    		가산점을 부여하는 규칙은
    		여자의 가산점 부여 규칙 :
    	기혼자라면 + 1 기혼자로서 자녀수가 1이면 + 1, 자녀수가 2이면 + 2
    		남자의 가산점 부여 규칙 :
    	군제대자라면 + 1, 군제대자로서 기혼자라면 + 1*/
    
    	//bool
    	int isMarried, isMilitary;
    	int child;
    	char gender;
    
    	printf("성별을 입력하세요 (M/F) : ");
    	scanf("%c", &gender);
    
    	printf("결혼 여부를 입력하세요 (미혼 : 0, 기혼 : 1) : ");
    	scanf("%d", &isMarried);
    
    	if (isMarried == 1) {
    		printf("자녀수를 입력하세요 : ");
    		scanf("%d", &child);
    	}
    	else {
    		child = 0;
    	}
    
    	printf("군제대 여부를 입력하세요 (미필 : 0, 필 : 1) : ");
    	scanf("%d", &isMilitary);
    
    	int res = 0;
    	if (gender == 'F') {
    		if (isMarried == 1) {
    			res += child + 1;
    		}
    	}
    	else {
    		if (isMilitary == 1) {
    			res += 1;
    		}
    	}
    
    	printf("당신의 가산점은 %d점 입니다.\n", isMarried + child + isMilitary);
    }
     
    ```
    
- 2번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	int n;
    	printf("임의의 정수 n을 입력하시오 : ");
    	scanf("%d", &n);
    
    	double total = 0;
    	for (int i = 2; i < n; i++) {
    		printf("%d/%d + ", i - 1, i);
    		total += (i-1.0)/i;
    	}
    	printf("%d/%d = ", n - 1, n);
    	total += (n - 1.0) / n;
    
    	printf("%.1lf\n", total);
    }
    ```
    
- 3번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	//3. 학생 10명의 퀴즈 점수를 입력받은 후, 전체 평균과 통과자수, 탈락자수를 구해 출력하시
    	//	오.점수가 70점 이상이면 합격이다.실행결과
    	//	1번 퀴즈점수는 ? 90
    	//	2번 퀴즈점수는 ? 78
    	//	..10번 퀴즈점수는 ? 58
    	//	====================== 평균 : 81.10점
    	//	통과자 : 8명
    	//	탈락자 : 2명
    	
    	int score[10];
    	int sum = 0;
    	//cnt
    	int pass = 0;
    	int fail = 0;
    
    	for (int i = 0; i < 10; i++) {
    		printf("%d번 퀴즈 점수는 ? ", i + 1);
    		scanf_s("%d", &score[i]);
    		sum += score[i];
    		if (score[i] >= 70) {
    			pass++;
    		}
    		else {
    			fail++;
    		}
    	}
    
    	printf("====================== 평균 : %.2f점\n", (double)sum / 10);
    	printf("통과자 : %d명\n", pass);
    	printf("탈락자 : %d명\n", fail);
    }
    ```
    
- 4번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	// 넓이가 100cm2인 사각형 종이를 반으로 접을 때 최초로 1cm2보다 작아지는 횟수를 확인
    	/*할 수 있도록 넓이 변화를 출력하시오.힌트 1. 반으로 접으면 넓이가 1 / 2로 줄어든다.
    		2. 반으로 접었을 때 넓이가 1보다 작으면 접는 것을 그만둔다
    		결과화면
    		1번 접었을 때의 넓이는 50.00
    		2번 접었을 때의 넓이는 25.00
    		3번 접었을 때의 넓이는 12.50
    		..7번 접었을 때의 넓이는 0.78*/
    
    	double area = 100.0;
    	int count = 0;
    
    	while (area > 1.0) {
    		area /= 2.0;
    		printf("%d번 접었을 때의 넓이는 %.2lf\n", ++count, area);
    	}
    }
    ```
    
- 5번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	//5. 특정단의 구구단 출력하기
    	//	키보드로 1~9단 사이의 숫자를 입력받아 그 숫자에 해당되는 구구단을 출력하는 프로그램을
    	//	for문을 이용해 작성하시오
    	//	결과화면
    	//	출력을 원하는 단은 ? 3
    	//	3 X 1 = 3
    	//	..3 X 9 = 27
    
    	int n;
    	printf("출력을 원하는 단은 ? ");
    	scanf_s("%d", &n);
    
    	for (int i = 1; i <= 9; i++) {
    		printf("%d X %d = %d\n", n, i, n * i);
    	}
    }
    ```
    
- 6번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	//6. 주사위를 두 개 던졌을 때 합이 7이 되는 경우의 수를 출력하시오
    	//결과 화면
    	//	============== 
    	// 주사위1 주사위2
    	//	--------------
    	//	1 6
    	//	2 5
    	//	...
    	//	6 1 ------------총 6가지
    
    	int d1, d2;
    	int count = 0;
    
    	printf("==============\n");
    	printf("주사위1 주사위2\n");
    	printf("--------------\n");
    
    	for (d1 = 1; d1 <= 6; d1++) {
    		d2 = 7 - d1;
    		if (d2 >= 1 && d2 <= 6) {
    			printf("%d %d\n", d1, d2);
    			count++;
    		}
    	}
    }
    ```
    
- 7번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	for (int i = 1; i <= 26; i++) {
    		//공백
    		for (int j = 0; j < 26 - i; j++) {
    			printf(" ");
    		}
    		//알파벳
    		for (int j = 0; j < i; j++) {
    			char tar = 'z' - j;
    			printf("%c", tar);
    		}
    		printf("\n");
    	}
    }
    ```
    
- 8번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	//8. 1~n까지의 곱을 구합니다 정수 n은 ? 10
    	//	1부터 10까지의 곱은 3628800입니다
    	int n;
    	printf("1~n까지의 곱을 구합니다 정수 n은 ? ");
    	scanf("%d", &n);
    
    	int result = 1;
    	for (int i = 1; i <= n; i++) {
    		result *= i;
    	}
    
    	printf("1부터 %d까지의 곱은 %d입니다\n", n, result);
    }
    ```
    
- 9번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	/*9. 인의 반감기는 14일이다.연구소에 들어온 실험 물질에 포함된 인의 양은 100mg이었다.앞으로 n일후 인의 양이 최소 몇 mg일지를 구하는 프로그램을 작성하시오
    		힌트
    		현재 1일~14일 : 100mg / 2 = 50mg
    		15일~30일 : 50mg / 2 = 25mg이다
    		결과화면
    		현재 인의 양은 100mg이다
    		몇일째 되는 날의 인의 최소잔량을 알고 싶나요 ? 42
    		14일까지 인의 최소 잔량은 50mg입니다
    		28일까지의 인의 최소 잔량은 25mg입니다
    		42일까지의 인의 최소 잔량은 12.5mg입니다.그러므로 42일째에는 최소 12.5mg입니다*/
    
    	int n;
    	double p = 100;
    
    	printf("현재 인의 양은 100mg이다\n");
    	printf("몇일째 되는 날의 인의 최소잔량을 알고 싶나요 ? ");
    	scanf_s("%d", &n);
    
    	int i = 0;
    	while(1) {
    		i += 14;
    		if (i % 14 == 0) {
    			p /= 2.0;
    			printf("%d일까지 인의 최소 잔량은 %.1fmg입니다.\n", i, p);
    		}
    		if (i >= n) break;
    	}
    
    	printf("그러므로 %d일째에는 최소 %.1fmg입니다\n", n, p);
    }
    ```
    
- 10번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include<conio.h>
    #define ESC 0x1b
    
    int main() {
    	/*10. 알파벳 소문자를 입력받아 대문자로 바꾸는 프로그램을 작성하시오.입력된 문자가 소문
    		자가 아니면 다시 문자를 입력받고 ESC키(키값은 Ox1b)가 눌리면 프로그램을 종료하시오
    		결과 화면
    		알파벳 소문자를 입력하시오 a
    		변환된 대문자는 A입니다
    		알파벳 소문자를 입력하시오 ESC키를 치면 종료되게 한다*/
    
    	char ch;
    	while (1) {
    		printf("알파벳 소문자를 입력하시오 \n");
    		ch = _getch();
    
    		//esc
    		if (ch == ESC) {
    			printf("프로그램을 종료합니다.\n");
    			break;
    		}
    
    		if (ch >= 'a' && ch <= 'z') {
    			printf("변환된 대문자는 %c입니다\n", ch - 32);
    		}
    	}
    }
    ```
    
- 22번(GCD)
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    	int x, y;
    
    	printf("두 개의 정수를 입력하시오 ");
    	scanf("%d %d", &x ,&y);
    
    	if (y < x) {
    		int temp = x;
    		x = y;
    		y = temp;
    	}
    
    	while (y != 0) {
    		int temp = x % y;
    		x = y;
    		y = temp;
    	}
    
    	printf("최대공약수는 %d입니다", x);
    }
    ```
    

### 함수 예제

- 1번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    double sum(double a, double b) {
    	return a + b;
    }
    
    int sum2(int x, int y, int z) {
    	return x + y + z;
    }
    int main() {
    	double a, b, s;
    	printf("실수 2개 입력 : ");
    	scanf("%lf %lf", &a, &b);
    	s = sum(a, b);
    	printf("합 : %.1lf\n", s);
    
    	int x, y, z;
    	printf("정수 3개 입력 : ");
    	scanf("%d %d %d", &x, &y, &z);
    
    	int total = sum2(x, y, z);
    	printf("합 : %d", total);
    	return 0;
    }
    ```
    

### 포인터

**포인터**(pointer)란 **메모리의 주소값을 저장하는 변수**입니다.

포인터 변수는 `*` (참조연산자)를 사용하여 선언합니다

C언어에서 포인터는 다음 문법에 따라 선언합니다. 자료형은 포인터가 가리키고자 하는 변수의 자료형을 명시합니다.

**형식**

```
자료형 * 포인터이름;
```

초기화하지 않은 채로 참조 연산자를 사용하게 되면 의도하지 않은 메모리 장소에 값을 저장하는 것이 됩니다.

이러한 동작은 오류를 발생시킵니다. 따라서 다음과 같이 포인터의 선언과 동시에 초기화를 함께 하는 것이 좋습니다.

**형식**

```
자료형 * 포인터이름 = &변수이름;
자료형 * 포인터이름 = 주소값;
```

### 함수 문제

- 1번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #define _USE_MATH_DEFINES
    #include <math.h>
    int main() {
    	/*1. 0도부터 180도까지 사인 함수의 값을 출력하시오
    	(0도 30도 60도 90도 120도 150도 180도)
    		힌트 : main()함수에서 반복문으로 0도 30도등을 인수에 넣어서 함수로 호출하세요
    		math.h 헤더 파일을 사용하세요
    		결과화면
    		======== =
    		sin(0) = 0.0
    		sin(30) = 0.5
    		sin(45) = 0.836603
    		... sin(180) = 0.0*/
    
    	for (int i = 0; i <= 180; i += 30) {
    		printf("sin(%d) = %.1f\n", i, sin(i * M_PI / 180));
    	}
    }
    ```
    
- 2번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #define _USE_MATH_DEFINES
    #include <math.h>
    
    void salary(int wt) {
    	int salary;
    	if (wt >= 8) {
    		salary = 100000 + (wt - 8) * 10000;
    	}
    	else {
    		salary = wt * 10000;
    	}
    	printf("총 하루 임금은 %d원입니다.", salary);
    }
    int main() {
    	//2. 근로자의 오늘 하루 근로시간에 대한 하루 임금을 임금 기준에 따라 시급제와 일급제를 혼
    	//	영하여 계산하시오
    	//	임금기준
    	//	시급은 10000원이며 8시간 이상 일하면 일급으로 100000원을 적용한다
    	//	일급을 적용할 수 없는 나머지 시간을 시급을 적용한다.즉 10시간 일하면 8시간은 일급으로
    	//	나머지 2시간은 시급으로 계산한다.조건 : 총근로시간, 임금, 시급-- > 함수(총 근로시간으로부터 오늘의 임금을 계산하기)
    	//결과화면
    	//	근로시간은 ? 11
    	//	총 하루 임금은 130000원입니다.
    	int wt;
    
    	printf("근로시간은 ? ");
    	scanf("%d", &wt);
    	salary(wt);
    }
    ```
    
- 3번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #define _USE_MATH_DEFINES
    #include <math.h>
    
    void header();
    void content();
    int main() {
    	//3. 2025년 3월 달력을 출력하시오
    	header();
    	content();
    }
    
    void header() {
    	printf("2025년 3월 달력\n");
    	printf("===================\n");
    	printf("일 월 화 수 목 금 토\n");
    	printf("-------------------\n");
    }
    
    void content() {
    	int flag = 0;
    	for (int i = 1; i <= 6; i++) {
    		printf("   ");
    		flag++;
    	}
    	
    	for (int i = 1; i <= 31; i++) {
    		printf("%2d ", i);
    		flag++;
    		if (flag % 7 == 0) {
    			printf("\n");
    		}
    	}
    	printf("\n");
    }
    ```
    
- 4번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #define _USE_MATH_DEFINES
    #include <math.h>
    
    void days(int year);
    int leapYear(int year);
    int main() {
    	//4. 사용자가 입력한 연도가 365일까지 있는지 366일까지 있는지를 출력하시오
    
    	int year;
    	printf("연도를 입력하세요: ");
    	scanf("%d", &year);
    
    	days(year);
    }
    
    void days(int year) {
    	if (leapYear(year)) {
    		printf("입력하신 %d년은 366일까지 있습니다.",year);
    	}
    	else {
    		printf("입력하신 %d년은 365일까지 있습니다.", year);
    	}
    }
    
    int leapYear(int year) {
    	//4로 나누어 떨어지고 100으로 나누어 떨어지나, 400으로 나누어 떨어지면 윤년
    	return year % 4 == 0 || year % 100 == 0 || year % 400 == 0 ? 1 : 0;
    }
    ```
    
- 6번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int sum(int n) {
    	if (n == 1) {
    		return 1;
    	}
    	return sum(n - 1) + n;
    }
    int main() {
    	printf("n을 입력하세요: ");
    
    	int n;
    	scanf("%d", &n);
    	int total = sum(n);
    	printf("1부터 %d까지의 합은 %d입니다.\n", n, total);
    	return 0;
    }
    
    ```
    
- 7번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int sum(int n) {
    	if (n == 1) return 1;
    	return n * sum(n - 1);
    }
    int main() {
    	printf("n을 입력하세요: ");
    
    	int n;
    	scanf("%d", &n);
    
    	int total = sum(n);
    	printf("결과: %d\n", total);
    	return 0;
    }
    
    ```
    
- 8번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int fibo(int n) {
    	if (n == 0) return 0;
    	if (n == 1) return 1;
    
    	return fibo(n - 1) + fibo(n - 2);
    }
    int main() {
    	printf("n을 입력하세요: ");
    
    	int n;
    	scanf("%d", &n);
    
    	int total = fibo(n);
    	printf("결과: %d\n", total);
    	return 0;
    }
    
    ```
    
- 9번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int sum(int n, int t) {
    	if (t == 0) return 1;
    	return n * sum(n, t - 1);
    }
    int main() {
    	printf("정수를 입력하세요 ");
    
    	int n;
    	scanf("%d", &n);
    
    	printf("곱할 횟수를 입력하세요 ");
    	int t;
    	scanf("%d", &t);
    
    	int total = sum(n,t);
    	printf("정답은 %d", total);
    }
    ```
