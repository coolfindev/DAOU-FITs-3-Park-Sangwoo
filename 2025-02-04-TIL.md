### 자바 환경 세팅

- JDK
    - https://www.oracle.com/kr/java/technologies/javase/javase8-archive-downloads.html
- INTELLIJ
    - https://www.jetbrains.com/ko-kr/idea/download/download-thanks.html?platform=windows

### JAVA 언어 특징

- 운영체제에 독립적임; 동일한 코드를 여러 OS에서 재사용할 수 있음
- 하이레벨 LANGUAGE로 작성한 코드를 컴파일러 → 자바는 다른 언어와 다르게 머신코드로 변경되지 않고 `바이트코드`로 변경
- 바이트 코드를 JVM이 머신 코드로 변경해서 실행되기 때문에 JVM은 운영체제에 종속적임
- `.java` → (javac complier 활용) → `.class` → (JVM)
- strong type 언어(강타입  언어)로, 변수를 선언할 때 반드시 변수의 데이터 타입을 명시해야 함
    - 동적 데이터 타입을 지원하긴 하지만 참고만 할 것
- 대소문자 구분, 변수길이 제한이 없음

**절차지향과 객체지향**

- 절차지향의 대표적인 언어는 C
    - 기능으로 프로그램을 세분화함
    - 더 이상 분할 할 수 없는 단위 기능이 나오게 되는데, 이 단위 기능을 함수로 구현함
        - 요구사항이 명확하기 때문에 프로그램을 빠르고 쉽게 구현 가능함

- 위의== 패러다임을 변화하기 위해 객체지향 프로그래밍 등장
    - 현실세계의 문제를 모델링 해야하는 구현 방법론
    - 구성요소를 파악한 후 표현해야 함
        - 추상화(abstraction)를 거쳐서 나온 여러거지 정보, 행위를 구분
        - 이러한 정보, 행위는 class라는 단위로 묶여서 표현됨
        - 이러한 class가 모여서 하나의 자바 프로그램을 구성함
    - 사람의 생각에 따라서 설계가 달라질 수 있음
    - 해결해야 하는 현실세계를 프로그램으로 모델링하는 작업을 한 후에 프로그래밍 진행

 

**JVM, JRE, JDK**

- JVM
    - 바이트코드를 읽어들여서 실행하는 실행 주체
- JRE
    - 자바 실행환경
    - JVM + JAVA CLASS LIB
- JDK
    - JRE + 자바 개발 도구(JAVAC 컴파일러, JAVA 인터프리터, JAVADOC, …)

### 클래스

클래스의 이름을 파일 이름으로 명명

- 첫 글자는 대문자로 하자

```java
class Customer {
//fileds
//변수들

//생성자
//인스턴스를 생성하기 위해 필요

//methods
//함수처럼 사용
}
```

- 이 클래스로부터 저장공간을 생성해서 만들어지는 것을 인스턴스라고 함
- 클래스는 데이터 타입의 역할을 수행하는데, 고정된 데이터 타입이 아니므로 추상 데이터 타입(ADT)이라고 함
- 하나의 프로그램에는 상당히 많은 클래스가 존재함
    - 이러한 클래스를 효율적으로 관리하기 위해 package 존재
    - package는 소문자로만 작성
- 클래스 명은 파스칼 케이스, 변수 이름은 카멜 케이스
    - MyFriend/myFriend

### 데이터 타입

- primitive data type
    - 8개
        - int, long, double, float, boolean, byte, short, char
        - 위의 데이터 타입으로 선언된 변수 안에 데이터가 직접적으로 저장됨
- reference data type
    - java.lang.Object 클래스를 상속하는 모든 클래스
    - 클래스로 선언된 변수를 지칭
    - 지역변수/필드/…
    - 변수 안에 실제 값이 아닌 실제 메모리(X) 메모리에 대한 해시 값이 저장됨
        - 직접적인 엑세스 할 수 없도록

### 메모리 구조

가비지컬렉터가 다 쓴 메모리를 정리해 줌

1. method area 
    1. customer의 정보가 들어감
        1. age …
        2. myprint …
    2. 공간x 클래스에 대한 내용(바이트코드)이 들어감
2. stack
    1. 메인 메소드의 실질적인 값이 저장됨
        1. 지역변수
    2. 레퍼런스 변수는 메모리 값이 저장됨
3. heap
    1. 클래스 기반 인스턴스가 만들어짐
    2. fields가 저장됨

- 형변환
    - 묵시적
        - 작은 → 큰 타입으로 가는건 괜찮음
    - 명시적
        - 앞에 괄호를 활용함 → `int myData = (int) phi;`

### 클래스

- java는 전역변수라는 개념이 없음
- static
    - static은 필드와 메소드 앞에 붙을 수 있음
    - static이 있으면 method area에 정보가 아닌 값이 저장되어 인스턴스 없이도 접근할 수 있음
    
    ```java
    package mypackage;
    
    public class Customer {
        static int a = staticCall("1");  // --- (1) static field or static variable
        int b = staticCall("2"); // --- (2) instance variable
    
        // --- (3) static method
        static public int staticCall(String msg) {
            System.out.println(msg);
            return 100;
        }
    
        // --- (4) constructor
        public Customer() {
            this.b = staticCall("3");
        }
    
        public static void main(String[] args) {
            //System.out.println("temp"+Customer.a);
            System.out.println("4");
            int c = staticCall("5");
            Customer d = new Customer();
        }
    }
    
    ```
    
- 기본 생성자는 상속을 위해 명시적으로 작성해놓는 것이 좋음

### 오버로딩

- 같은 이름, 다른 용도의 메소드
- 생성자 오버로딩이 대표적임
    
    ```java
    public InstanceTest() {}
    public InstanceTest(int a) {}
    ```
