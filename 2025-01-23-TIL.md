### 인덱스

- 데이터베이스의 테이블 검색 성능을 향상시키고자 사용
- 책의 목차같은 역할
- 목적
    - 검색 성능 향상
    - 쿼리 실행 시간 단축
    - 특정 컬럼을 기준으로 데이터 정렬 및 관리
- 스캔 방식
    - 테이블 스캔(TABLE FULL SCAN)
        - 테이블 규모가 작을 때 보통 사용됨
        - 인덱스가 없는 경우 테이블의 모든 데이터를 처음부터 끝까지 읽어야 함
        - 데이터 양이 많아질 수록 성능 저하 발생
    - 인덱스 스캔(INDEX SCAN)
        - 인덱스를 사용하면 저장된 위치를 빠르게 탐색 가능
        - 테이블 일부만 읽기에 성능 향상
- 구조
    - B-TREE 인덱스(가장 일반적)
        - 균형 이진 트리로 데이터 검색에 최적화
        - 이런 방식으로 검색을 하면 데이터 위치를 빠르게 찾을 수 있다
- 접근 방식
    - SEQUENTIAL ACCESS
        - 데이터를 순서대로 읽거나 쓰는 방식
        - TABLE FULL SCAN
    - RANDOM ACCESS
        - 주소값을 찾아 테이블로 점프를 뛰는 순서와 무관한 접근 방식
- 비용
    - 루트 노드 → 수직적 탐색(BRANCH NODE) → 수평적 탐색(LEAF NODE) → 테이블 RANDOM ACCESS
- 장점
    - 검색 속도 향상
    - 정렬된 데이터 관리
    - 대용량 데이터 처리
- 단점
    - 추가 저장 공간 필요
    - 쓰기 작업의 성능 저하
    - 인덱스 과다 생성 문제 → 관리 비용 증가
- 유형
    - 단일 컬럼
        - 하나의 컬럼만 기준으로 생성
    - 결합
        - 두 개 이상의 조합으로 생성
    - 유니크
        - 중복을 허용하지 않는 고유한 인덱스
- 인덱스 관리 팁
    - 적절한 인덱스 개수 유지
    - 쓰기/읽기 비율 고려
    - 중복도 파악
        - 중복도가 낮은 컬럼은 B-TREE, 높은 컬럼은 BITMAP으로 확인
    - 자주 사용되는 컬럼에 생성 → WHERE, JOIN, ORDER BY, GROUP BY, …
- 유의 사항
    - CREATE, DROP은 운영시간 이후에 진행할 것
        - 실행 계획이 초기화 됨
    - 카디널리티
        - 값의 고유도가 높은 컬럼에 인덱스 생성
    - 컬럼의 순서
        - 자주 사용되는 컬럼을 우선적으로 배치
    - 데이터 크기, 접근 빈도
- 인덱스를 활용하지 못하게 하는 주요 원인
    - 인덱스 컬럼에 함수 또는 연산 사용
        
        ```sql
        --(X)
        select * from employees where round(salary) = 5000;
        --(O)
        select * from employees where salary between 4999.5 and 5000.5;
        ```
        
    - 데이터 타입 불일치
        - db 내부적으로 함수를 씌우기 때문에 인덱스로 사용되기 어려움
        
        ```sql
        --(X)
        select * from employees where employee_id = '101';
        --(O)
        select * from employees where employee_id = 101;
        ```
        
    - 와일드카드(%) 사용 위치
        - 앞에 위치한 경우 인덱스를 탈 수 없다
        - 예외는 있음
        
        ```sql
        --(X)
        select * from customers where name like '%John%';
        --(O)
        select * from customers where name like 'John%';
        ```
        
    - null 값 처리
        
        ```sql
        --(X)
        select * from employees where salary is null;
        --(O)
        alter table employees modify salary default 0;
        ```
        
    - or 조건이 포함된 경우
        
        ```sql
        --(X)
        select * from employees where department_id or manager_id = 5;
        --(O)
        select * from employees where department_id = 10
        union all
        select * from employees where manager_id = 5;
        ```
        

- REVERSE KEY INDEX
    - 인덱스에 저장되는 값을 반전시켜 저장
- CLUSTERED INDEX
    - 인덱스 순서에 따라 물리적으로 정렬되어 저장되는 인덱스
    - 로그 데이터와 같은 읽기 작업이 많은 테이블에 적합
- IOT(INDEX ORGANIZED TABLE)
    - 인덱스 자체에 모든 데이터가 저장됨
    - 테이블 데이터를 B-TREE 인덱스에 저장
    - PK 검색이 매우 빠르나, 그 외의 값 검색에는 느림
    - 별도 데이터, 인덱스 유지가 필요 없기에 저장 공간이 절약됨

### SQL 실전문제

```sql
--지점 별 승인된 대출의 총 금액을 계산한 뒤 해당 금액이 전체 지점에서 승인된 대출 금액의 평균보다 낮은 지점의 지점명과, 승인된 대출의 총 금액, 전체 지점에서 승인된 대출 금액의 평균을 출력하세요.
--평균은 반올림하여 소수점 아래 둘째자리까지 표현합니다.

--지점 별 승인된 대출의 총 금액
--전체 지점 승인된 대출의 총 금액
--전체 지점 승인된 대출 금액의 평균
--지점명
SELECT NAME, TOTAL_AMOUNT, TOTAL_AVG
FROM
(
SELECT
    (SELECT B.NAME FROM BRANCHES B WHERE B.BRANCH_ID = L.BRANCH_ID) NAME, 
    SUM(L.AMOUNT) OVER(PARTITION BY BRANCH_ID) TOTAL_AMOUNT,
    ROUND(AVG(L.AMOUNT) OVER(),2) TOTAL_AVG
FROM LOANS L
WHERE STATUS = 'APPROVED'
ORDER BY L.BRANCH_ID
)
WHERE TOTAL_AMOUNT < TOTAL_AVG;

--모든 직원의 사번과 이름을 출력하되 각 직원의 상사의 이름과 상사가 속한 부서의 이름도 함께 출력하세요

SELECT E1.EMPLOYEE_ID 사번, E1.NAME 이름, NVL(E2.NAME, 'NO MANAGER') 상사이름, NVL(D.DEPARTMENT_NAME, 'NO DEPARTMENT') 상사부서
FROM EMPLOYEES E1
LEFT JOIN EMPLOYEES E2
ON E1.MANAGER_ID = E2.EMPLOYEE_ID
LEFT JOIN DEPARTMENTS D
ON E2.DEPARTMENT_ID  = D.DEPARTMENT_ID
ORDER BY 사번;

-- 각 직원의 급여와 그 급여가 전체 직원 급여에서 차지하는 백분율 계산
SELECT EMPLOYEE_ID 사번, NAME 이름, SALARY 급여, ROUND((SALARY/TOTAL)*100,2) 백분율
FROM
(SELECT EMPLOYEE_ID, NAME, SALARY, SUM(SALARY) OVER() TOTAL
FROM EMPLOYEES
WHERE SALARY IS NOT NULL
)
ORDER BY 백분율 DESC;

--각 계좌의 최고 거래 금액이 높은 순으로 상위 5위까지의 계좌를 출력하세요

SELECT ACCOUNT_ID 계좌ID, MAX_AMOUNT 최고거래금액
FROM (
    SELECT ACCOUNT_ID,
    MAX(AMOUNT) MAX_AMOUNT,
    DENSE_RANK() OVER(ORDER BY MAX(AMOUNT) DESC) RANK_AMOUNT
    FROM TRANSACTIONS
    GROUP BY ACCOUNT_ID
    )
WHERE RANK_AMOUNT <= 5;

-- 각 부서에서 급여가 가장 높은 직원의 급여와 가장 낮은 직원의 급여 차이를 계산, 출력하세요
SELECT MAX(SALARY) - MIN(SALARY) 급여차
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
ORDER BY DEPARTMENT_ID;

-- 각 부서에서 급여가 가장 높은 직원의 급여와 가장 낮은 직원의 급여 차이를 계산, 출력하세요
SELECT EMPLOYEE_ID 사번, NAME 이름, SALARY 급여, AVG(SALARY) OVER (PARTITION BY DEPARTMENT_ID) 부서평균급여,
    ROUND((SALARY-AVG(SALARY) OVER (PARTITION BY DEPARTMENT_ID)),2) 급여의차이 ,
    CASE
        WHEN SALARY > AVG(SALARY) OVER (PARTITION BY DEPARTMENT_ID) THEN '양수'
        ELSE '음수'
    END 평균급여차이 
FROM EMPLOYEES
ORDER BY DEPARTMENT_ID;

--이름이 EMPLOYEE 241인 사원의 모든 상사를 출력하세요
SELECT 상사들
FROM
(SELECT 
    SYS_CONNECT_BY_PATH(E.NAME, ' > ') 상사들,
    LEVEL L
FROM EMPLOYEES E
START WITH E.NAME = 'Employee 241'
CONNECT BY PRIOR MANAGER_ID = EMPLOYEE_ID
)
WHERE L = 5;
-------------------------------
SELECT 상사들
FROM
(
    SELECT RANK() OVER (ORDER BY 상사들 DESC) RANKS,
    상사들
    FROM
    (
        SELECT 
            SYS_CONNECT_BY_PATH(E.NAME, ' > ') 상사들
        FROM EMPLOYEES E
        START WITH E.NAME = 'Employee 241'
        CONNECT BY PRIOR MANAGER_ID = EMPLOYEE_ID
    )
)WHERE RANKS=1;

--사원 테이블에서 급여가 높은 상위 10명의 사원 정보를 출력하세요. 급여가 동일할 경우 사원 이름 순으로 정렬하세요.

SELECT NAME, SALARY
FROM
(
    SELECT NAME, SALARY, DENSE_RANK() OVER(ORDER BY SALARY DESC NULLS LAST, NAME) RANKS
    FROM EMPLOYEES
)
WHERE RANKS <= 10;

-- 입사 년도 별 급여 합계를 출력하고 전체 급여 합계를 출력하세요

SELECT NVL(SUBSTR(TO_CHAR(HIRE_DATE),1, 2), 'ALL YEARS') HIRE_YEAR, SUM(SALARY) SUM_SALARY
FROM EMPLOYEES
GROUP BY ROLLUP(SUBSTR(TO_CHAR(HIRE_DATE),1, 2))
ORDER BY HIRE_YEAR;

-- 부서별 급여합계, 직무별 급여합계 그리고 전체 급여합계를 출력하세요.
SELECT
    (SELECT D.DEPARTMENT_NAME FROM DEPARTMENTS D WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID),
    JOB_ID,
    SUM(SALARY)
FROM EMPLOYEES E
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY GROUPING SETS(DEPARTMENT_ID, JOB_ID)
ORDER BY DEPARTMENT_ID, JOB_ID;

--부서별 급여합계, 직무별 급여합계 그리고 전체 급여합계를 출력하세요.

SELECT DEPARTMENT_ID, NAME, SALARY, SUM(SALARY) OVER(PARTITION BY DEPARTMENT_ID ORDER BY SALARY RANGE BETWEEN 100 PRECEDING AND 100 FOLLOWING) 부서내급여합
FROM EMPLOYEES;

--직무별 평균 급여와 직원의 급여를 비교하여 표시합니다.

SELECT 
    EMPLOYEE_ID,
    SALARY,
    SUM(SALARY) OVER(PARTITION BY JOB_ID),
    CASE
    WHEN SALARY > AVG(SALARY) OVER (PARTITION BY JOB_ID) THEN '직무 평균 이상'
    ELSE '직무 평균 이하'
    END 직무,
    
    CASE
    WHEN SALARY > AVG(SALARY) OVER () THEN '전체 평균 이상'
    ELSE '전체 평균 이하'
    END 전체
FROM EMPLOYEES;
```
