### 실습

```c
//실습과제 6
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	char gender;
	int age;
	float hei;
	printf("성별은? (남자라면 M 여자라면 F) >> ");
	gender = getchar();
	getchar();
	printf("나이는? ");
	scanf("%d", &age);
	getchar();
	printf("키는? ");
	scanf("%f", &hei);
	getchar();
	printf("\n=================================\n");
	

	printf("성별: %c", gender);
	printf("나이: %d세", age);
	printf("키: % .1fcm", hei);
}

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	//정수 1개 입력 받아서 짝수 홀수 출력 삼항연산자
	int n;
	printf("짝수? 홀수? ");
	scanf_s("%d", &n);

	printf(n % 2 == 0 ? "짝수" : "홀수");
}

//실습과제 8
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	char name[10];
	int year;
	double GPA, score;

	printf("이름은? ");
	gets(name);
	printf("몇 학년? ");
	scanf("%d", &year);
	printf("%d학년 평점은? ", year);
	scanf("%lf", &GPA);

	score = (GPA * 100) / 4.5;

	printf("\n===============================\n");
	printf("이름  학년  평점(점수)");
	printf("\n===============================\n");
	printf("%-10s %d %7.1lf (%5.1lf)", name, year, GPA, score);
	printf("\n===============================\n");

	return 0;
}
```

## C 언어 연산자 정리

### 1. 산술 연산자 (Arithmetic Operators)

| 연산자 | 설명 | 예시 |
| --- | --- | --- |
| `+` | 덧셈 | `a + b` |
| `-` | 뺄셈 | `a - b` |
| `*` | 곱셈 | `a * b` |
| `/` | 나눗셈 | `a / b` |
| `%` | 나머지 | `a % b` |

### 2. 관계 연산자 (Relational Operators)

| 연산자 | 설명 | 예시 |
| --- | --- | --- |
| `==` | 같음 | `a == b` |
| `!=` | 다름 | `a != b` |
| `>` | 큼 | `a > b` |
| `<` | 작음 | `a < b` |
| `>=` | 크거나 같음 | `a >= b` |
| `<=` | 작거나 같음 | `a <= b` |

### 3. 논리 연산자 (Logical Operators)

| 연산자 | 설명 | 예시 |
| --- | --- | --- |
| `&&` | 논리 AND | `(a > 0) && (b > 0)` |
| `\|\|` | 논리 OR | `(a > 0) \|\| (b > 0)` |
| `!` | 논리 NOT | `!(a > 0)` |

### 4. 비트 연산자 (Bitwise Operators)

| 연산자 | 설명 | 예시 |
| --- | --- | --- |
| `&` | 비트 AND | `a & b` |
| `\|` | 비트 OR | `a \| b` |
| `^` | 비트 XOR | `a ^ b` |
| `~` | 비트 NOT | `~a` |
| `<<` | 왼쪽 시프트 | `a << 2` |
| `>>` | 오른쪽 시프트 | `a >> 2` |

### 5. 대입 연산자 (Assignment Operators)

| 연산자 | 설명 | 예시 |
| --- | --- | --- |
| `=` | 대입 | `a = b` |
| `+=` | 덧셈 후 대입 | `a += b` (→ `a = a + b`) |
| `-=` | 뺄셈 후 대입 | `a -= b` (→ `a = a - b`) |
| `*=` | 곱셈 후 대입 | `a *= b` (→ `a = a * b`) |
| `/=` | 나눗셈 후 대입 | `a /= b` (→ `a = a / b`) |
| `%=` | 나머지 후 대입 | `a %= b` (→ `a = a % b`) |

### 6. 삼항 연산자 (Ternary Operator)

```c
조건 ? 참일 때 값 : 거짓일 때 값;
```

### 문제

```c
//문1 연산자 = 와 ==의 차이점은?
//대입과 비교

//문2 다음 연산자를 우선순위가 높은 순서대로 정리하시오
//증감 > 산술 > 비트 > 관계 > 논리 > 조건(삼항) > 대입 > 콤마

//문3 코드 실행 결과
//3-1 ) 8 4
//3-2 ) 5 31 26
//3-3 ) x=1 y=2 z=5
//3-4 ) y=2 y=2 z=2.0 z=2.5 z=2.0

//문4 킬로그램 단위의 무게를 입력받아 파운드(lb)단위로 출력하는 프로그램을 작성하시오 소스 1자리
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	double kg; 

	printf("kg단위의 무게를 입력하세요 : ");
	scanf_s("%lf", &kg);

	printf("입력하신 무게 %.1lfkg은 %.1lflb에 해당합니다", kg, kg*2.205);
	return 0;
}

//문5 원기둥의 밑면의 반지름과 높이를 입력받아 부피를 계산하는 프로그램을 작성하시오
#define _CRT_SECURE_NO_WARNINGS
#define _USE_MATH_DEFINES
#include <stdio.h>
#include <math.h>

int main()
{
	double r; 

	printf("원기둥의 밑면의 반지름 높이를 입력하세요(cm 단위) : ");
	scanf_s("%lf", &r);

	printf("계산한 원기둥의 부피는 %.1lfcm입니다", r*r*r*M_PI);
	return 0;
}

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	//키보드로 월을 입력 받아 입력 받은 월이 상반지인지 하반지인지 판단하여 출력하는 프로그램을 작성하시오. 

	int mon;

	printf("월(1~12) 입력 : ");
	scanf_s("%d", &mon);

	printf(mon < 7 ? "%d월은 상반기입니다." : "%d월은 하반기입니다.", mon);
	return 0;
}

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	//문7 기가바이트 수치를 입력받아 입력한 값이 몇 메가바이트인지, 몇 킬로바이트인지 계산하는 프로그램을 작성하시오.

	int g;

	printf("입력(기가바이트) : ");
	scanf_s("%d", &g);

	printf("%d기가바이트 = %d메가바이트 = %d킬로바이트", g, g*1024, g*1024*1024);
	return 0;
}

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	//문8 초 단위의 시간을 입력 받아 몇시간 몇분 몇초인지를 계산하는 프로그램을 작성하시오

	int sec;

	printf("초단위 입력 : ");
	scanf_s("%d", &sec);

	printf("입력한 %d초는 %d시간 %d분 %d초입니다", sec, (int)sec/60/60, (int)sec/60%60, sec%60);
	return 0;
}

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	//문9 다음 수식을 계산하는 프로그램 작성하시오
	//f(x) - 2x ^ 3 – 3x ^ 2 + 1 / x

	int x;

	printf("x입력 : ");
	scanf_s("%d", &x);

	printf("x가 %d일 때 값은 %.1lf입니다", x, 2*x*x*x - 3*x*x + 1.0/x);
	return 0;
}

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    char ch;
    printf("문자 입력 >> ");
    scanf("%c", &ch);
    printf("%c %d \n", ch, ch);
    printf("%2d", ch & (1 << 7) ? 1 : 0);
    printf("%2d", ch & (1 << 6) ? 1 : 0);
    printf("%2d", ch & (1 << 5) ? 1 : 0);
    printf("%2d", ch & (1 << 4) ? 1 : 0);
    printf("%2d", ch & (1 << 3) ? 1 : 0);
    printf("%2d", ch & (1 << 2) ? 1 : 0);
    printf("%2d", ch & (1 << 1) ? 1 : 0);
    printf("%2d", ch & (1) ? 1 : 0);

    return 0;
}

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    int n;
    printf("정수 입력 >> ");
    scanf("%d", &n);

    // 비트 오른쪽 회전
    int rotated = (n >> 1) | ((n & 1) << 15); // 오른쪽 1비트 회전

    // (n>>1)로 오른쪽으로 이동시킨 비트
    // (n&1) 현재 맨 오른쪽 값 추출하고 15비트 왼쪽으로 이동
    // 이를 or로 결합하여 회전한 것 같은 효과

    printf("오른쪽 회전된 비트: ");
    for (int i = 15; i >= 0; i--) {
        printf("%d", (rotated & (1 << i)) ? 1 : 0);

        if (i % 4 == 0) printf(" ");
    }
    printf("\n");

    return 0;
}

```
