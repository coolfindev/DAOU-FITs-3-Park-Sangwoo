## 전처리기

- C언어에서 전처리기는 컴파일 전에 소스 코드에 특정 작업을 수행하는 기능
    - 이를 통해 코드의 유지보수성을 높이고 중복을 줄이며 효율적인 컴파일을 가능
- #include
    - 여러 파일에서 공통적으로 사용되는 헤더 파일을 쉽게 포함
    - 코드가 모듈화되어 유지보수가 쉬워지며 반복되는 코드를 제거할 때 이용함
    - 함수 및 구조체를 헤더 파일로 분리
    - 팀 프로젝트시 공통 헤더를 공유할 때 이용
    - 예) #include <stdio.h> 표준 라이브러리 포함, #include “add.h” 사용자 정의 헤더 포함
- #define
    - 코드 간소화 및 가독성을 향상할 때 이용
    - 매크로를 사용하면 상수 및 함수를 간결하게 정의 가능
    - 상수를 정의하여 의미 있는 코드를 작성 가능
    - 매크로 함수로 반복되는 연산을 줄일 수 있음
    - 예) #define PI 3.14159, #define POW(X) ( (X) * (X) )
- #ifdef, #ifndef, #if
    - 특정 조건에 따라 코드 실행을 다르게 설정할 수 있음
    - 예시
        
        ```c
        #pragma once
        #ifndef MYHEADER_H
        #define MYHEADER_H
        
        void hello();
        #endif
        ```
        
- 디버깅 코드관리
- 운영체제 또는 환경에 따른 코드 분기 처리

```c
#include <stdio.h>
#define DEBUG // 이 줄을 주석 처리하면 디버깅 로그가 출력되지 않음
void save_trades(const char* filename) {
	FILE* file = fopen(filename, "w");
#ifdef DEBUG
	printf("[DEBUG] 파일을 열고 있습니다: %s\n", filename);
#endif
	if (file == NULL) {
		printf("파일을 열 수 없습니다!\n");
		return;
	}
	fprintf(file, "거래 내역: 100,000원 입금\n");
#ifdef DEBUG
	printf("[DEBUG] 파일에 데이터를 기록했습니다.\n");
#endif
	fclose(file);
#ifdef DEBUG
	printf("[DEBUG] 파일을 닫았습니다.\n");
#endif
	printf("거래 내역이 저장되었습니다!\n");
}
int main() {
	save_trades("trades.txt");
	return 0;
}
```

- #define 예제 1
    
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    
    #define OS_WINDOWS
    
    int main() {
    #ifdef OS_WINDOWS
        system("cls");
        printf("윈도우 환경입니다.\n");
    
    #elif defined(OS_LINUX) || defined(OS_MAC)
        system("clear");
        printf("Linux / Max OS환경입니다.\n");
    
    #else
        #error 지원되지 않은 운영체제입니다
    #endif
    
        return 0;
    }
    
    ```
    
- 예제 2
    
    ```c
    #include <stdio.h>
    // 수수료를 사용자가 정의하지 않았다면 기본값(2%) 적용
    // 이 부분을 작성하세요
    // 만약에 TRANSACTION_FEE 정의 되지 않았다면
    // TRANSACTION_FEE를 0.02로 매크로 상수로 만드세요. ______________________________________
    #ifndef TRANSACTION_FEE
    #define TRANSACTION_FEE 0.02
    #endif
    
    double calculate_fee(double amount) {
    	return amount * TRANSACTION_FEE;
    }
    int main() {
    	double amount = 500000; // 거래 금액 50만 원
    	double fee = calculate_fee(amount);
    	printf("거래 금액: %.2f원\n", amount);
    	printf("적용된 수수료 (%.2f%%): %.2f원\n", TRANSACTION_FEE * 100, fee);
    	return 0;
    }
    ```
    
- 예제3
    
    ```c
    //[문제3] 개발 환경에 따라 로그 출력 여부 결정하기
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #define DEBUG
    
    void debug();
    void release();
    
    int main() {
    #ifdef DEBUG
    	debug();
    #else
    	release();
    #endif
    }
    
    void debug() {
    	printf("#define DEBUG활성화 시 \n");
    	printf("거래할 금액 입력: ");
    	double amount;
    	scanf("%lf", &amount);
    	FILE* file = fopen("debug.txt", "w");
    	if (file == NULL) {
    		printf("파일을 열 수 없습니다!\n");
    		return;
    	}
    
    	printf("거래 완료 %.1lf원\n", amount);
    	fprintf(file,"%.1lf원", amount);
    	printf("[DEBUG] 거래 로그가 저장되었습니다.\n");
    	fclose(file);
    }
    
    void release() {
    	printf("#define DEBUG 주석 시 RELEASE 모드\n");
    	printf("거래할 금액 입력: ");
    	double amount;
    	scanf("%lf", &amount);
    	printf("거래 완료 %.1lf원", amount);
    }
    ```
    
- 예제4
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #define USER_NORMAL
    #define USER_VIP
    
    int LIMIT = 5000000;
    
    void normal() {
    	if (LIMIT > 1000000) {
    		printf("한도 초과입니다.\n");
    	}
    	else {
    		printf("일반 사용자입니다.\n");
    		printf("정상적으로 거래되었습니다.");
    	}
    }
    
    void vip() {
    	if (LIMIT > 10000000) {
    		printf("한도 초과입니다.\n");
    	}
    	else {
    		printf("VIP 사용자입니다.\n");
    		printf("정상적으로 거래되었습니다.");
    	}
    }
    
    void def() {
    	if (LIMIT > 5000000) {
    		printf("한도 초과입니다.\n");
    	}
    	else {
    		printf("기본 사용자입니다.\n");
    		printf("정삭적으로 거래되었습니다.");
    	}
    }
    
    int main() {
    	printf("거래 금액을 입력하세요: ");
    	scanf("%d", &LIMIT);
    #ifdef USER_NORMAL
    	normal();
    #elif USER_VIP
    	vip();
    #else
    	def();
    #endif
    }
    ```
    

## 모듈화

- 모둘화는 프로그램을 독립적인 기능을 수행하는 작은 단위(모듈)로 나누어 개발하는 소프트
웨어 설계 기법
- 모듈은 각각의 기능을 담당하며 서로 독립적으로 작성,수정,테스트할 수 있어 유지보수성과
재사용성이 증가
- 논리적으로 같은 등급의 소스 파일을 계속해서 분할하는 것
- 소스파일에서 혹은 어떤 함수에서 일어나는 일의 책임을 그 소스 파일과 그 함수가 지도록
하는 것

**기준**

1. 하나의 모듈은 한 가지 역할만 수행해야 한다.
2. 인터페이스와 구현 분리, 헤더파일과 소스파일 분리한다.
3. 높은 응집도와 낮은 결합도, 관련 기능끼리 묶고 모듈 간 의존성을 최소화한다. 
4. 재사용성을 고려하여 공통의 기능은 별도의 모듈로 분리한다.
5. 변경 가능성을 고려하고 확장 가능하도록 설계하여 유지보수를 쉽게 한다.

- 모듈화 문제1
    - header
        
        ```c
        #ifndef FIXED_DEPOSIT_H
        #define FIXED_DEPOSIT_H
        
        #include <time.h>
        
        #define MAX_DEPOSITS 5
        
        typedef struct {
            char name[30];
            char account_number[11];
            struct tm start_date;
            int duration_months;
            double amount;
        } FixedDeposit;
        
        void add_months(struct tm* date, int months);
        void sort_by_maturity_date(FixedDeposit deposits[], int n);
        void check_maturity(FixedDeposit* deposit);
        void display_deposits(FixedDeposit deposits[], int n);
        
        #endif // FIXED_DEPOSIT_H
        
        ```
        
    - main
        
        ```c
        #define _CRT_SECURE_NO_WARNINGS
        #include <stdio.h>
        #include "myheader.h"
        
        int main() {
            FixedDeposit deposits[MAX_DEPOSITS] = {
                {"김철수", "1234567890", {0, 0, 0, 10, 0, 124}, 12, 5000000},
                {"이영희", "9876543210", {0, 0, 0, 1, 6, 123}, 24, 3000000},
                {"박민수", "5678901234", {0, 0, 0, 15, 2, 123}, 18, 2000000},
                {"조수빈", "3456789012", {0, 0, 0, 5, 8, 123}, 6, 1000000},
                {"정하나", "6789012345", {0, 0, 0, 20, 10, 123}, 12, 7000000}
            };
            sort_by_maturity_date(deposits, MAX_DEPOSITS);
            display_deposits(deposits, MAX_DEPOSITS);
            return 0;
        }
        
        ```
        
    - deposit
        
        ```c
        #include "myheader.h"
        #include <stdio.h>
        #include <time.h>
        
        void add_months(struct tm* date, int months) {
            date->tm_mon += months;
            mktime(date);
        }
        
        void sort_by_maturity_date(FixedDeposit deposits[], int n) {
            for (int i = 0; i < n - 1; i++) {
                for (int j = 0; j < n - i - 1; j++) {
                    struct tm d1 = deposits[j].start_date;
                    struct tm d2 = deposits[j + 1].start_date;
                    add_months(&d1, deposits[j].duration_months);
                    add_months(&d2, deposits[j + 1].duration_months);
                    if (mktime(&d1) > mktime(&d2)) {
                        FixedDeposit temp = deposits[j];
                        deposits[j] = deposits[j + 1];
                        deposits[j + 1] = temp;
                    }
                }
            }
        }
        
        void check_maturity(FixedDeposit* deposit) {
            time_t now = time(NULL);
            struct tm maturity_date = deposit->start_date;
            add_months(&maturity_date, deposit->duration_months);
            if (difftime(mktime(&maturity_date), now) <= 0) {
                printf("만기 도래 ✅ | ");
            }
            else {
                printf("만기 대기  | ");
            }
        }
        
        void display_deposits(FixedDeposit deposits[], int n) {
            printf("\n[ 정기 예금 목록 (만기일 빠른 순) ]\n");
            for (int i = 0; i < n; i++) {
                struct tm maturity_date = deposits[i].start_date;
                add_months(&maturity_date, deposits[i].duration_months);
                printf("%d. %s | 계좌: %s | 예금 시작: %04d-%02d-%02d | 만기: %04d-%02d-%02d | 금액: %.2lf | ",
                    i + 1, deposits[i].name, deposits[i].account_number,
                    deposits[i].start_date.tm_year + 1900, deposits[i].start_date.tm_mon + 1,
                    deposits[i].start_date.tm_mday,
        	            maturity_date.tm_year + 1900, maturity_date.tm_mon + 1,
                    maturity_date.tm_mday,
                    deposits[i].amount);
                check_maturity(&deposits[i]);
                printf("\n");
            }
        }
        
        ```
