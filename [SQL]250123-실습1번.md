### 지점 별 승인된 대출의 총 금액을 계산한 뒤 해당 금액이 전체 지점에서 승인된 대출 금액의 평균보다 낮은 지점의 지점명과, 승인된 대출의 총 금액, 전체 지점에서 승인된 대출 금액의 평균을 출력하세요. 평균은 반올림하여 소수점 아래 둘째자리까지 표현합니다.

```SQL
SELECT NAME, TOTAL_AMOUNT, TOTAL_AVG
FROM
(
SELECT
    (SELECT B.NAME FROM BRANCHES B WHERE B.BRANCH_ID = L.BRANCH_ID) NAME, 
    SUM(L.AMOUNT) OVER(PARTITION BY BRANCH_ID) TOTAL_AMOUNT,
    ROUND(AVG(L.AMOUNT) OVER(),2) TOTAL_AVG
FROM LOANS L
WHERE STATUS = 'APPROVED'
ORDER BY L.BRANCH_ID
)
WHERE TOTAL_AMOUNT < TOTAL_AVG;
```

### 모든 직원의 사번과 이름을 출력하되 각 직원의 상사의 이름과 상사가 속한 부서의 이름도 함께 출력하세요

```SQL
SELECT E1.EMPLOYEE_ID 사번, E1.NAME 이름, NVL(E2.NAME, 'NO MANAGER') 상사이름, NVL(D.DEPARTMENT_NAME, 'NO DEPARTMENT') 상사부서
FROM EMPLOYEES E1
LEFT JOIN EMPLOYEES E2
ON E1.MANAGER_ID = E2.EMPLOYEE_ID
LEFT JOIN DEPARTMENTS D
ON E2.DEPARTMENT_ID  = D.DEPARTMENT_ID
ORDER BY 사번;
```

### 각 직원의 급여와 그 급여가 전체 직원 급여에서 차지하는 백분율 계산

```SQL
SELECT EMPLOYEE_ID 사번, NAME 이름, SALARY 급여, ROUND((SALARY/TOTAL)*100,2) 백분율
FROM
(SELECT EMPLOYEE_ID, NAME, SALARY, SUM(SALARY) OVER() TOTAL
FROM EMPLOYEES
WHERE SALARY IS NOT NULL
)
ORDER BY 백분율 DESC;
```

### 각 계좌의 최고 거래 금액이 높은 순으로 상위 5위까지의 계좌를 출력하세요

```SQL
SELECT ACCOUNT_ID 계좌ID, MAX_AMOUNT 최고거래금액
FROM (
    SELECT ACCOUNT_ID,
    MAX(AMOUNT) MAX_AMOUNT,
    DENSE_RANK() OVER(ORDER BY MAX(AMOUNT) DESC) RANK_AMOUNT
    FROM TRANSACTIONS
    GROUP BY ACCOUNT_ID
    )
WHERE RANK_AMOUNT <= 5;
```

### 각 부서에서 급여가 가장 높은 직원의 급여와 가장 낮은 직원의 급여 차이를 계산, 출력하세요

```SQL
SELECT MAX(SALARY) - MIN(SALARY) 급여차
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
ORDER BY DEPARTMENT_ID;
```

### 각 부서에서 급여가 가장 높은 직원의 급여와 가장 낮은 직원의 급여 차이를 계산, 출력하세요

```SQL
SELECT EMPLOYEE_ID 사번, NAME 이름, SALARY 급여, AVG(SALARY) OVER (PARTITION BY DEPARTMENT_ID) 부서평균급여,
    ROUND((SALARY-AVG(SALARY) OVER (PARTITION BY DEPARTMENT_ID)),2) 급여의차이 ,
    CASE
        WHEN SALARY > AVG(SALARY) OVER (PARTITION BY DEPARTMENT_ID) THEN '양수'
        ELSE '음수'
    END 평균급여차이 
FROM EMPLOYEES
ORDER BY DEPARTMENT_ID;
```

### 이름이 EMPLOYEE 241인 사원의 모든 상사를 출력하세요

```SQL
SELECT 상사들
FROM
(SELECT 
    SYS_CONNECT_BY_PATH(E.NAME, ' > ') 상사들,
    LEVEL L
FROM EMPLOYEES E
START WITH E.NAME = 'Employee 241'
CONNECT BY PRIOR MANAGER_ID = EMPLOYEE_ID
)
WHERE L = 5;

SELECT 상사들
FROM
(
    SELECT RANK() OVER (ORDER BY 상사들 DESC) RANKS,
    상사들
    FROM
    (
        SELECT 
            SYS_CONNECT_BY_PATH(E.NAME, ' > ') 상사들
        FROM EMPLOYEES E
        START WITH E.NAME = 'Employee 241'
        CONNECT BY PRIOR MANAGER_ID = EMPLOYEE_ID
    )
)WHERE RANKS=1;
```

### 사원 테이블에서 급여가 높은 상위 10명의 사원 정보를 출력하세요. 급여가 동일할 경우 사원 이름 순으로 정렬하세요.

```SQL
SELECT NAME, SALARY
FROM
(
    SELECT NAME, SALARY, DENSE_RANK() OVER(ORDER BY SALARY DESC NULLS LAST, NAME) RANKS
    FROM EMPLOYEES
)
WHERE RANKS <= 10;
```

### 입사 년도 별 급여 합계를 출력하고 전체 급여 합계를 출력하세요

```SQL
SELECT NVL(SUBSTR(TO_CHAR(HIRE_DATE),1, 2), 'ALL YEARS') HIRE_YEAR, SUM(SALARY) SUM_SALARY
FROM EMPLOYEES
GROUP BY ROLLUP(SUBSTR(TO_CHAR(HIRE_DATE),1, 2))
ORDER BY HIRE_YEAR;
```

### 부서별 급여합계, 직무별 급여합계 그리고 전체 급여합계를 출력하세요.

```SQL
SELECT
    (SELECT D.DEPARTMENT_NAME FROM DEPARTMENTS D WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID),
    JOB_ID,
    SUM(SALARY)
FROM EMPLOYEES E
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY GROUPING SETS(DEPARTMENT_ID, JOB_ID)
ORDER BY DEPARTMENT_ID, JOB_ID;
```

### 부서별 급여합계, 직무별 급여합계 그리고 전체 급여합계를 출력하세요.

```SQL
SELECT DEPARTMENT_ID, NAME, SALARY, SUM(SALARY) OVER(PARTITION BY DEPARTMENT_ID ORDER BY SALARY RANGE BETWEEN 100 PRECEDING AND 100 FOLLOWING) 부서내급여합
FROM EMPLOYEES;
```

### 직무별 평균 급여와 직원의 급여를 비교하여 표시합니다.

```SQL
SELECT 
    EMPLOYEE_ID,
    SALARY,
    SUM(SALARY) OVER(PARTITION BY JOB_ID),
    CASE
    WHEN SALARY > AVG(SALARY) OVER (PARTITION BY JOB_ID) THEN '직무 평균 이상'
    ELSE '직무 평균 이하'
    END 직무,
    
    CASE
    WHEN SALARY > AVG(SALARY) OVER () THEN '전체 평균 이상'
    ELSE '전체 평균 이하'
    END 전체
FROM EMPLOYEES;
```
