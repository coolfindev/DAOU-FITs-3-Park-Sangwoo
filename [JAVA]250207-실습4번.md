### Plane.java

```java
package javaexam2.plane;

public abstract class Plane {
    private String planeName;
    private int fuelSize;

    public Plane() {}
    public Plane(String planeName, int fuelSize) {
        this.planeName = planeName;
        this.fuelSize = fuelSize;
    }

    public String getPlaneName() {
        return planeName;
    }
    public int getFuelSize() {
        return fuelSize;
    }
    public void setPlaneName(String planeName) {
        this.planeName = planeName;
    }
    public void setFuelSize(int fuelSize) {
        this.fuelSize = fuelSize;
    }

    public void refuel(int fuel) {
        this.fuelSize += fuel;
    }
    public abstract void flight(int distance);
}

```

### Cargoplane.java

```java
package javaexam2.plane;

public class Cargoplane extends Plane {
    public Cargoplane() {}
    public Cargoplane(String planeName, int fuelSize) {
        super(planeName, fuelSize);
    }

    @Override
    public void flight(int distance) {
        //10 운항 시 30 연료 감소
        int curFuel = getFuelSize();
        if (curFuel < distance*5) {
            System.out.println("연료가 부족합니다.");
        } else {
            setFuelSize(curFuel-distance*5);
        }
    }
}

```


### Airplane.java

```java
package javaexam2.plane;

public class Airplane extends Plane {

    public Airplane() {}
    public Airplane(String planeName, int fuelSize) {
        super(planeName, fuelSize);
    }

    @Override
    public void flight(int distance) {
        //10 운항 시 30 연료 감소
        int curFuel = getFuelSize();
        if (curFuel < distance*3) {
            System.out.println("연료가 부족합니다.");
        } else {
            setFuelSize(curFuel-distance*3);
        }
    }
}

```


### PlaneTest.java

```java

package javaexam2.plane;

public class PlaneTest {
    public static void main(String[] args) {
        // Airplane과 Cargoplane 객체 생성
        Plane air = new Airplane("L747", 1000);
        Plane cargo = new Cargoplane("C40", 1000);
        // 생성된 객체의 정보 출력
        System.out.println("Plane\tfuelSize");
        System.out.println("-----------------------------");
        System.out.printf("%-9s%-9s\n", air.getPlaneName(), air.getFuelSize());
        System.out.printf("%-9s%-9s\n\n", cargo.getPlaneName(), cargo.getFuelSize());

        // Airplane과 Cargoplane 객체에 100씩 운항
        // 100 운항 후 객체 정보 출력
        System.out.println("100 운항");
        air.flight(100);
        cargo.flight(100);

        System.out.println("Plane\tfuelSize");
        System.out.println("-----------------------------");
        System.out.printf("%-9s%-9s\n", air.getPlaneName(), air.getFuelSize());
        System.out.printf("%-9s%-9s\n\n", cargo.getPlaneName(), cargo.getFuelSize());
        //Airplane과 Cargoplane 객체에 200씩 주유
        // 200 주유 후 객체 정보 출력
        System.out.println("200 주유");
        air.refuel(200);
        cargo.refuel(200);

        System.out.println("Plane\tfuelSize");
        System.out.println("-----------------------------");
        System.out.printf("%-9s%-9s\n", air.getPlaneName(), air.getFuelSize());
        System.out.printf("%-9s%-9s\n", cargo.getPlaneName(), cargo.getFuelSize());
 }
}
```
