### 상속

- 서브 클래스의 인스턴스를 만들 때 상위 클래스의 생성자가 같이 호출

![image (1)](https://github.com/user-attachments/assets/27bdfc94-198e-4b34-a439-42bcf876f5be)


- `super` : this와 같은 의미이지만 실제로 가리키는 객체는 상위 클래스의 객체
- `super()` : constructor 내에서 상위 class의 constructor를 호출하기 위해 사용
- annotation ; 명시를 해주는 일종의 메타데이터
    - `@Override`오버라이딩 된 메소드라는 것을 알려주는 것
    - 해주는 것이 좋음
- is-a 관계
    - 하위 클래스는 상위 클래스이다. 라는 명제를 말함
    - 타입 캐스팅 의미로 사용
    - 역은 성립하지 않음

```java
Human h = new Human();
Human f = new Female();
//사람의 속성만을 가짐 
//오버라이딩 된 메소드가 있으면 해당 내용으로 출력이 된다
```

### 오버라이딩

- 부모로부터 물려받은 메소드를 하위 클래스에서 재정의 하는 것
- 메소드 간 이름이 같아야 함
- 리턴 타입, 파라미터 수와 파라미터 타입이 모두 같아야 함
- 접근 제어자가 부모의 것과 같거나 더 넓어야 함
- 다이나믹 바인딩
    - 오버라이딩 된 메소드가 있으면 해당 내용으로 출력됨
- A a = new B();
    - B의 필드를 접근하고 싶다면 타입 캐스팅을 해야 함
    
    ```java
    (B)a.field = @;
    // 위와 같이 할 경우 연산자 우선순위 때문에 에러가 발생
    ((B)a).field = @;
    // 위와 같이 명시적으로 우선순위를 지정해줘야 가능
    ```
    

### this

- 같은 클래스 내 다른 레퍼런스를 호출하기 위해 사용
- this
    - 필드, 메소드 호출에 사용
- this()
    - 생성자 호출에 사용

### 생성자, 상속, 오버라이딩에 관련된 간단 예제

```java

package inherit;

public class SuperClass {
    static int staticCall(String msg) {
        System.out.println(msg);
        return 0;
    }

    int a = staticCall("1번 문장입니다.");
    static int b = staticCall("2번 문장입니다.");

    public SuperClass() {
        staticCall("3번 문장입니다.");
    }

    public SuperClass(int a) {
        this();
        staticCall("4번 문장입니다.");
    }

    public void myFunc() {
        System.out.println("5번 문장입니다.");
    }
}

//------------------------------------------
package inherit;

public class Exam01_InheritanceOrder extends SuperClass{
    int c  = staticCall("6번 문장입니다.");
    static int d = staticCall("7번 문장입니다.");

    public Exam01_InheritanceOrder() {
        super(100);
        staticCall("8번 문장입니다.");
        super.myFunc();
    }

    @Override
    public void myFunc() {
        System.out.println("9번 문장입니다.");
    }

    public static void main(String[] args) {
        System.out.println("10번 문장입니다.");
        SuperClass s = new Exam01_InheritanceOrder();
        s.myFunc();
    }
}

// 2 - 7 - 10 - 1 - 3 - 4 - 6 - 8 - 5 - 9; 
```

### 추상화

- 내가 표현하고자 하는 현실 세계의 구성 요소에 이름을 붙이는 것
- 추상클래스
    - 인스턴스 생성 불가
    - 하위 클래스에서 오버라이딩 한 후 인스턴스 생성
        - 붕어빵 틀 같은 느낌

```java
abstract class Animal { abstract void cry(); }

class Cat extends Animal { void cry() { System.out.println("냐옹냐옹!"); } }
```

### 캡슐화

- 데이터 구조와 데이터를 다루는 방법을 결합시켜 묶은 것을 지칭함
- 클래스로 부터 파생된 객체가 독립적으로 역할을 수행하기 위해 데이터(field)와 기능(method)을 묶어서 관리
- 재활용성을 높이고 에러발생을 최소화

**Information Hiding**

- class는 외부에서 알 필요가 없는 정보를 외부에 노출 시킬 필요가 없음
- 불필요한 정보는 숨기고 최소한의 정보만을 이용하여 class를 쉽게 사용할 수 있어야 함

### **final keyword**

- class 앞에 final이 붙었을 경우 : 해당 class를 **상속**할 수 없음을 의미
- field 앞에 final이 붙었을 경우 : 해당 field의 값을 **변경할 수 없음**을 의미 ( 상수화 )
- method 앞에 final이 붙었을 경우 : 해당 method를 하위 class에서 **overriding**할 수 없음을 의미

### interface

추상 클래스를 붕어빵 틀이라고 하면 인터페이스는 틀을 만드는 부품 도안 정도?

- field 는 모두 public static final field가 됨(상수)
- method는 abstract method가 됨
- 상속의 경우 implements로 가능함
- interface 간에는 extends로 상속 할 수 있음

### 객체지향 실습 문제

- 1번

```java
package javaexam1.tv;

public class Tv {
    private String name;
    private int price;
    private String description;

    public Tv() {}

    public Tv(String name, int price, String description) {
        this.name = name;
        this.price = price;
        this.description = description;
    }

    //getter
    public String getName() {
        return name;
    }
    public int getPrice() {
        return price;
    }
    public String getDescription() {
        return description;
    }

    //setter
    public void setName(String name) {
        this.name = name;
    }
    public void setPrice(int price) {
        this.price = price;
    }
    public void setDescription(String description) {
        this.description = description;
    }

    public String toString() {
        return name + " " + price + " " + description;
    }
}

package javaexam1.tv;

public class TvTest {
    public static void main(String[] args) {
        Tv[] tvArray = new Tv[3];

        tvArray[0] = new Tv("INFINIA", 1500000, "LED TV");
        tvArray[1] = new Tv("XCANVAS", 1000000, "LCD TV");
        tvArray[2] = new Tv("CINEMA", 2000000, "3D TV");

        int total = 0;
        for (Tv tv : tvArray) {
            total += tv.getPrice();
            System.out.println(tv);
        }

        System.out.println("가격의 합: "+total);
    }
}

```

- 2번

```java
package javaexaxm2.student;
// public(+), protected(#), default(~), private(-)
public class Human {
    protected String name;
    protected int age;
    protected int height;
    protected int weight;

    public Human() {}
    public Human(String name, int age, int height, int weight) {
        this.name = name;
        this.age = age;
        this.height = height;
        this.weight = weight;
    }

    public String printInformation() {
        return name+"\t"+age+"\t"+height+"\t"+weight;
    }
}

package javaexaxm2.student;

public class Student extends Human{
    private String number;
    private String major;

    public Student() {}
    public Student(String name, int age, int height, int weight, String number, String major) {
        super(name, age, height, weight);
        this.number = number;
        this.major = major;
    }

    //getter
    public String getNumber() {
        return number;
    }
    public String getMajor() {
        return major;
    }

    //setter
    public void setNumber(String number) {
        this.number = number;
    }
    public void setMajor(String major) {
        this.major = major;
    }

    @Override
    public String printInformation() {
        return name+"\t"+age+"\t"+height+"\t"+weight+"\t"+number+"\t"+major;
    }
}

package javaexaxm2.student;

public class StudentTest {
    public static void main(String[] args) {
    Student[] arrays = new Student[3];

        arrays[0] = new Student("홍길동", 15, 171, 81, "201101", "영문");
        arrays[1] = new Student("한사람", 13, 183, 72, "201102", "건축");
        arrays[2] = new Student("임걱정", 16, 175, 65, "201103", "무영");

        for (Student arr: arrays) {
            System.out.println(arr.printInformation());
        }
     }
}

```

### Object Class

- 모든 클래스의 최상위 클래스
- equals()
    - 본인의 기준을 세워 비교할 수 있음
    - 내용이 같다면 True를 반환하게 하고 싶을 때
    - String의 경우는 예외가 있음
- instance of
    - 객체의 인스턴스인지 확인할 때
