### 검색 입력 시 초기 리스트 유지하는 방법

문제:
input에 공백을 한 번 줬다가 빼면 검색이 정상 작동하지만, 처음에는 리스트가 보이지 않음.
이유는 searchedStockList의 초기값이 undefined이거나 빈 배열이기 때문.

해결 방법:
useState에서 초기값을 stockList로 설정.
useEffect를 사용하여 stockList 변경 시 searchedStockList도 업데이트.

```js
const [searchedStockList, setSearchedStockList] = useState(stockList);

useEffect(() => {
  setSearchedStockList(stockList);
}, [stockList]);

const handleSearch = (e: React.ChangeEvent<HTMLInputElement>) => {
  const value = e.target.value;

  if (!value) {
    setSearchedStockList(stockList);
    return;
  }

  const filteredStockList = stockList.filter((stock: StockType) =>
    stock.stockName.includes(value)
  );

  setSearchedStockList(filteredStockList);
};

```

### useMutation에서 refetchOnMount 대체 방법

문제:
useQuery에서는 refetchOnMount로 자동 갱신 가능하지만, useMutation에는 없음.
mutation 실행 후 최신 데이터를 유지하고 싶음.

해결 방법:
onSuccess에서 queryClient.invalidateQueries()를 호출하여 특정 쿼리 갱신.
queryClient.refetchQueries()를 사용하여 강제로 새로고침.
cacheTime: 0을 설정하여 캐시를 바로 폐기하는 것도 가능.

```js
const queryClient = useQueryClient();

const mutation = useMutation(updateStock, {
  onSuccess: () => {
    queryClient.invalidateQueries(["stockList"]); // 특정 쿼리 데이터 새로 가져오기
  },
});

// 혹은 수동으로 호출
mutation.mutate(data, {
  onSuccess: () => {
    queryClient.refetchQueries(["stockList"]); // useQuery 데이터 다시 불러오기
  },
});

```

### 페이지 변경사항

![til1](https://github.com/user-attachments/assets/93c5a8e1-85ee-496e-bfe9-e7be60a3b9ae)

![til2](https://github.com/user-attachments/assets/0969eaee-9c43-47e1-beff-3f5ec4597cff)

![til3](https://github.com/user-attachments/assets/2274579a-e8df-48b0-9e89-f611a29b85cb)

![til4](https://github.com/user-attachments/assets/eeea87d9-21ad-46a1-9528-cc7f5252e175)
