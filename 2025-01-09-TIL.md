### 인공지능

- 개념
    - 인간의 지각/학습/추론 능력을 인공적으로 구현하는 기술
        - 로봇 저널리즘(eq. 인공지능의 자연어 처리 기술을 응용한 기사)
        - 로봇 어드바이저(eq. 인공지능 자산 관리 서비스)
- 분류
    - 인공지능
        - 컴퓨터가 사람의 행동을 흉내내는 모든 기술들
    - 머신러닝
        - 회귀, 분류를 이용하여 기계가 학습하게 하는 인공지능 기술
    - 딥러닝
        - 다중층 신경망을 이용하는 머신러닝 기술
- 발전
    - 튜링 테스트
        - 인공지능의 지능을 평가하는 방법론으로 기계가 인간처럼 지능적 행동이 가능한지 판단
        - AI의 발전을 가늠하는 기준
        - 인간의 복잡한 사고와 감정을 완전히 평가하는 데에는 한계 존재
- 방법론
    - 규칙 기반 알고리즘(Rule Based Algorithm)
        - 정해진 규칙에 따라 판단을 내리며, 규칙에서 벗어나는 예외가 많은 경우에는 인공지능이 제대로 작동하지 않음
    - 전문가 시스템
        - 결과에 도달하기 위해 추론 능력을 이용하는 인공지능 시스템
    - 변수 스케일링
        - 정규화를 하면 변수가 결과에 미치는 영향을 비례적으로 알 수 있음
        - 정규화와 표준화가 있음

### 머신러닝

- 개념
    - 사람 개입없이 자동으로 기계가 학습하게 함
    - 과거의 데이터를 모델로 만들고, 이를 활용하여 새로운 데이터 예측
- 종류
    - 지도 학습(Supervised Learning)
        - 분류(Classfication)
            - 의사결정트리(Decision Tree)
            - * 랜덤포레스트(Random Forest)
            - 선형판별분석(Linear Discriminant Analysis)
            - K최근접이웃(K-Nearest Neighbor)
            - 지원벡터머신(Support Vector Machine)
            - 신경망(Neural Network)
            - * 부스팅(GBM/XGBoost/Light GBM)
        - 회귀(Regression)
            - 변수들 사이 관계를 추정하는 통계 기법
            - 데이터를 표현하는 식을 찾아냄
        - 군집(Clustering)
            - 비슷한 개체 들을 묶어주는 기법
    - 비지도 학습(Unsupervised Learning)
    - 강화 학습(Reinforcement Learning)
        - 보상 시스템을 이용하여 액션을 배우며 발전하는 방식
- 절차
    1. 수집(get)
    2. 전처리(manipulate)
    3. 학습(train)
    4. 테스트(test)
    5. 향상(improve)
- 학습되지 않은 데이터의 분류 성능이 떨어지며, 입력과 분류에 인간의 생각이 개입됨

### 딥 러닝

- 개념
    - 인간의 신경망을 모형화한 알고리즘에 방대한 데이터를 학습시켜 결과를 얻는 머신러닝 기술
    - 아주 많은 양의 데이터를 학습하기 위해 여러 층의 인공 신경망을 사용
    - 퍼셉트론(Perceptron)
        - 가중치를 가진 여러 개의 입력으로부터 하나의 출력을 만들어내는 기계 == 인간의 신경
- 신경망
    - 합성곱 신경망(CNN, Convolutional Neural Network)
        - 시각적 이미지를 분석하는데 사용되는 깊고 피드포워드적인 인공신경망
        - 합성곱 층
            - 이미지 안에 있는 특성을 찾아내고자 컨볼루션 계산을 하는 각 층
        - 사전학습 모델
            - 대규모 이미지 분류 작업에 대해 미리 훈련되고 저장된 네트워크로, 그대로 사용하거나 전이 학습을 사용하여 이 모델을 지정된 작업에 맞게 바꿀 수 있음
    - 순환 인공 신경망(RNN, Recurrent Neural Network)
        - 신경망의 종류로, 이전 단계의 결과물이 현재 단계의 입력값이 됨
        - 연속된 데이터를 처리하기 가장 좋은 모델스

### 생성형 인공지능

- 새로운 데이터, 이미지, 음성 또는 텍스트 등을 생성하는 기술
- 생성형 AI는 인공지능의 한 분야로, 신경망 기반의 다양한 아키텍처를 사용하여 작동ㅋ

### 초거대 언어모델(LLM, Large Language Model)

- 방대한 양의 데이터를 기반으로 사전에 학습된 딥 러닝 모델
- 입력을 순차전으로 처리하는 RNN과 달리 전체 시퀀스를 병렬로 처리함
- 인코더, 디코더가 일련의 텍스트에서 의미를 추출하고 텍스트 내 단어와 구문 간의 관계를 이해하며 자체 학습을 수행함
- 인코더
    - 입력 시퀀스를 하나의 벡터로 압축
- 디코더
    - 벡터를 통해 출력 시퀀스를 만듦
- 트랜스포머
    - 기존 인코더-디코더 구조를 따르면서도 어텐션만으로 구현한 모델
    - 어텐션
        - 입력 시퀀스 각 요소의 중요도를 측정하고자, 소프트맥스 함수를 사용하여 어텐션 가중치를 계산하고 이 가중치 간 가중합을 통해 출력이 생성되며 이러한 계산은 모델이 특정 요소에 집중하도록 유도됨
- 한계
    - LLM은 일반적인 상황에서 프롬프트에 응답하는 데 탁월하나, 훈련한 적이 없는 특정 영역에서는 성능이 떨어짐
    - LLM은 사용자의 질문에 추정치를 제공하여 컴퓨터 비용에 대한 답변을 제공할 수 있으나, 회사에서 판매하는 특정 컴퓨터 모델의 가격을 표시할 수는 없음(랭체인을 통해 일부 해결 가능)
