### 컴퓨터 통신

- 데이터 통신
- 컴퓨터 네트워크
    - 근거리 네트워크(LAN)
    - 장거리 네트워크(WAN)
    
    ---
    
    - 중거리 네트워크(MAN)
    - 개인 네트워크(PAN)
    - 초근거리 네트워크(NFC)
- 인터넷
- 가상 사설망(VPN, Virtual Private Network)
    - 인터넷과 같은 공용 네트워크를 통해 사설 네트워크에 안전하게 연결할 수 있도록 하는 기술
    - 데이터가 암호화되어 전송되기 때문에 정보 유출이나 해킹으로부터 보호
- 방향에 따른 분류
    - 단방향
        - 모스 부호, 방송
    - 양방향
        - 대부분의 통신 시스템
    - 반양방향
        - 무전기
- 통신 대상에 따른 분류
    - 유니캐스트
        - 일대일 연결(전화)
    - 브로드캐스트
        - 일대다 연결(방송)
    - 멀티캐스트
        - 일대다 연결(집단 화상회의)

### LAN 토폴로지

- 컴퓨터를 연결하는 LAN의 구조, 위상 배치에 따라 아래 3가지로 나눔
- 스타형
    - 중앙에 위치한 한 장치에서 모든 장치를 연결
    - 항상 중앙 장치를 통해 데이터 전송이 이루어짐
- 링형
    - 장치들을 원형으로 연결
    - 데이터 전송을 위해 토큰 사용
- 버스형
    - 하나의 통신 회선에 장치를 연결하는 구조
    - 데이터를 목적지 주소와 함께 버스에 실어 보냄

### 네트워크 통신 계층

- 물리 계층(bit)
    - 통신 기기의 연결을 담당
    - 실제 전송 매체에서 데이터를 전기적 신호나 광신호로 전송하는 프로토콜
- 데이터 링크 계층(frame)
    - 인접 기기 간 오류 없이 데이터를 전송하도록 제어
    - 전송 방식
        - 이더넷
        - Wi-Fi
    - MAC 주소
        - 데이터 링크 계층에서 사용되는 물리적 주소
        - 네트워크 인터페이스 카드(NIC)나 네트워크 장치에 고유하게 할당
    - 오류 제어
        - 탐색 코드(발견만 함)
            - 패리티비트
                - 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 하는 방식
            - 체크섬
                - 2진수로 쪼개어 음수는 보수계산을 하고 최종적으로 자릿수를 맞춰준다. 0000)
            - 순환 중복 검사(CRC)
                - 주어진 데이터를 이진수로 표현한다.
                - 다항식을 최고차항 기준으로 이진수로 표현한다.
                - 다항식에 데이터 비트 수를 맞춘다.(뒤에 0을 붙임)
                - XOR 연산으로 나머지를 구한다.
        - 보정 코드
            - 허밍
- 네트워크 계층(packet)
    - LAN을 하나로 묶어 인터넷을 만드는 핵심 계층
    - 라우팅
        - 어떤 길로 패킷을 보낼지 결정하는 것
- 전송 계층(segment)
    - 네트워크 계층에서 목적지까지 오면서 생기는 오류를 보정
    - TCP는 일련번호를 매겨 패킷을 전송, 패킷 누락, 순서 뒤바뀜을 방지함
- 응용 계층(data)
    - 인터넷을 사용하는 각종 응용 프로그램이 위치하는 계층
    - OSI 7계층에서는 해당 계층이 세션/표현/응용 계층 3가지로 나뉨

### TCP/IP 모델

- 현재 가장 많이 사용되는 네트워크의 모델
- 송신측과 수신측의 연결이 이루어진 것을 확인한 뒤, 데이터를 전송(shake-hands)
    - SYN
    - ACK
- 인터넷의 기초 프로토콜로, 네트워크로 연결된 컴퓨터 간 메시지를 안정적으로 전달하는데 사용됨
- 헤더
    - 패킷
        - 네트워크 계층에서 다루는 데이터 단위
    - 데이터 전송을 위해 각 계층에서 추가되는 자료구조

### IP

- 구분
    - 네트워크 주소
        - 해당 장치가 속해있는 네트워크를 식별
    - 호스트 주소
        - 네트워크 내에서 해당 장치를 식별
    - 한 네트워크 내의 모든 장치들은 같은 네트워크 주소를 갖지만, 호스트 주소는 모두 다르다
- 클래스
    - A클래스
        - 0~ (0~127)
    - B클래스
        - 10~ (128~191)
    - C클래스
        - 110~ (192~223)
- 서브넷 마스크
    - IP 네트워크에서 서브넷을 나누기 위해 사용
- IPv4
    - 32비트
- IPv6
    - 128비트
- IP 헤더
    - 20 바이트
    - 서비스 타입, TTL, 프로토콜, 체크섬, IP주소 등이 담김
- 주소 유형
    - 사설
        - 내부 네트워크에서 사용됨. 외부 인터넷과 직접 통신이 불가능함. NAT를 통해 공인 IP로 변환되어 연결
    - 공인
        - 인터넷에서 고유한 주소로 전 세계적으로 유일하며, ISP가 직접 할당
            - eq. 구글은 8.8.8.8

### 네트워크 장치

- 게이트웨이
    - 네트워크와 네트워크가 연결되도록 도와주는 다리같은 역할
    - 같은 네트워크 내의 데이터를 관리하는 라우터, 다른 네트워크와 연결하는 게이트웨이
- 허브
    - 컴퓨터 네트워크에서 여러 장치를 연결하는데 사용되는 재전송 장치
    - 소규모 네트워크에서 사용
    - 연결된 장치들 간 데이터 주고받을 수 있음
- 스위치
    - 네트워크 장치들 간 데이터프레임을 전송하는 장비
    - 트래픽을 효율적으로 관리함
    - 대규모 네트워크, 고급 네트워크 환경에 적합

### 라우팅 프로토콜

- RIP(Routing Information Protocol)
    - 거리 벡터 기반
    - 목적지까지의 거리를 홉 수로 측정(15로 제한)
- OSPF(Open Shortest Path First)
    - 링크 상태 라우팅 프로토콜로 계층적 라우팅 지원
    - 네트워크의 각 라우터가 자신의 상태 정보를 전체 네트워크에 전파하여 최적 경로 계산
- BGP(Border Gateway Protocol)
    - 경계 라우팅 프로토콜
    - 서로 다른 AS(Autonomous System)간에 라우팅 정보 교환
    - 경로 벡터를 이용, 네트워크 간 경로 결정

### 도메인

- IP주소에 이름을 부여한 것
- DNS
    - 도메인 네임과 IP 주소를 테이블 형태로 모아놓은 서버
    - 로컬에서 루트까지 점점 범위를 넓혀가며 계층 구조가 이루어져 있음

### 포트

- 응용 프로그램 구분을 위해 사용하는 주소
- 수신 측에서 동작하는 여러 애플리케이션 중 데이터를 수신할 애플리케이션을 식별하는 번호
- 번호
    - HTTP(80)
    - HTTPS(443)
    - FTP(21)
    - SSH(22)
    - DNS(53)
    - …

### WWW

- 인터넷에서 정보를 쉽게 찾을 수 있도록 제공하는 ㄴ서비스
- 하이퍼텍스트, 하이퍼미디어 방식 이용
- SSL
    - 클라이언트와 서버 사이의 보안 연결
    - 인증서를 통해 보안을 유지
- 암호화
    - 모든 사람을 위해서 공개키를, 받는 사람을 위해 개인키를 이용하여 암호화 함
- TLS
    - SSL의 후속 프로토콜로 SSL의 보안 취약점을 개선한 방식
- 방화벽
    - 네트워크 보안 강화를 위해 트래픽을 제어하는 장치 시스템
