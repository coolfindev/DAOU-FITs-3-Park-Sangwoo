## 추가문제 1

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>


int main() {
    //1. 행과 열의 크기를 입력받아 정적 2차원 배열을 생성하고 배열 내부에 각 요소의 인덱스 값
    //    을 저장하는 프로그램을 작성하세요.제약조건
    //    행과 열은 크기 최대 5 X 5로 제한한다.행과 열의 원소값은 0행에 0 1 2 로 1행은 10 11 12 2행은 20 21 23으로 자동 넣게 한다.특정 행을 삭제하려면 삭제할 행 인덱스를 입력받아 삭제한다.결과화면
    //    행과 열의 수를 입력하세요 ? 3 4
    //    배열의 초기값는 자동으로 입력됩니다. 0 1 2 3
    //    10 11 12 13
    //    20 21 22 23
    //    삭제하려는 행의 인덱스 번호를 입력하세요 ? 1
    //    수정된 배열 출력
    //    0 1 2 3
    //    20 21 22 23

    int r, c;
	int arr[5][5];
	printf("행과 열의 수를 입력하세요 ? ");
	scanf("%d %d", &r, &c);
	for (int i = 0; i < r; i++) {
		for (int j = 0; j < c; j++) {
			arr[i][j] = i * 10 + j;
		}
	}

	printf("배열의 초기값은 자동으로 입력됩니다.\n");
	for (int i = 0; i < r; i++) {
		for (int j = 0; j < c; j++) {
			printf("%2d ", arr[i][j]);
		}
		printf("\n");
	}

	int del;
	printf("삭제하려는 행의 인덱스 번호를 입력하세요 ? ");
	scanf("%d", &del);

	//i번 행 인덱스 삭제는 i+1번을 i로 복사, i+2번을 i+1로 복사, ... , r-1번을 r-2로 복사

	for (int i = del; i < r - 1; i++) {
		for (int j = 0; j < c; j++) {
			arr[i][j] = arr[i + 1][j];
		}
	}

	r--;
	
	printf("수정된 배열 출력\n");
	for (int i = 0; i < r; i++) {
		for (int j = 0; j < c; j++) {
			printf("%2d ", arr[i][j]);
		}
		printf("\n");
	}
    return 0;
}
```

## 추가문제 2

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

#define ROWS 3
#define COLS 3

void sum(int** array) {
	int sum = 0;
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			sum += array[i][j];
		}
	}
	printf("배열의 합은 %d\n", sum);
}

void maxNum(int** array) {
	int max = array[0][0];
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			if (max < array[i][j]) {
				max = array[i][j];
			}
		}
	}
	printf("배열의 최대값은 %d\n", max);
}

void minNum(int** array) {
	int min = array[0][0];
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			if (min > array[i][j]) {
				min = array[i][j];
			}
		}
	}
	printf("배열의 최소값은 %d\n", min);
}

void square(int** array) {
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			array[i][j] *= array[i][j];
		}
	}
	printf("배열의 제곱값은\n");
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			printf("%2d ", array[i][j]);
		}
		printf("\n");
	}
}

int main() {
    //1. 이중 포인터와 함수 포인터를 활용한 연산 프로그램을 구현하세요. *


	int array[3][3] = {
		{0, 1, 2},
		{3, 4, 5},
		{6, 7, 8}
	};

	int* pArray[3] = { array[0], array[1], array[2] };

	void (*operation[4])(int**) = { sum, maxNum, minNum, square };

	int num;
	printf("연산 방법을 선택하기\n(0: 합, 1 : 최대값, 2 : 최소값, 3 : 제곱) : ");
	scanf("%d", &num);

	operation[num](pArray);

    return 0;
}
```
