### 오답

- 함수 포인터
    - 서로 다른 자료형을 매개변수로 갖는 함수가 여러 개 있을 때 하나의 함수 포인터로 여러 개의 함수를 호출하여 사용할 경우 유용하다.
    - 하나의 프로그램에 여러 개의 함수가 존재할 경우 여러 개의 실행파일이 만들어짐
        - 이 때 실행파일은 `.exe` 뿐만 아니라 .dll과 같은 파일도 해당됨(..?)
        - `.dll` `.so`와 같은 파일은 라이브러리 파일

### 문제

- 이중 포인터 문제3
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    
    int main() {
    	int arr[3][3];
    	int rotated[3][3];
    	int* ptr[3] = {arr[0], arr[1], arr[2]};
    
    	printf("3x3 행렬 입력 :\n");
    	for (int i = 0; i < 3; i++) {
    		scanf_s("%d %d %d", ptr[i], ptr[i] + 1, ptr[i] + 2);
    	}
    
    	printf("입력된 행렬 :\n");
    	for (int i = 0; i < 3; i++) {
    		for (int j = 0; j < 3; j++) {
    			printf("%d ", arr[i][j]);
    		}
    		printf("\n");
    	}
    
    	printf("90도 회전된 행렬 :\n");
    	for (int i = 0; i < 3; i++) {
    		for (int j = 0; j < 3; j++ ) {
    			rotated[j][2 - i] = arr[i][j];
    		}
    	}
    
    	for (int i = 0; i < 3; i++) {
    		for (int j = 0; j < 3; j++) {
    			printf("%d ", rotated[i][j]);
    		}
    		printf("\n");
    	}
    	return 0;
    }
    
    ```
    
- 함수 포인터 문제2
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    #include <ctype.h>
    
    void getLength(char* str) {
        printf("결과 : %d\n", (int)strlen(str));
    }
    
    void upperString(char* str) {
        for (int i = 0; str[i]; i++) {
            str[i] = toupper(str[i]);
        }
        printf("결과 : %s\n", str);
    }
    
    void lowerString(char* str) {
        for (int i = 0; str[i]; i++) {
            str[i] = tolower(str[i]);
        }
        printf("결과 : %s\n", str);
    }
    
    int main() {
        //2. 문자열에 대해 3가지 동작을 수행하는 프로그램을 함수포인터로 작성해 보세요. * 요구사항
        //	문자열의 길이를 구하는 함수
        //	문자열을 대문자로 변환하는 함수
        //	문자열을 소문자로 변환하는 함수
        //	각 기능은 함수 포인터 배열을 통해 호출될 수 있도록 해야 합니다.사용자가 선택한 기능
        //	에 맞는 함수를 호출하여 해당 작업을 수행하도록 합니다. * 결과화면
        //	1. 문자열 길이 구하기
        //	2. 대문자로 변환하기
        //	3. 소문자로 변환하기
        //	선택 : 2
        //	변환할 문자열 입력 : hello world
        //	결과 : HELLO WORLD
    
        char str[100];
        void (*func_ptr[3])(char*) = { getLength, upperString, lowerString };
    
        int num;
        printf("1. 문자열 길이 구하기\n");
        printf("2. 대문자로 변환하기\n");
        printf("3. 소문자로 변환하기\n");
    
        printf("선택 : ");
        scanf("%d", &num);
        getchar(); // 개행 문자 제거
    
        if (num >= 1 && num <= 3) {
            printf("문자열 입력 : ");
            fgets(str, sizeof(str), stdin); 
            str[strcspn(str, "\n")] = '\0'; // 개행 문자 제거
    
            func_ptr[num - 1](str);
        }
        else {
            printf("잘못된 입력입니다.\n");
        }
    
        return 0;
    }
    
    ```
    
- 함수 포인터 문제3
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    // 오름차순 비교 함수
    int compare_ascending(const void* a, const void* b) {
        return (*(int*)a - *(int*)b);
    }
    
    // 내림차순 비교 함수
    int compare_descending(const void* a, const void* b) {
        return (*(int*)b - *(int*)a);
    }
    
    int main() {
        int arr[5];
        int choice;
        void (*compare_func)(const void*, const void*);
    
        printf("배열을 입력하세요(5개의 정수) : ");
        scanf("%d %d %d %d %d", &arr[0], &arr[1], &arr[2], &arr[3], &arr[4]);
    
        printf("1. 오름차순\n");
        printf("2. 내림차순\n");
        printf("선택 : ");
        scanf("%d", &choice);
    
        if (choice == 1) {
            compare_func = compare_ascending;
        }
        else if (choice == 2) {
            compare_func = compare_descending;
        }
        else {
            printf("잘못된 입력입니다.\n");
            return 1;
        }
    
        qsort(arr, 5, sizeof(int), compare_func);
    
        printf("정렬된 배열 : ");
        for (int i = 0; i < 5; i++) {
            printf("%d ", arr[i]);
        }
        printf("\n");
    
        return 0;
    }
    
    ```
    

- 이중 포인터 추가 문제1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    int main() {
        //1. 행과 열의 크기를 입력받아 정적 2차원 배열을 생성하고 배열 내부에 각 요소의 인덱스 값
        //    을 저장하는 프로그램을 작성하세요.제약조건
        //    행과 열은 크기 최대 5 X 5로 제한한다.행과 열의 원소값은 0행에 0 1 2 로 1행은 10 11 12 2행은 20 21 23으로 자동 넣게 한다.특정 행을 삭제하려면 삭제할 행 인덱스를 입력받아 삭제한다.결과화면
        //    행과 열의 수를 입력하세요 ? 3 4
        //    배열의 초기값는 자동으로 입력됩니다. 0 1 2 3
        //    10 11 12 13
        //    20 21 22 23
        //    삭제하려는 행의 인덱스 번호를 입력하세요 ? 1
        //    수정된 배열 출력
        //    0 1 2 3
        //    20 21 22 23
    
        int r, c;
    	int arr[5][5];
    	printf("행과 열의 수를 입력하세요 ? ");
    	scanf("%d %d", &r, &c);
    	for (int i = 0; i < r; i++) {
    		for (int j = 0; j < c; j++) {
    			arr[i][j] = i * 10 + j;
    		}
    	}
    
    	printf("배열의 초기값은 자동으로 입력됩니다.\n");
    	for (int i = 0; i < r; i++) {
    		for (int j = 0; j < c; j++) {
    			printf("%2d ", arr[i][j]);
    		}
    		printf("\n");
    	}
    
    	int del;
    	printf("삭제하려는 행의 인덱스 번호를 입력하세요 ? ");
    	scanf("%d", &del);
    
    	//i번 행 인덱스 삭제는 i+1번을 i로 복사, i+2번을 i+1로 복사, ... , r-1번을 r-2로 복사
    
    	for (int i = del; i < r - 1; i++) {
    		for (int j = 0; j < c; j++) {
    			arr[i][j] = arr[i + 1][j];
    		}
    	}
    
    	r--;
    	
    	printf("수정된 배열 출력\n");
    	for (int i = 0; i < r; i++) {
    		for (int j = 0; j < c; j++) {
    			printf("%2d ", arr[i][j]);
    		}
    		printf("\n");
    	}
        return 0;
    }
    
    ```
    
- 이중 포인터 추가 문제2
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    #define ROWS 3
    #define COLS 3
    
    void sum(int** array) {
    	int sum = 0;
    	for (int i = 0; i < ROWS; i++) {
    		for (int j = 0; j < COLS; j++) {
    			sum += array[i][j];
    		}
    	}
    	printf("배열의 합은 %d\n", sum);
    }
    
    void maxNum(int** array) {
    	int max = array[0][0];
    	for (int i = 0; i < ROWS; i++) {
    		for (int j = 0; j < COLS; j++) {
    			if (max < array[i][j]) {
    				max = array[i][j];
    			}
    		}
    	}
    	printf("배열의 최대값은 %d\n", max);
    }
    
    void minNum(int** array) {
    	int min = array[0][0];
    	for (int i = 0; i < ROWS; i++) {
    		for (int j = 0; j < COLS; j++) {
    			if (min > array[i][j]) {
    				min = array[i][j];
    			}
    		}
    	}
    	printf("배열의 최소값은 %d\n", min);
    }
    
    void square(int** array) {
    	for (int i = 0; i < ROWS; i++) {
    		for (int j = 0; j < COLS; j++) {
    			array[i][j] *= array[i][j];
    		}
    	}
    	printf("배열의 제곱값은\n");
    	for (int i = 0; i < ROWS; i++) {
    		for (int j = 0; j < COLS; j++) {
    			printf("%2d ", array[i][j]);
    		}
    		printf("\n");
    	}
    }
    
    int main() {
        //1. 이중 포인터와 함수 포인터를 활용한 연산 프로그램을 구현하세요. *
    
    	int array[3][3] = {
    		{0, 1, 2},
    		{3, 4, 5},
    		{6, 7, 8}
    	};
    
    	int* pArray[3] = { array[0], array[1], array[2] };
    
    	void (*operation[4])(int**) = { sum, maxNum, minNum, square };
    
    	int num;
    	printf("연산 방법을 선택하기\n(0: 합, 1 : 최대값, 2 : 최소값, 3 : 제곱) : ");
    	scanf("%d", &num);
    
    	operation[num](pArray);
    
        return 0;
    }
    
    ```
    
- pArray를 선언하지 않고 바로 array를 함수에 전달하면 문제가 발생합니다.
- 이는 함수들이 int** 타입의 인자를 기대하기 때문입니다. array는 int[3][3] 타입이므로, 이를 int**로 직접 전달할 수 없습니다.**
- **pArray를 사용하여 array의 각 행을 가리키는 포인터 배열을 만들어야 합니다. 이렇게 하면 함수들이 기대하는 int** 타입으로 전달할 수 있습니다.
