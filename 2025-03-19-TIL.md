### realloc 문제

- 응용문제1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <malloc.h>
    
    typedef struct {
    	char name[20];
    	int quantity;
    	double price;
    } Stock;
    
    int main() {
    /*	1. 증권사에서 주식 주문을 처리하는 시스템을 구현해야 합니다.이 시스템에서는 고객이 주
    		식 주문을 할 수 있으며, 주식 주문 내역은 동적으로 관리해야 합니다. 
    		1) 요구사항
    		- 고객은 여러 개의 주식 주문을 할 수 있습니다. 
    		- 각 주문에는 주식 이름, 주문 수량, 주문 가격을 포함합니다. 
    		- 고객은 처음에 주문할 주식의 개수를 입력한 후, 주문을 추가할 때마다 주문 수를 늘릴 수
    		있습니다. 
    		- realloc() 함수를 사용하여 주문을 추가할 때마다 기존 배열의 크기를 늘려야 합니다.
    		- 사용자가 입력한 주식 주문 정보를 출력해야 합니다. 
    		- 고객의 주문 내역이 모두 입력된 후, 각 주문에 대해 주식 이름, 수량, 가격을 출력합니다.
    		
    		2) 처리순서
    		- 사용자가 주문할 초기 주문 개수를 입력받습니다.
    		- 주식 주문 내역을 동적 배열로 관리하고, 주문을 추가할 때마다 realloc()을 사용하여 주문
    		수를 늘려야 합니다. 
    		- 각 주문에 대해 주식 이름, 주문 수량, 주문 가격을 입력받고, 주문 정보를 출력합니다. 
    		- 최종적으로 모든 주문 내역을 출력합니다
    	*/
    	int n, m = 0;
    	printf("초기 주문 개수를 입력하세요: ");
    	scanf("%d", &n);
    
    	Stock* p = (Stock*)malloc(n * sizeof(Stock));
    	if (p == NULL) {
    		printf("메모리 할당에 실패했습니다.\n");
    		return 1;
    	}
    
    	for (int i = 0; i < n; i++) {
    		printf("주식 주문 %d:\n", i + 1);
    		printf("주식 이름: ");
    		scanf("%s", p[i].name);
    		printf("주문 수량: ");
    		scanf("%d", &p[i].quantity);
    		printf("주식 가격: ");
    		scanf("%lf", &p[i].price);
    	}
    
    	char ch;
    	printf("추가로 주문을 입력하시겠습니까? (Y/N): ");
    	scanf(" %c", &ch);
    
    	if (ch == 'Y' || ch == 'y') {
    		printf("추가 주문 개수를 입력하세요: ");
    		scanf("%d", &m);
    
    		p = (Stock*)realloc(p, (n + m) * sizeof(Stock));
    		if (p == NULL) {
    			printf("메모리 할당에 실패했습니다.\n");
    			return 1;
    		}
    		for (int i = n; i < n + m; i++) {
    			printf("주식 주문 %d:\n", i + 1);
    			printf("주식 이름: ");
    			scanf("%s", p[i].name);
    			printf("주문 수량: ");
    			scanf("%d", &p[i].quantity);
    			printf("주식 가격: ");
    			scanf("%lf", &p[i].price);
    		}
    	}
    
    	printf("주식 주문 내역\n");
    	for (int i = 0; i < n+m; i++) {
    		printf("주식 이름: %s, 수량: %d, 가격: %.2lf\n", p[i].name, p[i].quantity, p[i].price);
    	}
    
    	free(p);
    
    	return 0; 
    }
    
    ```
    
- 응용문제2
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <malloc.h>
    
    typedef struct {
    	char name[20]; //주식 이름
    	int quantity; //수량
    	double price; //구매 가격
    	double current_price; //현재 가격
    } Stock;
    
    int main() {
     /*2. 증권사 고객이 여러 주식을 보유하는 포트폴리오 관리 시스템을
    구현해야 합니다.이 시스템에서는 고객이 여러 주식을 동적으로
    추가하거나 수정할 수 있습니다. */
    	int n;
    	printf("포트폴리오에 포함될 주식 종목 개수를 입력하세요 : ");
    	scanf("%d", &n);
    	Stock* portfolio = (Stock*)malloc(n * sizeof(Stock));
    	
    	if (portfolio == NULL) {
    		printf("메모리 할당에 실패했습니다.\n");
    		return -1;
    	}
    	for (int i = 0; i < n; i++) {
    		printf("주식 종목 %d\n", i + 1);
    		printf("주식 이름 : ");
    		scanf("%s", portfolio[i].name);
    		printf("구매 가격 : ");
    		scanf("%lf", &portfolio[i].price);
    		printf("현재 가격 : ");
    		scanf("%lf", &portfolio[i].current_price);
    		printf("보유 수량 : ");
    		scanf("%d", &portfolio[i].quantity);
    	}
    
    	char ch;
    	printf("포트폴리오를 수정하시겠습니까? (Y/N) : ");
    	scanf(" %c", &ch);
    
    	int m = 0;
    	if (ch == 'Y' || ch=='y') {
    		printf("추가할 주식 종목 개수를 입력하세요 : ");
    		scanf("%d", &m);
    		portfolio = (Stock*)realloc(portfolio, (n + m) * sizeof(Stock));
    		for (int i = n; i < n + m; i++) {
    			printf("주식 종목 %d\n", i + 1);
    			printf("주식 이름 : ");
    			scanf("%s", portfolio[i].name);
    			printf("구매 가격 : ");
    			scanf("%lf", &portfolio[i].price);
    			printf("현재 가격 : ");
    			scanf("%lf", &portfolio[i].current_price);
    			printf("보유 수량 : ");
    			scanf("%d", &portfolio[i].quantity);
    		}
    	}
    
    	printf("고객 포트폴리오\n");
    	for (int i = 0; i < n + m; i++) {
    		printf("주식 이름 : %s, 구매 가격 : %.2lf, 현재 가격 : %.2lf, 수량 : %d, 총 구매 금액 : %.2lf, 현재 가치 : %.2lf\n", portfolio[i].name, portfolio[i].price, portfolio[i].current_price, portfolio[i].quantity,portfolio[i].price * portfolio[i].quantity, portfolio[i].current_price * portfolio[i].quantity);
    	}
    
    	free(portfolio);
    	return 0; 
    }
    
    ```
    
- 복합문제1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #define MAX_NAME_LEN 100
    // 거래 유형 정의
    typedef enum {
        CASH,
        CARD,
        CRYPTO
    } TransactionType;
    
    // 거래 데이터 (공용체)
    typedef union {
        double cash_amount;         // 현금 거래 금액
        struct {                    // 카드 거래 정보
            char card_number[20];
            int installment_months;
            double amount;           // 카드 거래 금액
        } card_info;
        struct {                    // 가상화폐 거래 정보
            char wallet_address[100];
            double crypto_amount;   // 가상화폐 거래 금액
        } crypto_info;
    } TransactionData;
    
    // 거래 구조체
    typedef struct {
        int id;                     // 거래 ID
        char customer_name[MAX_NAME_LEN];
        TransactionType type;       // 거래 유형
        TransactionData data;       // 거래 데이터
    } Transaction;
    // 거래 배열
    Transaction* transactions = NULL;
    int transaction_count = 0;
    // 거래 ID 자동 증가 함수
    int get_next_id() {
        return transaction_count + 1;
    }
    // 거래 추가
    void add_transaction() {
        transaction_count++;
        transactions = realloc(transactions, transaction_count * sizeof(Transaction));
        if (transactions == NULL) {
            printf("메모리 할당 실패!\n");
            return;
        }
        Transaction* new_transaction = &transactions[transaction_count - 1];
        // 거래 ID 설정
        new_transaction->id = get_next_id();
        // 고객 이름 입력
        printf("고객 이름: ");
        getchar(); // 버퍼 비우기
        fgets(new_transaction->customer_name, MAX_NAME_LEN, stdin);
        new_transaction->customer_name[strcspn(new_transaction->customer_name,
            "\n")] = '\0';
        // 거래 유형 입력
        printf("거래 유형 (0: 현금, 1: 카드, 2: 가상화폐): ");
        scanf("%d", &new_transaction->type);
        // 거래 정보 입력
        switch (new_transaction->type) {
        case CASH:
            printf("현금 거래 금액: ");
            scanf("%lf", &new_transaction->data.cash_amount);
            break;
        case CARD:
            printf("카드 번호: ");
            getchar(); // 버퍼 비우기
            fgets(new_transaction->data.card_info.card_number, 20, stdin);
    
            new_transaction->data.card_info.card_number[strcspn(new_transaction->data.card_info.card_number, "\n")] = '\0';
            printf("할부 개월 수: ");
            scanf("%d", &new_transaction->data.card_info.installment_months);
            printf("카드 거래 금액: ");
            scanf("%lf", &new_transaction->data.card_info.amount);
            break;
        case CRYPTO:
            printf("가상화폐 지갑 주소: ");
            getchar(); // 버퍼 비우기
            fgets(new_transaction->data.crypto_info.wallet_address, 100, stdin);
    
            new_transaction->data.crypto_info.wallet_address[strcspn(new_transaction->data.crypto_info.wallet_address, "\n")] = '\0';
            printf("가상화폐 거래 금액: ");
            scanf("%lf", &new_transaction->data.crypto_info.crypto_amount);
            break;
        default:
            printf("잘못된 거래 유형입니다.\n");
            break;
        }
    }
    // 거래 출력
    void print_transactions() {
        for (int i = 0; i < transaction_count; i++) {
            Transaction* t = &transactions[i];
            printf("ID: %d, 고객 이름: %s, 거래 유형: ", t->id, t->customer_name);
            switch (t->type) {
            case CASH:
                printf("현금 거래, 금액: %.2f\n", t->data.cash_amount);
                break;
            case CARD:
                printf("카드 거래, 카드 번호: %s, 할부 개월 수: %d, 금액: %.2f\n",
                    t->data.card_info.card_number,
                    t->data.card_info.installment_months, t->data.card_info.amount);
                break;
            case CRYPTO:
                printf("가상화폐 거래, 지갑 주소: %s, 금액: %.2f\n",
                    t->data.crypto_info.wallet_address, t->data.crypto_info.crypto_amount);
                break;
            default:
                printf("잘못된 거래 유형입니다.\n");
                break;
            }
        }
    }
    // 메모리 해제
    void free_transactions() {
        free(transactions);
        transactions = NULL;
    }
    int main() {
        int choice;
        transactions = (Transaction*)malloc(sizeof(transaction_count * sizeof(Transaction)));
        while (1) {
            printf("\n1. 거래 추가\n2. 거래 내역 출력\n3. 종료\n선택: ");
            scanf("%d", &choice);
            if (choice == 1) {
                add_transaction();
            }
            else if (choice == 2) {
                print_transactions();
            }
            else if (choice == 3) {
                free_transactions();
                break;
            }
            else {
                printf("잘못된 선택입니다.\n");
            }
        }
        return 0;
    }
    ```
    

### 최적화의 종류

최적화의 종류는 **메모리 최적화, 속도 최적화, 가독성 최적화**가 있다.

**1. 메모리 최적화**

- **목표**: 최소한의 메모리로 데이터를 저장하고 공간을 절약하는 것
- **방법**: 비트 필드나 비트 연산자 활용

**2. 속도 최적화**

- **목표**: 연산 속도를 높여 프로그램이 더 빠르게 실행되도록 하는 것
- **방법**:
    1. **구조체 정렬을 CPU 워드 크기에 맞추기**
        - CPU는 4바이트 또는 8바이트 정렬을 선호한다.
        - 비트 연산을 사용할 때 데이터를 **4바이트 단위로 묶으면** 더 빠르게 읽고 쓸 수 있다.
        
        ```c
        typedef struct {
            uint32_t flags; // 4바이트 정렬로 성능 최적화
        } AccountStatus;
        
        ```
        
    2. **비트 연산을 이용한 속도 개선**
        - `&`, `|`, `~` 연산자를 직접 이용하여 연산 속도를 높인다.

**3. 가독성 최적화**

- **목적**: 코드가 더 이해하기 쉽도록 개선하는 것

**4. 비트 필드란?**

- 비트 필드(Bit Field)는 구조체(struct) 내에서 변수를 비트 단위로 저장할 수 있도록 해주는
기능입니다.
즉, 여러 개의 불린(boolean) 값이나 작은 숫자를 한 개의 정수형 변수에 비트 단위로 저장
하는 방법입니다.

**비트 연산**

- 비트 필드 대신 uint8_t(1바이트)를 사용하면 직접 비트 연산을 하면 메모리 낭비를 줄이
고 성능을 향상 시킬 수 있습니다.

**구조체 정렬 (Padding)**

C 컴파일러는 **메모리 효율성과 CPU의 접근 성능을 고려하여 구조체를 정렬**합니다. 대부분의 시스템에서 기본적으로 4바이트 또는 8바이트 정렬을 사용하므로, 컴파일러가 구조체의 멤버들을 정렬할 때 `unsigned int`와 같은 **4바이트 단위**로 맞추게 됩니다.

따라서, 비트 필드가 4비트씩 할당되더라도, **구조체의 크기는 4바이트로 정렬**됩니다.

- **32비트 시스템**에서는 보통 **4바이트 단위**로 정렬됩니다.
- **64비트 시스템**에서는 보통 **8바이트 단위**로 정렬됩니다.
- 응용 문제
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <stdint.h>
    
    typedef struct {
        unsigned int is_read : 1;
        unsigned int is_write : 1;
        unsigned int is_execute : 1;
        unsigned int is_admin : 1;
    } UserPermissions;
    
    void printStatus(UserPermissions* st) {
        printf("\n사용자 권한 상태:\n");
        printf("\t - READ: %s\n", st->is_read ? "0" : "X");
        printf("\t - WRITE: %s\n", st->is_write ? "0" : "X");
        printf("\t - EXECUTE: %s\n", st->is_execute ? "0" : "X");
        printf("\t - ADMIN: %s\n", st->is_admin ? "0" : "X");
    }
    
    int main() {
        // 문제) 사용자 권한 관리 시스템을 작성해 보세요. 사용자의 권한을 관리하는 프로그램을 작성해 보세요.
        // 1) 요구사항
        // - 사용자 권한을 각각 입력 받아서 권한 4개를 설정하세요.
        // - 권한이 있으면 ‘0’ 없으면 ‘X’로 표시하여 출력하세요.
        // - 각 사용자에게 다음과 같이 4가지 권한이 있다.
        //   READ – 읽기권한 1비트
        //   WRITE – 쓰기권한 1비트
        //   EXECUTE – 실행권한 1비트
        //   ADMIN – 관리자권한 1비트
        // 2) 풀이 방식은 비트필드, 비트 연산으로 푸세요.
    
        UserPermissions u = { 0, };
    	unsigned int is_read, is_write, is_execute, is_admin;
    
        printf("사용자 권한을 설정합니다.\n");
        printf("READ 권한을 부여할까요? (1: 예, 0: 아니오) : ");
        scanf_s("%u", &is_read);
    
        printf("WRITE 권한을 부여할까요? (1: 예, 0: 아니오) : ");
        scanf_s("%u", &is_write);
    
        printf("EXECUTE 권한을 부여할까요? (1: 예, 0: 아니오) : ");
        scanf_s("%u", &is_execute);
    
        printf("ADMIN 권한을 부여할까요? (1: 예, 0: 아니오) : ");
        scanf_s("%u", &is_admin);
    
    	u.is_read = is_read;
    	u.is_write = is_write;
    	u.is_execute = is_execute;
    	u.is_admin = is_admin;
    
        printStatus(&u);
    
        printf("UserPermissions 구조체 크기: %lu바이트\n", sizeof(u));
        return 0;
    }
    
    ```
    

 

### 파일

- 운영체제에서 파일 관리하기 위해 파일 시스템을 사용
Windows → FAT32, NTFS, exFAT
Mac →  exFAT, APFS
Linux → ext4

**파일 열기와 닫기**

C에서 파일을 열거나 닫는 데는 `fopen()`과 `fclose()` 함수를 사용합니다.

```c
c
코드 복사
FILE *fp = fopen("data.txt", "r"); // 읽기 모드로 파일 열기
if (fp == NULL) {
    printf("파일을 열 수 없습니다.\n");
    return 1;
}
fclose(fp); // 파일 닫기

```

**파일 열기 모드**

| 모드 | 설명 |
| --- | --- |
| `"r"` | 읽기 모드 (파일이 존재해야 함) |
| `"w"` | 쓰기 모드 (파일이 없으면 생성, 있으면 덮어쓰기) |
| `"a"` | 추가 모드 (파일이 없으면 생성, 있으면 기존 파일 뒤에 추가) |
| `"r+"` | 읽기/쓰기 모드 (파일이 존재해야 함) |
| `"w+"` | 읽기/쓰기 모드 (파일이 없으면 생성, 있으면 덮어쓰기) |
| `"a+"` | 읽기/추가 모드 (파일이 없으면 생성, 있으면 기존 파일 뒤에 추가) |

---

**파일에 데이터 쓰기**

`fprintf()`, `fputc()`, `fputs()` 등을 사용하여 파일에 데이터를 쓸 수 있습니다.

```c
c
코드 복사
FILE *fp = fopen("data.txt", "w");
if (fp == NULL) {
    printf("파일을 열 수 없습니다.\n");
    return 1;
}
fprintf(fp, "Hello, World!\n"); // 문자열 쓰기
fclose(fp);

```

**다른 쓰기 함수**

- `fputc(c, fp)`: 한 문자 `c`를 파일에 씀
- `fputs(str, fp)`: 문자열 `str`을 파일에 씀

---

**파일에서 데이터 읽기**

`fscanf()`, `fgetc()`, `fgets()` 등을 사용하여 파일에서 데이터를 읽을 수 있습니다.

```c
c
코드 복사
FILE *fp = fopen("data.txt", "r");
if (fp == NULL) {
    printf("파일을 열 수 없습니다.\n");
    return 1;
}
char buffer[100];
while (fgets(buffer, sizeof(buffer), fp) != NULL) {
    printf("%s", buffer); // 파일 내용 출력
}
fclose(fp);

```

**다른 읽기 함수**

- `fgetc(fp)`: 한 문자 읽기
- `fscanf(fp, "%s", str)`: 형식 지정하여 읽기
- 파일 입출력 문제
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <stdint.h>
    #define FILE_NAME "customers.txt"
    
    typedef struct {
        int id;
        char name[50];
        double balance;
    } Customer;
    
    void addCustomer() {
        FILE* file = fopen(FILE_NAME, "a");
        if (!file) {
            printf("파일을 열 수 없습니다.\n");
            return;
        }
    
        Customer c;
        printf("고객 ID: ");
        scanf("%d", &c.id);
        printf("고객 이름: ");
        scanf("%s", c.name);
        printf("잔액: ");
        scanf("%lf", &c.balance);
        fprintf(file, "ID: %d, Name: %s, Balance: %.2lf\n", c.id, c.name, c.balance);
        fclose(file);
        printf(">> 고객 정보가 저장되었습니다.\n");
    }
    
    void listCustomers() {
        FILE* file = fopen(FILE_NAME, "r");
        if (!file) {
            printf("파일을 열 수 없습니다.\n");
            return;
        }
        char line[100];
        printf("\n[고객 목록]\n");
        while (fgets(line, sizeof(line), file)) {
            printf("%s", line);
        }
        fclose(file);
    }
    
    void searchCustomer() {
        FILE* file = fopen(FILE_NAME, "r");
        if (!file) {
            printf("파일을 열 수 없습니다.\n");
            return;
        }
        int searchId;
        printf("검색할 고객 ID: ");
        scanf("%d", &searchId);
        char line[100];
        int found = 0;
        while (fgets(line, sizeof(line), file)) {
            if (strstr(line, "ID:") && strstr(line, line)) {
                int id;
                sscanf(line, "ID: %d,", &id);
                if (id == searchId) {
                    printf("%s", line);
                    found = 1;
                    break;
                }
            }
        }
        if (!found) {
            printf("해당 ID의 고객을 찾을 수 없습니다.\n");
        }
        fclose(file);
    }
    
    int main() {
        char choice;
        while (1) {
            printf("\n[고객 정보 시스템]\n");
            printf("1. 신규 고객 추가 (N)\n");
            printf("2. 고객 목록 출력 (L)\n");
            printf("3. 고객 정보 검색 (S)\n");
            printf("4. 종료 (Q)\n");
            printf("선택: ");
            scanf(" %c", &choice);
            switch (choice) {
            case 'N': case 'n': addCustomer(); break;
            case 'L': case 'l': listCustomers(); break;
            case 'S': case 's': searchCustomer(); break;
            case 'Q': case 'q': return 0;
            default: printf("올바른 선택이 아닙니다.\n");
            }
        }
        return 0;
    }
    
    ```
