```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void cal(int n1, int n2) {
	printf("%d + %d = %d\n", n1, n2, n1 + n2);
	printf("%d - %d = %d\n", n1, n2, n1 - n2);
	printf("%d * %d = %d\n", n1, n2, n1 * n2);
	printf("%d / %d = %d\n", n1, n2, n1 / n2);
}

void dan(int n) {
	for (int i = 1; i < 10; i++) {
		printf("%d * %d = %d\n", n, i, n * i);
	}
}

void handleString() {
	char str[100];
	printf("문자열입력? ");
	scanf("%s", str);

	for (int i = 0; i < 100; i++) {
		if (str[i] >= 'A' && str[i] <= 'Z') {
			str[i] += 32;
		}
		else if (str[i] >= 'a' && str[i] <= 'z') {
			str[i] -= 32;
		}
	}
	printf("변환된 결과 %s\n", str);
}
int main() {
	// 1. 함수를 사용하여서 계산기 프로그램을 작성하세요.
	printf("가감승제를 원하는 두 수를 입력하세요 : ");
	int n1, n2;
	scanf("%d %d", &n1, &n2);
	cal(n1, n2);

	//2. 함수를 사용하여서 출력하고 싶은 단을 입력받아 구구단 프로그램을 작성하세요.
	int n3;
	printf("출력하고 싶은 단을 입력하세요 : ");
	scanf("%d", &n3);
	dan(n3);

	//3. 함수를 이용하여 대문자는 소문자로 소문자는 대문자로 변환하는 프로그램 작성
	handleString();
}
```
