### 정규화

데이터의 무결성을 유지하고자 테이블을 잘게 쪼개는 과정

- 데이터 중복을 제거, 무결성 유지
- 논리적으로 분리하여 효율적으로 저장, 관리하도록 설계
- 데이터 저장소의 효율 향상
- 삽입, 삭제, 갱신 이상 최소화

### 제1정규형(1NF)

- 모든 속성은 반드시 하나의 값만 가져야 한다
- 유사한 속성이 반복되는 경우도 정규화의 대상이다

### 제2정규형(2NF)

- 엔터티의 모든 일반 속성은 반드시 모든 주식별자에 종속되어야 한다.
- PK가 결합으로 이루어져 있는데, 이 부분함수에 종속인 데이터가 있으면 안된다.

### 제3정규형(3NF)

- 주식별자가 아닌 모든 속성 간에는 서로 종속될 수 없다.

### 반정규화(Denormalization)

- 분리된 테이블을 특정 성능 향상을 요하고자 다시 결합하거나 중복 데이터를 일정 허용하는 과정
- 정규화의 한계
    - 데이터의 무결성과 중복 문제를 해결하지만, 다수의 테이블 조인이 필요한 쿼리에서는 성능 저하
- 성능 최적화
    - 쿼리 성능 개선을 위해 데이터를 중복 저장하거나 테이블을 합치는 방식으로 읽기 속도를 높임
- 적용 방법
    - 속성 추가
    - 테이블 병합
    - 중복 데이터 저장
    - 이력 데이터 관리
- 장점
    - 성능 개선
    - 간단한 쿼리가 됨
    - 특정 요구사항 대응 용이
- 단점
    - 데이터 중복
    - 무결성 문제
    - 유연성 감소

### OLTP(Online Transaction Processing)

실시간으로 다수의 트랜잭션을 처리하는 시스템

- 구성 요소
    - 데이터베이스
    - 애플리케이션
    - 사용자
- 데이터 입력, 저장, 수정 및 조회를 빠르게 처리
- 은행 시스템
- 전자상거래
- 예약 시스템
- 특징
    - 실시간 처리
        - 다수의 사용자 요청을 신속하게 처리
    - 정규화된 데이터 구조
        - 데이터 중복 최소화 및 데이터 무결성 유지
    - 트랜잭션 관리
        - ACID 보장

### OLAP(Online Analytical Processing)

대규모 데이터를 분석하고, 요약 정보와 통찰력을 제공하는 시스템

- 구성 요소
    - 데이터 웨어하우스
    - OLAP 서버
    - 사용자 인터페이스
- 데이터의 분석, 보고서 생성, 인사이트 제공
- 데이터 마트
- 대시보드
- 비즈니스 인텔리전스
- 특징
    - 복잡한 분석
        - 비즈니스 인사이트 제공
    - 대규모 데이터 처리
        - DW, DM 활용하여 분석
    - 다차원 데이터 분석
        - 시간, 지역, 제품 등 여러 차원 기준으로 데이터 요약

### OLTP와 OLAP 상호작용

- OLTP 시스템은 데이터 생성, OLAP 시스템은 데이터 활용
- ETL(Extract, Transform, Load) 프로세스 : 데이터 웨어하우스로 데이터를 전송하기 위해 사용
- OLTP → ETL → OLAP의 흐름

### 논리 모델

- 데이터베이스 설계의 한 단계로 데이터의 구조와 관계를 기술하는 모델
- 엔터티와 속성을 정의함
- 관계와 제약 조건을 표현함
- 중복 제거를 위한 **정규화 적용**

### 물리 모델

- 시스템에 반영할 수 있는 형태로 만드는 것
- 최종 설계 단계로, 특정 DBMS에 종속적인 구조를 정의
- 성능, 보안, 저장소를 고려하여 실제 구현
- 데이터 타입, 제약 조건을 지정
- 인덱스, 파티션, 저장소 구조 설계

### 트랜잭션

- 데이터베이스에서 하나의 논리적 작업 단위
- ACID
    - Atomicity
        - 모두 실행되거나, 모두 실행되지 않아야 함
    - Consistency
        - 완료된 후 항상 일관된 무결성이 보장되어야 함
    - Isolation
        - 격리 수준으로 구현
    - Durability
        - 완료된 데이터는 영구적으로 저장
- 설계 시 주의사항
    - 작업 단위를 명확히 정의
    - 최소한의 데이터 변경
    - 장기 트랜잭션 방지
    - 적절한 격리 수준 설정

### SQL(Structured Query Language)

데이터베이스를 관리하고 조작하기 위한 언어

- DDL(Data Definition Language)
    - 데이터베이스 구조 정의
    - CREATE
    - ALTER
    - DROP
    - TRUNCATE
- DML(Data Manipulation Language)
    - 데이터를 조작 CRUD
- DCL(Data Control Language)
    - 데이터 접근 권한 제어
- TCL(Transaction Control Language)
    - 트랜잭션 관리

### DDL

- CREATE TABLE
    
    ```sql
    CREATE TABLE CUSTOMERS (
    	CUSTOMER_ID NUMBER PRIMARY KEY,
    	NAME VARCHAR2(50) NOT NULL,
    	EMAIL VARCHAR2(100) UNIQUE,
    	PHONE VARCHAR2(15),
    	CREATED_AT DATE DEFAULT SYSDATE --현재 시간
    );
    ```
    
- ALTER
    
    ```sql
    ALTER TABLE CUSTOMERS ADD ADDRESS VARCHAR2(200); --컬럼 추가
    ALTER TABLE CLIENTS MODIFY CONTACT_EMAIL VARCHAR2(200); --컬럼 변경
    ALTER TABLE CUSTOMERS DROP COLUMN PHONE; --컬럼 삭제
    
    ALTER TABLE CLIENTS RENAME COLUMN NAME TO FULL_NAME; --컬럼명 변경
    ALTER TABLE CUSTOMERS RENAME TO CLIENTS; --테이블명 변경
    ```
    
- DROP
    
    ```sql
    DROP TABLE CUSTOMERS; --테이블 삭제 
    
    TRUNCATE TABLE CUSTOMERS; --테이블 데이터 초기화
    ```
    

### DML

- SELECT
    - SQL에서 데이터를 검색하는 명령어
    - 테이블에서 원하는 데이터를 추출
    - 조건, 정렬, 그룹핑을 사용하여 복잡한 질의 가능
        - ALIAS(AS) 연산 결과에 의미를 부여하기 위해 테이블에 부여하는 임시 이름
    
    ```sql
    SELECT NAME, EMAIL
    FROM CUSTOMERS
    WHERE PHONE = '555-1234';
    ```
    
- INSERT
    
    ```sql
    INSERT INTO CUSTOMERS (CUSTOMER_ID, NAME, EMAIL, PHONE)
    VALUES (1, 'JOHN DOE', 'JOHN.DOE@EXAMPLE.COM', '555-1234');
    ```
    
- UPDATE
    
    ```sql
    UPDATE CUSTOMERS
    SET EMAIL = 'JOHN.NEW@EXAMPLE.COM'
    WHERE CUSTOMER_ID = 1;
    ```
    
- DELETE
    
    ```sql
    DELETE FROM CUSTOMERS
    WHERE CUSTOMER_ID = 1;
    ```
    

### DCL

- GRANT(권한 부여)
    
    ```sql
    GRANT SELECT ON CUSTOMERS TO USER1;
    ```
    
- REVOKE(권한 회수)
    
    ```sql
    REVOKE SELECT ON CUSTOMERS FROM USER1;
    ```
    

### TCL

- COMMIT(트랜잭션 확정)
    
    ```sql
    DROP TABLE CUSTOMERS; --테이블 삭제 
    
    TRUNCATE TABLE CUSTOMERS; --테이블 데이터 초기화
    ```
    
- ROLLBACK(트랜잭션 취소)
    
    ```sql
    DROP TABLE CUSTOMERS; --테이블 삭제 
    
    TRUNCATE TABLE CUSTOMERS; --테이블 데이터 초기화
    ```
    
- SAVEPOINT(특정 지점 저장)
    
    ```sql
    DROP TABLE CUSTOMERS; --테이블 삭제 
    
    TRUNCATE TABLE CUSTOMERS; --테이블 데이터 초기화
    ```
    

### NULL

NULL 값은 다른 값과 비교할 수 없음

- 값이 없음을 의미
- 숫자 문자 등 어떤 데이터도 저장되지 않은 상태
- 정의되지 않은 상태
- `select * from a where b = null (x) b is null (o)`

### WHERE

데이터를 필터링 하기 위한 키워드

- 비교 연산자
    - =, <> , >, <, ≥, ≤
- 논리 연산자
    - AND, OR, NOT
- 특수 연산자
    - LIKE, IN, BETWEEN, IS NULL
    - LIKE
        - % : 0개 이상
        - _: 1개
        - ESCAPE: ‘#_3%’ ESCAPE ‘#’처럼 맨 뒤에 와일드카드 %가 오기전까지의 패턴을 실제 문자로 인식
- 연산자의 우선순위
    - 괄호 - 비교 연산자 - NOT - AND - OR
