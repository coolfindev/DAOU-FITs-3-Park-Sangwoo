## 2차원 배열 할당 예제

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
int main() {
	int row = 3, col = 4;

	int** arr = (int**)calloc(row, sizeof(int*));
	for (int i = 0; i < row; i++) {
		arr[i] = (int*)calloc(col, sizeof(int));
	}

	for (int i = 0; i < row; i++) {
		for (int j = 0; j < col; j++) {
			arr[i][j] = i * col + j;
			printf("%2d\t", arr[i][j]);
		}
		printf("\n");
	}

	for (int i = 0; i < row; i++) {
		free(arr[i]);
	}
	return 0; 
}


```
