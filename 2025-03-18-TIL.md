### struct(구조체) - union(공용체) - enum(열거형)

- 공용체는 다양한 데이터 타입을 처리해야 하지만, 동시에 하나만 저장하면 되는 상황에서 유
용
- 금융권에서는 데이터 저장 공간을 절약하기 위해 다양한 금융 상품을 하나의 공용체로 표현하
는 경우가 많음

- 응용문제1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <time.h>
    
    typedef enum {
    	CASH,
    	CARD,
    	CRYPTO
    } TransactionType;
    
    typedef union {
    	int cash;
    	struct {
    		char cardNumber[20];
    		int month;
    	} card;
    	struct {
    		char walletAddress[50];
    		int amount;
    	} crypto;
    } TransactionData;
    
    typedef struct {
    	TransactionType type;
    	TransactionData data;
    } Transaction;
    
    int main() {
    	//1. 거래 내역 관리 시스템 구현 하세요.
    
    	Transaction cardTransaction;
    	cardTransaction.type = CARD;
    	sprintf(cardTransaction.data.card.cardNumber, "1234-5678-1234-5678");
    	cardTransaction.data.card.month = 12;
    
    	Transaction cashTransaction;
    	cashTransaction.type = CASH;
    	cashTransaction.data.cash = 10;
    
    	Transaction cryptoTransaction;
    	cryptoTransaction.type = CRYPTO;
    	sprintf(cryptoTransaction.data.crypto.walletAddress, "0x1234567890abcdef");
    	cryptoTransaction.data.crypto.amount = 100;
    
    	printf("현금 거래 - 지폐 개수 : %d장\n", cashTransaction.data.cash);
    	printf("카드 거래 - 카드번호 : %s | 할부 : %d개월\n", cardTransaction.data.card.cardNumber, cardTransaction.data.card.month);
    	printf("가상화폐 거래 - 지갑 주소 : %s | 거래 금액 : %d BTC\n", cryptoTransaction.data.crypto.walletAddress, cryptoTransaction.data.crypto.amount);
    	return 0; 
    }
    
    ```
    

### 메모리

| 메모리 영역 | 설명 | 특징 |
| --- | --- | --- |
| Text Segment | 코드영역 | - 실행 코드(기계어 코드) 저장
- 낮은 메모리 주소
- 컴파일러가 생성한 실행 파일(.text section)에 포함
- 읽기 전용(read-only) |
| Data Segment | 초기화된 데이터 영역 | - 초기화된 **전역변수 및 정적 변수** 저장
- 프로그램 실행 전에 메모리에 로드됨 |
| BSS Segment | 미초기화 데이터 영역 | - 초기화되지 않은 전역/정적 변수 저장
- 실행 시 0으로 자동 초기화됨 |
| Heap | 동적 할당 영역 | - malloc, calloc, realloc, free 사용
- 동적 메모리 할당 |
| Stack | 임시 데이터 영역 | - 함수 호출시 생성되는 지역변수
- 높은 메모리 주소 |

### malloc()

- Memory Allication함수는 C언어에서 동적 메모리를 할당하는 함수
- 실시간(런타임) 동안 힙 영역에 메모리를 할당
- 초기화되지 않은 메모리 블록의 포인터를 반환
- 할당된 메모리는 반드시 free()을 사용하여 해제

**문제점**

- 초기화 되지 않은 메모리 블록은 쓰레기 값 대입
- 초기화가 필요하면 callock으로 사용하면 초기값이 자동으로 채워짐

**예시**

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
int main() {
	int* ptr = (int*)malloc(sizeof(int)); //여기 *n으로 malloc하면 해당 크기만큼의 메모리 할당 가능
	if (ptr == NULL) {
		printf("Memory not allocated.\n");
		exit(0);
	}

	*ptr = 42;
	*ptr += 400;
	if (*ptr > 100) {
		printf("Value of ptr: %d\n", *ptr);
	}
	free(ptr);

	return 0; 
}

```

### calloc()

- calloc() (Contiguous Allocation) 함수는 C 언어에서 동적 메모리를 할당하는 함수
- 0으로 초기화된 메모리 블록을 반환하며, 연속된 여러 개의 요소를 한 번에 할당할 때 유용함

**사용시 주의점**

- calloc은 성능이 다소 malloc 보다 느릴 수 있다.
왜? 할당 후 모든 바이트를 0으로 초기화하기 때문
- 0으로 초기화 할 필요 없는 경우 malloc을 사용할 것

### realloc()

- realloc() 함수는 이미 할당된 메모리 블록의 크기를 변경하는 함수
- 기존에 할당된 메모리 블록을 새로운 크기로 확장하거나 축소할 수 있음
- 크기를 확장할 때 기존 데이터를 그대로 유지하며 새로운 메모리 블록을 할당
- 기존 메모리 크기를 변경하면서 새로운 메모리 주소를 반환할 수 있기 때문에, 변경된 포인
터를 반드시 저장해야 함

### 메모리할당 문제 예제

- 2차원 배열 할당 예제
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    int main() {
    	int row = 3, col = 4;
    
    	int** arr = (int**)calloc(row, sizeof(int*));
    	for (int i = 0; i < row; i++) {
    		arr[i] = (int*)calloc(col, sizeof(int));
    	}
    
    	for (int i = 0; i < row; i++) {
    		for (int j = 0; j < col; j++) {
    			arr[i][j] = i * col + j;
    			printf("%2d\t", arr[i][j]);
    		}
    		printf("\n");
    	}
    
    	for (int i = 0; i < row; i++) {
    		free(arr[i]);
    	}
    	return 0; 
    }
    
    ```
    
- malloc 응용 문제1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    int main() {
    	//1. 학생들의 성적을 동적으로 관리하는 프로그램을 작성하세요.
    	// 각 학생은 이름과 점수를 가지고 있습니다.학생수는 프로그램 실행 중에 입력받고, 동적으로
    	//	할당된 배열을 사용하여 학생들의 정보를 관리하는 프로그램입니다.
    	//	1) 요구사항
    	//	- 학생 수는 사용자로부터 입력받습니다. - 각 학생은 이름(최대 100자)과 점수(정수)를 가지고 있습니다. - malloc()을 이용하여 학생 수만큼의 메모리를 동적으로 할당합니다. - 학생 정보 입력 : 학생의 이름과 점수를 입력받습니다. - 학생 정보 출력 : 각 학생의 이름과 점수를 출력합니다. - 메모리 해제 : 동적으로 할당된 메모리는 사용 후 free()를 이용해 해제해야 합니다. - 
    	//	각 학생의 점수는 0~100 사이의 정수여야 하며, 점수 입력 시 유효성 검사를 해야 합니다.
    
    	int n;
    	printf("학생 수를 입력하세요 : ");
    	scanf("%d", &n);
    
    	char** name = (char**)malloc(sizeof(char*) * n);
    	int* score = (int*)calloc(n, sizeof(int));
    
    	for (int i = 0; i < n; i++) {
    		name[i] = (char*)malloc(sizeof(char) * 100);
    		printf("학생 %d의 이름을 입력하세요 : ", i + 1);
    		scanf("%s", name[i]);
    
    		do{
    			printf("학생 %d의 점수를 입력하세요 (0~100) : ", i + 1);
    			scanf("%d", &score[i]);
    			if (score[i] < 0 || score[i] > 100) {
    				printf("잘못된 점수입니다. 다시 입력해주세요.\n");
    			}
    		} while (score[i] < 0 || score[i] > 100);
    	}
    
    	printf("입력된 학생 정보\n");
    
    	for (int i = 0; i < n; i++) {
    		printf("학생 %d : %s, 점수: %2d\n", i + 1, name[i], score[i]);
    	}
    
    	for (int i = 0; i < n; i++) {
    		free(name[i]);
    	}
    	free(name);
    	free(score);
    	return 0; 
    }
    
    ```
    
- malloc 응용 문제2
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    typedef struct {
    	char name[20];
    	int account;
    	double balance;
    } Customer;
    
    int main() {
    	//2. 금융권에서 고객들의 계좌를 관리하는 프로그램을 작성하세요.
    	int n;
    	printf("고객 수를 입력하세요 : ");
    	scanf("%d", &n);
    
    	Customer* c = (Customer*)malloc(sizeof(Customer) * n);
    	for (int i = 0; i < n; i++) {
    		printf("고객 %d의 이름을 입력하세요 : ", i + 1);
    		scanf("%s", c[i].name);
    		printf("고객 %d의 계좌 번호를 입력하세요 : ", i + 1);
    		scanf("%d", &c[i].account);
    		while (1) {
    			printf("고객 %d의 잔고를 입력하세요 : ", i + 1);
    			scanf("%lf", &c[i].balance);
    			if (c[i].balance < 0) {
    				printf("잔고는 음수로 입력할 수 없습니다.\n");
    			}
    			else {
    				break;
    			}
    		}
    	}
    
    	printf("입력된 고객 정보\n");
    	for (int i = 0; i < n; i++) {
    		printf("고객 %d : %s, 계좌 번호 : %d, 잔고 : %.2lf\n", i + 1, c[i].name, c[i].account, c[i].balance);
    	}
    
    	free(c);
    	return 0; 
    }
    
    ```
    
- malloc 응용 문제3
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    typedef struct {
    	double amount; //대출금액
    	double interest;//대출이자율
    	int period;//대출기간
    	double balance; //대출 잔액
    } Loan;
    
    typedef struct {
    	char name[20];
    	int account; //계좌번호
    	//대출 정보
    	int loanCount;
    	Loan* loan;
    } Customer;
    
    int main() {
    	// 3. 은행 대출 관리 시스템을 구현합니다.고객은 대출을 받거나 상환을 할 수
    	//	있으며, 이를 동적으로 관리하는 프로그램을 작성합니다.
    	/*1) 요구사항
    	- 고객 정보(이름, 계좌 번호)와 대출 정보(대출 금액, 대출 이자율, 대출 기간)를 관리합니다.
    	- 고객은 여러 개의 대출을 받을 수 있으며, 각 대출의 정보를 동적으로 할당된 메모리로 관리합니다. 
    	- 대출 금액은 양의 정수, 이자율은 0~100 사이의 실수, 대출 기간은 1년 이상이어야 합니다.
    	- 각 고객은 대출을 상환할 수 있습니다.상환할 때는 상환 금액을 입력받아 대출 잔액을 차감합니다. 
    	- 프로그램 종료 전에 각 고객의 대출 잔액을 출력하고, 동적으로 할당된 모든 메모리를 해제해야합니다.*/
    
    	int n;
    	printf("고객 수를 입력하세요: ");
    	scanf("%d", &n);
    
    	Customer* customers = (Customer*)malloc(sizeof(Customer) * n);
    
    	for (int i = 0; i < n; i++) {
    		printf("고객 %d의 이름을 입력하세요:", i+1);
    		scanf("%s", customers[i].name);
    		printf("고객 %d의 계좌번호를 입력하세요:", i + 1);
    		scanf("%d", &customers[i].account);
    		printf("고객 %d이 받은 대출 수: ", i+1);
    		scanf("%d", &customers[i].loanCount);
    
    		int loanCount = customers[i].loanCount;
    		customers[i].loan = (Loan*)malloc(sizeof(Loan) * loanCount);
    		for (int j = 0; j < loanCount; j++) {
    			printf("대출 %d:\n", j + 1);
    			printf("대출 금액을 입력하세요: ", j + 1);
    			scanf("%lf", &customers[i].loan[j].amount);
    			customers[i].loan[j].balance = customers[i].loan[j].amount;
    			printf("대출 %d의 이자율을 입력하세요 (0~100) ", j + 1);
    			scanf("%lf", &customers[i].loan[j].interest);
    			printf("대출 %d의 기간을 입력하세요 (1년 이상): ", j + 1);
    			scanf("%d", &customers[i].loan[j].period);
    		}
    
    		for (int j = 0; j < loanCount; j++) {
    			printf("상환할 금액을 입력하세요 (대출 %d): ", j + 1);
    			double payment;
    			scanf("%lf", &payment);
    			customers[i].loan[j].balance -= payment;
    		}
    	}
    
    	printf("입력된 고객 및 대출 정보\n");
    	for (int i = 0; i < n; i++) {
    		printf("고객 %d: %s, 계좌 번호: %d\n", i + 1, customers[i].name, customers[i].account);
    		for (int j = 0; j < customers[i].loanCount; j++) {
    			printf("대출 %d: 대출 금액: %.2lf, 이자율: %.2lf%, 대출기간: %d년, 대출 잔액: %.2lf\n", j + 1, customers[i].loan[j].amount, customers[i].loan[j].interest, customers[i].loan[j].period, customers[i].loan[j].balance);
    		}
    	}
    
    	for (int i = 0; i < n; i++) {
    		free(customers[i].loan);
    	}
    	free(customers);
    
    	return 0; 
    }
    
    ```
    
- calloc 응용문제 1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    typedef struct {
    	char name[20];
    	int count;
    	int price;
    } Trade;
    
    typedef struct {
    	char name[20];
    	char account[20];
    	int tradeCount;
    	Trade* tradeList;
    } Customer;
    
    int main() {
    	//1. 증권사에서는 고객들의 주식 거래 내역을 관리하는 시스템을 운영합니다. 1) 요구사항
    	//- 고객 정보(고객 이름, 계좌 번호, 거래 내역)와
    	//	주식 거래 내역(주식 이름, 거래 수량, 거래 가격)을 동적으로 관리합니다.
    	//	- 고객은 여러 번 거래를 할 수 있으며, 각 거래 내역은 동적 배열로 관리됩니다.
    	//	- 프로그램은 사용자가 입력한 고객 수에 맞게 동적으로 메모리 할당하고,
    	//	각 고객의 주식 거래 정보를 저장하고 출력합니다.
    	//	- 주식 거래 내역을 출력할 때, 각 거래는 주식이름, 거래수량, 거래가격을 포함해야 합니다.
    	//	- 사용자가 종료 시, 동적으로 할당된 메모리는 반드시 free()를 사용하여 해제해야 합니다.
    	int n;
    	printf("고객 수를 입력하세요: ");
    	scanf("%d", &n);
    
    	Customer* c = (Customer*)malloc(n * sizeof(Customer));
    
    	for (int i = 0; i < n; i++) {
    		printf("고객 %d의 이름을 입력하세요: ", i + 1);
    		scanf("%s", c[i].name);
    		printf("고객 %d의 계좌번호를 입력하세요: ", i + 1);
    		scanf("%s", c[i].account);
    		printf("고객 %d의 거래 내역 수: ", i + 1);
    		scanf("%d", &c[i].tradeCount);
    
    		int tc = c[i].tradeCount;
    		c[i].tradeList = (Trade*)calloc(tc ,sizeof(Trade));
    		for (int j = 0; j < tc; j++) {
    			printf("거래 %d:\n", j + 1);
    			printf("주식 이름을 입력하세요: ");
    			scanf("%s", c[i].tradeList[j].name);
    			printf("거래 수량을 입력하세요: ");
    			scanf("%d", &c[i].tradeList[j].count);
    			printf("거래 가격을 입력하세요: ");
    			scanf("%d", &c[i].tradeList[j].price);
    		}
    	}
    	
    	for (int i = 0; i < n; i++) {
    		printf("고객 %d: %s, 계좌 번호: %s\n", i + 1, c[i].name, c[i].account);
    		for (int j = 0; j < c[i].tradeCount; j++) {
    			printf("거래 %d: 주식 이름 : %s, 거래 수량 : %d, 거래 가격: %d\n", j + 1, c[i].tradeList[j].name, c[i].tradeList[j].count, c[i].tradeList[j].price);
    		}
    		printf("\n");
    	}
    
    	for (int i = 0; i < n; i++) {
    		free(c[i].tradeList);
    	}
    	free(c);
    	return 0; 
    }
    
    ```
    
- calloc 응용문제 2
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    typedef struct {
    	char name[100];
    	double price;//구매 가격
    	double current; //현재 가격
    	int quantity; //수량
    } Portfolio;
    
    typedef struct {
    	char name[100];
    	int account;
    	Portfolio* portfolios;
    } Customer;
    
    int main() {
    	int n, pn;
    	printf("고객 수를 입력하세요: ");
    	scanf("%d", &n);
    
    	Customer* customers = (Customer*)malloc(sizeof(Customer) * n);
    
    	for (int i = 0; i < n; i++) {
    		printf("고객 %d의 이름을 입력하세요: ", i + 1);
    		scanf("%s", customers[i].name);
    		printf("고객 %d의 계좌번호를 입력하세요: ", i + 1);
    		scanf("%d", &customers[i].account);
    		printf("고객 %d의 포트폴리오에 포함될 종목 수: ", i + 1);
    		scanf("%d", &pn);
    
    		customers[i].portfolios = (Portfolio*)calloc(pn, sizeof(Portfolio));
    		for (int j = 0; j < pn; j++) {
    			printf("주식 종목 %d:\n", j + 1);
    			printf("주식 이름을 입력하세요: ");
    			scanf("%s", customers[i].portfolios[j].name);
    			printf("구매 가격을 입력하세요: ");
    			scanf("%lf", &customers[i].portfolios[j].price);
    			printf("현재 가격을 입력하세요: ");
    			scanf("%lf", &customers[i].portfolios[j].current);
    			printf("보유 주식 수를 입력하세요: ");
    			scanf("%d", &customers[i].portfolios[j].quantity);
    		}
    	}
    	printf("\n");
    	for (int i = 0; i < n; i++) {
    		printf("고객 %d, 계좌 번호 : %d\n", i + 1, customers[i].account);
    		printf("포트폴리오:\n");
    		for (int j = 0; j < pn; j++) {
    			printf("주식 종목 %d: %s, 구매 가격: %.2lf, 현재 가격: %.2lf, 보유 수량: %d, 총 구매 금액: %.2lf, 현재 가치: %.2lf\n", j + 1, customers[i].portfolios[j].name, customers[i].portfolios[j].price, customers[i].portfolios[j].current, customers[i].portfolios[j].quantity, customers[i].portfolios[j].price * customers[i].portfolios[j].quantity, customers[i].portfolios[j].current * customers[i].portfolios[j].quantity);
    		}
    	}
    
    	for (int i = 0; i < n; i++) {
    		free(customers[i].portfolios);
    	}
    	free(customers);
    
    	return 0; 
    }
    
    ```
