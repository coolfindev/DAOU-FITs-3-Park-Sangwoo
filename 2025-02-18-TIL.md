### Servlet 데이터 주고받는 과정

1. 클라이언트 → 웹 서버 서비스를 요청(브라우저)
    1. 이 때 HTTP 프로토콜을 기반으로 요청 정보가 생성됨
2. 웹 서버는 클라이언트로부터 받은 요청 정보의 `URI`를 살펴보고 servlet 요청이면 WAS에게 처리를 넘김
3. WAS안의 `Servlet Container`는 받은 `servlet class의 instance`를 탐색
4. 이때 만약 이 클라이언트의 최초 request면, servlet instance가 없다면 servlet class로부터 instance를 생성하고 `init()`를 호출
    1. 만약 servlet instance가 이미 존재하고 있다면 다시 생성하지 않음
5. init()이 끝난후에 `Servlet Container`는 client의 요청에 대한 내용을 `HttpServletReqeust class type`의 객체로 생성
    1. 요청 정보가 내부에 들어있고, 또한 응답을 위해 `HttpServletResponse class type`의 객체도 하나 생성함
6. `WAS`안에있는 `Thread Invoker`에 의해 클라이언트의 요청을 처리할 `Thread`가 하나 생성됨
7. Thread가 생성된 Servlet instance가 가지고 있는 `service()` 메소드를 호출
    1. 이 때 Container에 의해 생성된 객체 2개가 이 `service()`함수의 인자로 전달됨
8. `service()` 함수는 기본적으로 client의 요청이 어떤 request method를 이용했는지 파악해 그에 맞는 `doXXX() method`를 호출
9. 최종 처리는 해당 `doXXX()` method에서 `request`와 `response`객체를 이용하여 처리

### 상태

- 기본적으로 HTTP는 연결당 하나의 요청만 처리하는 무상태(stateless) 프로토콜
- 웹 서버는 하나의 요청에 대해 응답한 후 바로 연결을 종료함
- 따라서 동일한 사용자가 다시 요청했을 때, 웹 서버는 동일한 사용자라는 것을 인식하지 못함
- 이런 HTTP 프로토콜의 문제점을 극복하고자 사용자의 상태 정보를 관리하는 매커니즘 → **세션, 쿠키, URL 재작성 등이 존재**

### 쿠키

- **문자열 형식**
    - `name=홍길동;`
- **유효 기간 존재**
    - 쿠키의 `Expires` 또는 `Max-Age` 속성을 설정하여 만료 시점 지정 가능
- **클라이언트 웹 브라우저에 저장**
    - 브라우저가 자동으로 쿠키를 서버에 전송하여 사용자의 상태를 유지
- **보안성 문제**
    - 클라이언트에 저장되므로 조작이 가능하며, 보안이 중요한 데이터 저장에 부적합
    - **HttpOnly** 속성을 설정하면 JavaScript에서 접근을 차단할 수 있음
    - **Secure** 속성을 사용하면 HTTPS 환경에서만 전송됨
- **도메인 및 경로 제한**
    - 특정 도메인 및 경로에서만 쿠키를 전송하도록 설정 가능

### 세션

- **서버 측에서 관리하는 상태 정보**
    - 세션 ID를 이용해 클라이언트의 상태를 유지
- **쿠키 기반 세션 관리**
    - 일반적으로 세션 ID를 쿠키(`JSESSIONID` 등)를 통해 클라이언트에게 전달하고, 이후 요청 시 이를 참조하여 세션을 유지
- **보안성 우수**
    - 중요한 정보는 서버에 저장되므로 쿠키보다 보안성이 높음
- **유효 기간**
    - 일정 시간 동안 사용자의 요청이 없으면 세션이 만료됨 (예: 30분)
    - 사용자가 직접 로그아웃하면 세션이 즉시 종료됨
- **단점**
    - 모든 사용자 정보를 서버가 관리하므로 서버의 메모리 사용량 증가
    - 서버가 재시작되거나 문제가 발생하면 세션 데이터가 손실될 수 있음

```java
HttpSession session = req.getSession() ;
session.setAttribute("user",user);

//setAttribute() 값 저장
//getAttribute() 값 추출
```

### JSP

- `JSP`는 Java를 이용한 서버 사이드 템플릿 엔진이며, 자바를 웹 서버에서 쉽게 쓰기 위한 기술
- Java 코드를 포함하는 `html`
- JSP page는 `Web Container`에 의해 `Servlet`으로 변환 되며 변환된 Servlet은 해당 JSP page에 대한 요청을 처리
- 결국은 Servlet으로 실행

- ![image (3)](https://github.com/user-attachments/assets/83a2b61d-2e26-4636-8b23-69d8ac046f69)
