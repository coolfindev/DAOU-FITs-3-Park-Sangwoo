### Layered Architecture(4계층)

계층 간 한 번에 뛰어넘을 수 없고 계층끼리 소통함

1. Presentation Layer
    1. 입출력과 관련된 코드
2. Business Layer
    1. 로직 수행 및 처리
3. Persistence Layer
    1. 데이터베이스 처리
4. Database Layer
    1. 실제 데이터베이스

### MVC 패턴

- M(model)
    - 데이터 모델
        - 데이터로 인해 파생되는 인스턴스
        - VO
        - DTO
    - 서비스 모델(비즈니스 로직)
        - 로직을 처리하는 모델(Class)
        - DB처리 하지 않음
        - DAO를 이용하여 처리함
- V(view)
    - 로직을 처리하지 않고 입출력을 담당함(사용자에게 보여지는 부분)
- C(controller)
    - view, model(service)를 연결하는 브릿지 역할

### MVC 패턴 실습

- 모델(VO) 생성

```java
package booksearchmvc.vo;

public class BookVO {
    private String bisbn;
    private String btitle;
    private int bprice;
    private String bauthor;

    public BookVO() {}
    public BookVO(String bisbn, String btitle, int bprice, String bauthor) {
        this.bisbn = bisbn;
        this.btitle = btitle;
        this.bprice = bprice;
        this.bauthor = bauthor;
    }

    public String getBisbn() {
        return bisbn;
    }

    public void setBisbn(String bisbn) {
        this.bisbn = bisbn;
    }

    public String getBtitle() {
        return btitle;
    }

    public void setBtitle(String btitle) {
        this.btitle = btitle;
    }

    public int getBprice() {
        return bprice;
    }

    public void setBprice(int bprice) {
        this.bprice = bprice;
    }

    public String getBauthor() {
        return bauthor;
    }

    public void setBauthor(String bauthor) {
        this.bauthor = bauthor;
    }

}

```

- 로직을 처리하는 service
- 인자로 들어온 키워드를 이용해서 DB에서 관련된 책을 찾고
- 리스트로 만들어서 리턴

```java
package booksearchmvc.service;

import booksearchmvc.dao.BookDAO;
import booksearchmvc.vo.BookVO;
import javafx.collections.FXCollections;
import javafx.collections.ObservableList;

public class BookSearchMVCService {
    public BookSearchMVCService() {}

    public ObservableList<BookVO> searchBookByKeyword(String text) {
        //로직 처리
        BookDAO bookDAO = new BookDAO();
        return bookDAO.select(text);
    }
}

```

- 실제 처리는 DAO에서 담당
- DAO

```java
package booksearchmvc.dao;

import booksearchmvc.vo.BookVO;
import javafx.collections.FXCollections;
import javafx.collections.ObservableList;

import java.sql.*;

public class BookDAO {
    public ObservableList<BookVO> select(String text) {
        ObservableList<BookVO> books = FXCollections.observableArrayList();
        try {
            Class.forName("oracle.jdbc.driver.OracleDriver");
            String url = "jdbc:oracle:thin:@localhost:1521:xe";
            String user = "C##DEV";
            String password = "1423";
            Connection conn = DriverManager.getConnection(url, user, password);

            String sql = "select bisbn, btitle, bprice, bauthor ";
            sql += "from book where btitle like ?";

            PreparedStatement ps = conn.prepareStatement(sql);
            ps.setString(1, "%" + text + "%");

            ResultSet rs = ps.executeQuery();
            while (rs.next()) {
                BookVO book = new BookVO(rs.getString("bisbn"), rs.getString("btitle"), rs.getInt("bprice"), rs.getString("bauthor"));
                books.add(book);
            }

            rs.close();
            ps.close();
            conn.close();
        } catch (ClassNotFoundException e) {
            e.printStackTrace();
        } catch (SQLException e) {
            e.printStackTrace();
        }
        return books;
    }
}

```

- 화면에 보여지는 View

```java
package booksearchmvc.view;

import javafx.application.Application;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.stage.Stage;

public class BookSearchMVCView extends Application {
    @Override
    public void start(Stage stage) throws Exception {
        //화면 구성
        Parent root = null;

        FXMLLoader loader = new FXMLLoader(getClass().getResource("booksearchmvc.fxml"));
        try {
            root = loader.load();
        } catch(Exception e) {
            e.printStackTrace();
        }

        Scene scene = new Scene(root);
        stage.setScene(scene);
        stage.setTitle("도서검색 MVC");

        stage.show();
    }

    public static void main(String[] args) {
        launch(args);
    }
}

```

- 로직과 화면을 연결하는 Controller

```java
package booksearchmvc.controller;

import booksearchmvc.service.BookSearchMVCService;
import booksearchmvc.vo.BookVO;
import javafx.collections.ObservableList;
import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.control.Button;
import javafx.scene.control.TableColumn;
import javafx.scene.control.TableView;
import javafx.scene.control.TextField;
import javafx.scene.control.cell.PropertyValueFactory;

import java.net.URL;
import java.util.ResourceBundle;

public class BookSearchMVCController implements Initializable {

    @FXML private TextField textField;
    @FXML private Button searchBtn;
    @FXML private Button deleteBtn;
    @FXML private TableView<BookVO> tableView;
    @FXML private TableColumn<BookVO, String> isbnCol;
    @FXML private TableColumn<BookVO, String> titleCol;
    @FXML private TableColumn<BookVO, Integer> priceCol;
    @FXML private TableColumn<BookVO, String> authorCol;

    @Override
    public void initialize(URL url, ResourceBundle resourceBundle) {
        //View의 Controls같은 이벤트를 등록
        //refernece가 있어야

        isbnCol.setCellValueFactory(new PropertyValueFactory<>("bisbn"));
        titleCol.setCellValueFactory(new PropertyValueFactory<>("btitle"));
        priceCol.setCellValueFactory(new PropertyValueFactory<>("bprice"));
        authorCol.setCellValueFactory(new PropertyValueFactory<>("bauthor"));

        searchBtn.setOnAction(event -> {
            BookSearchMVCService service = new BookSearchMVCService();
            ObservableList<BookVO> list = service.searchBookByKeyword(textField.getText());
            tableView.setItems(list);
        });
    }
}

```

### Dependency Injection

- DAO에서 아래와 같이 중복된 코드 사용 발견

```java
 public void insert(UserVO user) {
        try {
            Class.forName("oracle.jdbc.driver.OracleDriver");
            String url = "jdbc:oracle:thin:@localhost:1521:xe";
            String id = "C##DEV";
            String pw = "1423";
            Connection con = DriverManager.getConnection(url, id, pw);
            String sql = "insert into users2 values(?,?,?)";
            PreparedStatement ps = con.prepareStatement(sql);

            ps.setString(1, user.getId());
            ps.setString(2, user.getName());
            ps.setString(3, user.getPassword());
            ps.executeUpdate();
            con.close();
            ps.close();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    public UserVO select(String userId) {
        try {
            Class.forName("oracle.jdbc.driver.OracleDriver");
            String url = "jdbc:oracle:thin:@localhost:1521:xe";
            String id = "C##DEV";
            String pw = "1423";
            Connection con = DriverManager.getConnection(url, id, pw);
            String sql = "select * from users2 where id = ?";
            PreparedStatement ps = con.prepareStatement(sql);

            ps.setString(1, userId);

            ResultSet rs = ps.executeQuery();
            rs.next();
            UserVO user = new UserVO(rs.getString("id"), rs.getString("name"), rs.getString("password"));

            con.close();
            ps.close();
            rs.close();

            return user;
        } catch (Exception e) {
            e.printStackTrace();
        }
        return null;
    }
```

- 이를 해결하기 위해 아래와 같이 메소드로 관리하기 쉽게 변경

```java
    private Connection getConnection() throws SQLException, ClassNotFoundException {
        Class.forName("oracle.jdbc.driver.OracleDriver");
        String url = "jdbc:oracle:thin:@localhost:1521:xe";
        String id = "C##DEV";
        String pw = "1423";
        Connection con = DriverManager.getConnection(url, id, pw);

        return con;
    }
```

- 그러나 해당 메소드의 경우 한 사람에게만 적용할 수 있는 코드(확장성x)이므로 추상클래스화 하여 이를 상속받아 범용성 확장

```java
package di.step3;

import java.sql.Connection;
import java.sql.DriverManager;

public class KUserDAO extends UserDAO {
    @Override
    public Connection getConnection()  {

        try {
            Class.forName("oracle.jdbc.driver.OracleDriver");
            String url = "jdbc:oracle:thin:@localhost:1521:xe";
            String id = "C##DEV";
            String pw = "1423";
            Connection con = DriverManager.getConnection(url, id, pw);

            return con;
        } catch (Exception e) {}
        return null;
    }
}

```

- 위와 같은 메소드 패턴 → Template Method Pattern
    - 상위 클래스에 기본적인 로직의 흐름을 구현하고 하위 클래스에서 구체적인 내용 구현
- 상속을 배제하고 UserDAO 변경부를 별도 코드(클래스)로 구현
    - 위의 경우는 추상 클래스이기 때문에 제약사항이 있음
    - 인터페이스를 이용, 느슨하게 연결
