### 승인된 대출(APPROVED)의 총 대출 금액과 해당 대출 건수를 출력하시오.

```SQL
SELECT STATUS, SUM(AMOUNT) 총대출금액,COUNT(*) 해당대출건수 FROM LOANS WHERE STATUS='APPROVED' GROUP BY STATUS;
```

### 각 월별 총 거래 금액을 계산하고, 거래 금액이 10,000 이상인 달만 출력하시오.

```SQL
SELECT TO_CHAR(TRANSACTION_DATE, 'YYYY-MM'), SUM(AMOUNT) FROM TRANSACTIONS GROUP BY TO_CHAR(TRANSACTION_DATE, 'YYYY-MM') HAVING SUM(AMOUNT)>= 10000;
```

### 각 지점에서 승인된 대출(APPROVED)의 평균 대출 금액과 총 대출 금액을 계산하시오.

```SQL
SELECT BRANCH_ID, AVG(AMOUNT) 평균대출금액, SUM(AMOUNT) 총대출금액 FROM LOANS WHERE STATUS='APPROVED' GROUP BY BRANCH_ID;
```
