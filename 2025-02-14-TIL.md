### 데이터 통신

- 데이터 통신을 하기 위해서는 주소가 필요함
- NIC(Network Interface Card)에 IP 주소가 할당됨
- 논리적인 IP 주소를 물리적인 MAC 주소로 변환하는 과정이 네트워크 통신에서 발생함 (ARP: Address Resolution Protocol 활용)

---

### IP(논리적)

- IP는 네트워크 상에서 장치를 식별하는 논리적인 주소
- IP 주소를 기반으로 패킷이 목적지까지 전달됨
- IP 주소는 계층적으로 구성됨(네트워크 부분 + 호스트 부분)
- v4
    - 32비트 주소 체계 (예: `192.168.0.1`)
- v6
    - 주소 공간이 매우 넓음 (2^128 개의 주소)
    - 멀티캐스트 개선 등 보안 및 성능 향상

---

### MAC(물리적)

- MAC(Media Access Control) 주소는 네트워크 인터페이스 카드(NIC)에 할당된 고유한 물리적 주소
- 일반적으로 48비트(6바이트) 길이를 가지며, 16진수 표기 (`00:1A:2B:3C:4D:5E`)
- 동일 네트워크 내에서 장치를 식별하는 역할 수행

---

### Port (포트)

- 네트워크에서 특정 애플리케이션(프로세스)로 데이터를 전달하기 위해 사용되는 논리적인 주소
- 하나의 프로세스에 할당되며, 같은 IP에서도 여러 개의 포트를 사용할 수 있음
- 포트 번호 범위
    - **0~1023 (Well-known Port)**: HTTP(80), HTTPS(443), FTP(21) 등
    - **1024~49151 (Registered Port)**: 특정 애플리케이션이 등록하여 사용
    - **49152~65535 (Dynamic/Private Port)**: 임시적인 통신에 사용됨

---

### 프로토콜

- 네트워크에서 데이터 교환을 위한 통신 규약
- 주요 프로토콜
    - **TCP (Transmission Control Protocol)**: 신뢰성 있는 연결 지향 프로토콜
    - **UDP (User Datagram Protocol)**: 빠르지만 신뢰성이 낮은 프로토콜
    - **IP (Internet Protocol)**: 패킷을 목적지까지 전달하는 역할

### HTTP (HyperText Transfer Protocol)

- 클라이언트-서버(CS) 구조에서 동작하는 웹 통신 프로토콜
- 요청(Request)과 응답(Response) 방식으로 동작
- 상태 정보를 유지하지 않는 비연결성(stateless) 특징

### HTTPS (HyperText Transfer Protocol Secure)

- HTTP에 SSL/TLS 보안 계층을 추가하여 암호화된 통신 제공.

---

### Web Service (웹 서비스)

웹 서비스는 정적인 웹과 동적인 웹으로 나뉨

### 1. Static Web (정적 웹)

- HTML, CSS, JavaScript 파일을 서버에서 제공
- 데이터베이스 연결 없이 클라이언트가 요청하면 동일한 파일을 제공
- 예: 단순한 랜딩 페이지, 회사 소개 웹사이트

### 2. Dynamic Web (동적 웹)

- 서버에서 클라이언트 요청에 따라 동적으로 데이터를 생성하여 제공
- 데이터베이스와 연결하여 사용자 요청에 맞는 데이터를 반환
- 예: 로그인 시스템, 게시판, 쇼핑몰 웹사이트

---

### Servlet

- Web에서 실행할 수 있는 java 프로그램(class)을 작성
- 인터페이스를 구현해서 class를 생성
- Apache(Tomcat)
    - 웹 클라이언트가 요청을 보냄
    - 웹 서버가 HTTP Req를 실행 가능한 웹 애플리케이션을 찾고(WAS) 서블릿 컨테이너에서 해당 메소드를 수행하고 Res를 넘겨줌
        - get → doGet()
        - post → doPost()

### **GET 방식**

- 브라우저의 주소창에 직접 입력방식으로 실습(Query String 이용)
- `http://127.0.0.1:8080/lab/login?loginId=aaa&pwd=bbb`
- 링크(a 태그 이용)를 이용한 인자 전달방식 실습
- `<a href = "http://127.0.0.1:8080/lab/login?loginId=aaa&pwd=bbb">이동</a>`

### **POST 방식**

- form을 이용한 인자 전달방식 실습
- textfield의 값을 받는 경우
- checkbox안에 check된 항목의 값을 받는 경우

### Servlet을 실행하려면

- http://localhost:8080
- protocol ⇒ http
- 서버 쪽 컴퓨터의 ip ⇒ localhost
- 웹 서버의 포트 번호 ⇒ 8080
- 프로젝트의 context ⇒  ?HelloServletProject_war_exploded
- 해당 프로젝트 Servlet에 대한 url mapping ⇒ /myservlet

```java
package org.example.helloservelet;

import jakarta.servlet.ServletConfig;
import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

import java.io.IOException;
import java.io.PrintWriter;

@WebServlet(name="/myservlet", value="/hello-servelet")
public class MyHello extends HttpServlet {
    // 기본 오버라이딩 메소드 조재
    private String message;
    MyHello() {

    }

    @Override
    public void init(ServletConfig config) throws ServletException {
        message="Hello Hello";
    }

    @Override
    protected void service(HttpServletRequest req, HttpServletResponse res) throws ServletException, IOException {
        super.service(req, res);
        System.out.println("service 호출");
    }

    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        PrintWriter out = resp.getWriter();
        out.println("<html><body>");
        out.println("<h1>" + message + "</h1>");
        out.println("</body></html>");
    }

    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        super.doPost(req, resp);
    }
}

```
