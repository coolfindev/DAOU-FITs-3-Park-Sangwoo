### 5-8

```c

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	//문8 초 단위의 시간을 입력 받아 몇시간 몇분 몇초인지를 계산하는 프로그램을 작성하시오

	int sec;

	printf("초단위 입력 : ");
	scanf_s("%d", &sec);

	printf("입력한 %d초는 %d시간 %d분 %d초입니다", sec, (int)sec/60/60, (int)sec/60%60, sec%60);
	return 0;
}
```

### 5-9

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	//문9 다음 수식을 계산하는 프로그램 작성하시오
	//f(x) - 2x ^ 3 – 3x ^ 2 + 1 / x

	int x;

	printf("x입력 : ");
	scanf_s("%d", &x);

	printf("x가 %d일 때 값은 %.1lf입니다", x, 2*x*x*x - 3*x*x + 1.0/x);
	return 0;
}
```

### 6-20

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    //조건 연산자를 사용해서 4개 정수의 최대값, 최소값을 출력하시오
    
    int a, b, c, d;
    printf("정수 4개를 입력해 주세요 : ");
    scanf_s("%d %d %d %d", &a, &b, &c, &d);

    int M1 = a < b ? b : a;
    int M2 = c < d ? d : c;

    int m1 = a < b ? a : b;
    int m2 = c < d ? c : d;

    printf("최대값: %d\n", M1 < M2 ? M2 : M1);
    printf("최소값: %d\n", m1 < m2 ? m1 : m2);
    return 0;
}
 
```

### 7-2

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    //키보드에서 8진수 정수와 16진수 정수를 하나씩 입력 받은 후, 이 입력 값들과 두 정수의
    //    합을 10진 정수로 출력하는 프로그램을 작성하시오
    //    8진수, 16진수를 순서대로 입력하세요 : 16 ff
    //    8진수 16 + 16진수 ff = 269

    int n1, n2;

    printf("8진수, 16진수를 순서대로 입력하세요 : ");
    scanf("%o %x", &n1, &n2);

    printf("8진수 %o + 16진수 %x = %d", n1, n2, n1 + n2);
    return 0;
}
```

### 7-4

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    //키와 몸무게를 입력 받아 키에 맞는 표준 체중을 구한 후 자신의 몸무게와 표준 체중과의 차이를 출력하는 프로그램을 작성하시오

    double hei, wei;

    printf("키를 입력하세요 ");
    scanf("%lf", &hei);

    printf("몸무게를 입력하세요 ");
    scanf("%lf", &wei);

    double standard = (hei - 100) * 0.9;
    printf("당신의 키는 %.1lfcm(%.1lffeet)입니다.당신의 체중은 %.1lfkg(%.1lflb)입니다.표준 체중 %.1lf과의 차이는 + %.1lfkg입니다.", hei, hei/(30.49), wei, wei*2.20459, standard, wei -standard );
    return 0;
}

```

### 8-9

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    //학생의 이름(이름 사이에 띄어쓰기 가능)과 국어, 영어, 수학, 출결점수를 키보드로 입력 받
    //    아 네 과목 점수의 평균(소수 1자리)을 구하고 합격 여부(평균 70점 이상이고 출결점수가 80
    //        점 이상인 경우에만 합격)을 출력하는 프로그램을 작성하시오

    char name[10];
    int kor, eng, math, att;

    printf("이름을 입력하세요 ");
    scanf("%s", name);

    printf("국어점수를 입력하세요 ");
    scanf("%d", &kor);


    printf("영어점수를 입력하세요 ");
    scanf("%d", &eng);


    printf("수학점수를 입력하세요 ");
    scanf("%d", &math);


    printf("출결점수를 입력하세요 ");
    scanf("%d", &att);
    
    double avg = (kor + eng + math + att) / 4;

    printf("%s 님 ", name);
    printf(avg >= 70 && att >= 80 ? "합격!" : "불합격");

    return 0;
}
```
