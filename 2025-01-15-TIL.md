### 금융 시스템

- 자금의 흐름을 관리하고 지원하는 시스템
- 구성 요소
    - 은행
        - 계좌 관리, 대출, 결제 서비스
    - 증권
        - 주식, 채권, 펀드 거래
    - 보험
        - 보험 가입, 계약 관리, 보상 처리
    - 결제
        - 카드 결제, 전자 화폐, 온라인 송금
- 역할
    - 자금의 원활한 흐름 지원
    - 금융 거래의 투명성과 신뢰성 확보
    - 고객과 금융 기관 간 연결
    - 경제 성장을 위한 자금 조달 및 분배

---

### ORDER BY

데이터를 정렬하는데 사용되는 절

- 오름차순(ASC), 내림차순(DESC)
- 여러 컬럼을 기준으로 정렬 가능
- 다중 컬럼의 경우 앞부터 같은 데이터를 대상으로 어떻게 정렬할 지 정할 수 있음
- Oracle에서는 NULL을 최대값으로 간주
    
    ```sql
    SELECT NAME, SALARY
    FROM EMPLOYEES
    ORDER BY SALARY ASC NULLS FIRST;
    ```
    

### 페이징 쿼리

대량의 데이터에서 일정한 크기로 데이터를 나누어 보여주는 방법

- 페이징은 성능 최적화, 사용자의 경험 개선을 위해 중요함
- ROWNUM
    - 오라클에서 결과 집합의 각 행에 부여되는 번호
- ROW_NUMBER()
    - 오라클의 윈도우 함수로, 정렬 기준에 따라 고유 번호를 부여

```sql
SELECT * FROM (
	SELECT ROW_NUMBER() OVER
		(ORDER BY A) AS row_num,
		A_list 
		FROM table_name
	)
	WHERE row_num BETWEEN :start_row AND :end_row;
```

- OFFSET-FETCH
    
    ```sql
    SELECT COLUMN_LIST
    FROM TABLE_NAME
    ORDER BY SOME_COLUMN
    OFFSET :START_ROW - 1 ROWS FETCH NEXT :PAGE_SIZE ROWS ONLY;
    
    -- :START_ROW는 조회를 시작할 행 번호
    -- FETCH NEXT :PAGE_SIZE ROW ONLY는 가져올 행 수를 지정
    ```
    
- 주의사항
    - 정렬 기준
    - 성능 고려
        - 대규모 데이터셋에서 사용 시 성능 저하 가능
        - 인덱스를 적절히 설정하여 쿼리 성능 최적화
    - 다양한 페이지 크기 테스트
        - 다양한 크기의 페이지를 고려하여 쿼리 설계

### DISTINCT

- 중복된 데이터를 제거하여 고유한 값만 반환하는 키워드
- 데이터의 요약/집계에 유용
- 여러 개의 컬럼이 나올 경우 모두 중복 기준에 적용됨

### GROUP BY

- 데이터 그룹핑
    - 데이터베이스에서 데이터를 특정 기준으로 묶는 작업
    - 집계 함수와 함께 사용하여 그룹 별 요약 정보 제공
- 집계 함수
    - COUNT
    - SUM
    - AVG
    - MAX
    - MIN
- HAVING
    - GROUP BY 이후 그룹 별 조건 필터링
    - WHERE에서 쓸 수 있는 조건을 쓸 수 있으나, 비효율적이기 때문에 WHERE에서 쓸 수 있는건 WHERE에서 쓰는 것이 좋다!

### 함수

- 데이터를 처리하거나 계산하여 결과를 반환하는 사전에 정의된 명령어
- 데이터 검색, 변환, 요약 등을 자동화하여 효율성을 높임
- 단일 값 함수, 집계 함수로 구분

- 단일 행 함수(Single-Row Functions)
    - 한 행에 대해 하나의 결과 반환
    - 문자
        - UPPER(), LOWER(), LENGTH(), SUBSTR(), INSTR(), TRIM()
        - TRIM의 진행 방식 → 한 글자씩 비교해가면서 없을 때까지 지워감
    - 숫자
        - ROUND(수, 어디까지), TRUNC(수, 어디까지), ABS(수), MOD(수, 나누는 수), CEIL(수), FLOOR(수)
    - 날짜
        - SYSDATE, CURRENT_DATE, ADD_MONTHS(달, 더할 숫자), MONTHS_BETWEEN(지금, 비교대상), NEXT_DAY(현재날짜, 바로 다음 일요일), LAST_DAY(해당 달의 마지막 날짜), EXTRACT(어디 날짜에서 뭘 뽑아올지), TRUNC(날짜 자르기)
    - 변환
    - NULL 처리
        - NVL(COL A, ‘특정 값으로’), NVL2(COL B, ‘NULL이 아닐 때 값’, ‘NULL일 때 값’)
- 집계 함수(Aggregate Functions)
    - 여러 행을 그룹으로 묶어 하나의 결과를 반환
- CASE WHEN
    - SQL에서 조건에 따라 값을 반환하는 문법
    - case when 조건1 then 결과1 …
    - case 컬럼 명 when 값 then 결과1 …
    - 복잡한 로직에 대한 처리가 용이하다
- DECODE
    - 특정 값에 따라 다른 값을 반환하는 함수
    - 간단한 매핑에 적합하다
    
    ```sql
    SELECT EMPLOYEE_ID,
    DECODE(JOB_ID,
    'ADMIN', '관리자',
    'DEV', '개발자',
    'HR', '인사담당자',
    '기타') 
    FROM EMPLOYEES;
    ```
    

### JOIN

- 여러 테이블의 데이터를 결합하여 하나의 결과 집합을 생성하는 SQL 절
- 기본적으로 두 개 이상의 테이블을 연결
- self
    - 동일 테이블 간 join
- outer
    - join 시 기준이 되는 쪽만 색칠
        - left (기준이 from)
        - right (기준이 join)
        - full (합집합)
    - 일치하지 않는 경우 null 반환
- inner
    - 교집합
- cross(카테시안 곱)
    - 모든 행의 조합 반환
    - 결과 조합의 크기가 매우 커질 수 있음
- natural
    - 동일한 컬럼명을 기준으로 join
- 유의사항
    - join 조건 누락
        - 조건이 어긋나면 cross join과 동일한 결과 발생
    - null 처리
        - outer join 시 null 값을 처리하는 로직 필요
    - 성능
        - 큰 테이블 간 join은 성능 저하 기능, 적절한 인덱스 설정 필요
