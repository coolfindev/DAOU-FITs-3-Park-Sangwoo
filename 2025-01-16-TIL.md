### SQL 실습

- 직원 이름과 소속 부서의 근무지 조회
    
    ```sql
    SELECT E.NAME "직원 이름", D.LOCATION 근무지
    FROM EMPLOYEES E
    JOIN DEPARTMENTS D
        ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
    ```
    
- 직원 이름과 해당 직원의 매니저 이름을 조회(매니저가 없는 직원도 출력)
    
    ```sql
    SELECT E1.NAME "직원 이름", E2.NAME "매니저 이름"
    FROM EMPLOYEES E1
    LEFT OUTER JOIN EMPLOYEES E2
        ON E1.MANAGER_ID = E2.EMPLOYEE_ID;
    ```
    
- 직원 이름과 급여를 조회(급여가 null인 경우 n/a)
    
    ```sql
    SELECT NAME "직원 이름", NVL(TO_CHAR(SALARY), 'N/A') 급여
    FROM EMPLOYEES;
    ```
    
- 근무지가 chicago인 직원 이름과 급여를 조회
    
    ```sql
    SELECT E.NAME, E.SALARY
    FROM EMPLOYEES E
    JOIN DEPARTMENTS D
        ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
    WHERE D.LOCATION = 'Chicago';
    ```
    
- 급여가 5,000 이상 10,000 이하인 직원의 이름과 급여, 부서 명 조회
    
    ```sql
    select e.name, e.salary, d.department_name
    from employees e
    left join departments d
    on e.department_id = d.department_id
    where e.salary between 5000 and 10000;
    ```
    

- 최근 3개월 이내 입사한 직원 이름과 입사일 조회
    
    ```sql
    SELECT NAME "직원 이름", HIRE_DATE 입사일
    FROM EMPLOYEES
    WHERE MONTHS_BETWEEN(SYSDATE, HIRE_DATE) <= 3;
    ```
    

- 각 부서의 부서장 이름과 부서 이름을 조회
    
    ```sql
    SELECT E.NAME "부서장 이름", D.DEPARTMENT_NAME "부서 이름"
    FROM DEPARTMENTS D
    LEFT OUTER JOIN EMPLOYEES E
        ON D.MANAGER_ID = E.EMPLOYEE_ID;
    ```
    

- 직무 별 직원 수 조회
    
    ```sql
    select count(*)
    from employees
    group by job_id;
    ```
    

- 부서 별 평균 급여
    
    ```sql
    SELECT
        DEPARTMENT_NAME,
        ROUND(AVG(SALARY)) AS AVG_SALARY
    FROM (SELECT *
        FROM DEPARTMENTS D 
        INNER JOIN EMPLOYEES E
            ON D.DEPARTMENT_ID = E.DEPARTMENT_ID
    )
    GROUP BY DEPARTMENT_NAME
    ;
    ```
    

- 직원의 총 급여 합계
    
    ```sql
    SELECT SUM(SALARY)
    FROM EMPLOYEES;
    ```
    

- 대출 정보와 고객 이름 조회
    
    ```sql
    SELECT l.AMOUNT, c.NAME
    FROM LOANS l
    LEFT JOIN CUSTOMERS c
    ON l.CUSTOMER_ID = c.CUSTOMER_ID
    ORDER BY l.AMOUNT DESC;
    ```
    
- 대출 상태 별 고객 수
    
    ```sql
    SELECT STATUS 대출상태, COUNT(DISTINCT CUSTOMER_ID) 고객수
    FROM LOANS
    GROUP BY STATUS;
    ```
    
- 직원이 가장 적은 부서
    
    ```sql
    SELECT DEPARTMENT_ID, CNT FROM (
    SELECT ROW_NUMBER() OVER(ORDER BY COUNT(*)) AS row_num, DEPARTMENT_ID, COUNT(*) CNT
    FROM EMPLOYEES
    WHERE DEPARTMENT_ID IS NOT NULL
    GROUP BY DEPARTMENT_ID
    ) WHERE row_num = 1;
    ```
    
- 월 별 신규 고객 수
    
    ```sql
    SELECT SUBSTR(TRUNC(CREATED_AT, 'MM'),1,5) AS CREATED_MONTH, COUNT(*)
    FROM CUSTOMERS
    GROUP BY TRUNC(CREATED_AT, 'MM')
    ;
    ```
    
- 상품 카테고리 계층 구조 조회
    
    ```sql
    SELECT
        S1.CATEGORY_NAME,
        S2.CATEGORY_NAME AS PARENT_CATEGORY 
    FROM SHOPPING_CATEGORIES S1
    LEFT OUTER JOIN SHOPPING_CATEGORIES S2
        ON S1.PARENT_CATEGORY_ID = S2.CATEGORY_ID
    ;
    ```
    
- 특정 지점의 대출 합계
    
    ```sql
    SELECT B.NAME 지점명, FLOOR(SUM(L.AMOUNT)) 지점대출총합
    FROM LOANS L
    LEFT JOIN BRANCHES B
    ON L.BRANCH_ID = B.BRANCH_ID
    WHERE B.NAME LIKE 'Branch 8';
    ```
    
- 직무 별 직원의 평균 급여와 급여 등급 조회
    
    ```sql
    SELECT JOB_ID 직무, AVG(SALARY) 평균급여,
    CASE 
        WHEN AVG(SALARY)>= 8000 THEN 'HIGH'
        WHEN AVG(SALARY)>= 6000 THEN 'MEDIUM'
        ELSE 'LOW'
    END 수준
    FROM EMPLOYEES
    GROUP BY JOB_ID
    HAVING AVG(SALARY) >= 5000
    ORDER BY 평균급여 DESC;
    ```
    
- 고객 별 거래 내역
    
    ```sql
    SELECT NAME "고객 이름", SUM(T.AMOUNT) "거래 내역(금액)" 
    FROM CUSTOMERS C
    JOIN ACCOUNTS A
        ON C.CUSTOMER_ID = A.CUSTOMER_ID
    JOIN TRANSACTIONS T
        ON A.ACCOUNT_ID = T.ACCOUNT_ID
    GROUP BY C.NAME
    ORDER BY TO_NUMBER(SUBSTR(C.NAME, 10));
    ```
    
- 부서 별 직원의 평균 입사일 차이
    
    ```sql
    SELECT D.DEPARTMENT_NAME D_NAME, ROUND(AVG(J.END_DATE - J.START_DATE),2)
    FROM EMPLOYEES E
    JOIN DEPARTMENTS D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
    JOIN JOB_HISTORY J
    ON J.DEPARTMENT_ID = D.DEPARTMENT_ID
    GROUP BY D.DEPARTMENT_NAME 
    ORDER BY TO_NUMBER(SUBSTR(DEPARTMENT_NAME,12));
    ```
    
- 대출 금액 상위 5명 고객 조회
    
    ```sql
    SELECT C.NAME, T.TOTAL_LOAN
    FROM (
        SELECT C.CUSTOMER_ID, SUM(L.AMOUNT) AS TOTAL_LOAN
        FROM CUSTOMERS C
        LEFT JOIN LOANS L
            ON C.CUSTOMER_ID = L.CUSTOMER_ID
        GROUP BY C.CUSTOMER_ID
        ORDER BY SUM(L.AMOUNT) DESC NULLS LAST) T
    JOIN CUSTOMERS C
        ON C.CUSTOMER_ID = T.CUSTOMER_ID
    WHERE ROWNUM <=5
    ;
    ```
    

### 실습 정리
  - ‘모두’ 라는 키워드가 나올 때는 CROSS JOIN 생각
  - 일반적인 숫자를 정렬 할 때 1, 10, 100, … 순으로 정렬되는데 이를 1, 2, 3과 같이 표현하고 싶으면
      - SUBSTR()로 해당 문자(숫자)를 추출한 뒤, TO_NUMBER()로 ORDER BY에 넣어주면 해결됨
  - CASE-WHEN-THEN 구문에서 NULL을 걸러내고 싶을 때에는 외곽에서 WHERE/HAVING으로 처리하자
  - 날짜 차이를 구하기 위해서는 단순히 DATE-DATE의 산술 연산도 가능하다
  - INNER JOIN이 가능한 경우 LEFT OUTER JOIN보다 효율적이므로 전자의 경우로 해결하자
  - 페이징 쿼리에서는 서브쿼리(ROW_NUMBER(), ROWNUM)를 활용해서 추출하자
  - 중복된 데이터가 존재할 수 있다는 것을 인지하고 이를 DISTINCT로 해결할 수 있음을 기억하자
