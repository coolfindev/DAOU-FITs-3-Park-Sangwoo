# TIL
**다우기술 FITs 3기 박상우**

---

### 알고리즘

어떤 문제를 해결하기 위한 일련의 절차

- 0개 이상의 입력, 1개 이상의 출력이 있어야 한다
- 반드시 종료되어야 한다
- 명령이 실행 가능해야 한다


### 시간복잡도

알고리즘이 얼마나 효율적으로 동작하는지 평가할 수 있는 척도

입력 크기(n)에 대해 실행되는 시간을 수학적으로 표현

- 점근적 표기법
    - 오메가 표기법
        - 최선의 경우 시간복잡도
    - 세타 표기법
        - 평균적인 경우 시간복잡도
    - 빅오 표기법
        - 일반적으로 시간복잡도라고 하면 n의 모든 입력에 대해 걸리는 최대의 시간을 말함
        - 최악의 경우 시간복잡도

- 문제단계에 따른 시간복잡도
    
    O(1)→O(logn)→O(n)→O(nlogn)→O(n^2)→O(C^n)
    


### 자료구조

프로그램에서 쉽게 이용할 수 있도록 구성된 데이터 간 논리적인 관계

- 종류
    - 배열
    - 연결 리스트
        - 메모리 상에서 요소들이 연속적이지 않고 각각의 노드는 동적으로 할당되어 연결
    - 스택
        - LIFO
    - 큐
        - FIFO
        - 우선순위 큐
        
        각 요소가 우선순위를 가지며 큐에서 데이터를 꺼낼 때 우선순위가 높은(최소, 최대) 요소부터 꺼내는 자료구조
        
        - 파이썬의 heapq는 최소 힙이 기본이다.
    - 그래프
        - 정점, 간선
        - 차수
        
        정점에 접한 간선의 수
        
        - 행렬
            - 정점과 정점의 관계를 나타내는 인접 행렬
            - 정점과 간선의 관계를 나타내는 근접 행렬
    - 트리
        - 레벨, 깊이
        - 이진 트리
            - 이진 트리 - 완전 이진 트리 - 포화 이진 트리
            - 순회
                - 전위 순회(루-왼-오)
                - 중위 순회(왼-루-오)
                - 후위 순회(왼-오-루)
        - 힙
            - 완전 이진 트리 기반 자료구조



### 탐색

- 깊이 우선 탐색(DFS, Depth-First Search)
    - 시작 정점에서 시작, 정점과 연결된 방문하지 않은 한 정점을 방문하고, 다시 연결된 방문하지 않은 한 정점을 방문하는 식으로 진행됨
- 너비 우선 탐색(BFS, Breadth-First Search)
    - 시작 정점을 먼저 방문하고, 시작 정점과 연결된 모든 정점을 방문하는 식으로 진행됨
- 이진 탐색 트리(BST, Binary Search Tree)
    - 이진 트리 중에서 같은 데이터를 갖는 노드가 없으며, 왼쪽 서브 트리에 있는 모든 데이터가  현재 데이터보다 작고, 오른쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 큰 트리
    - ABL트리
        
        균형 이진 탐색 트리로 모든 노드의 균형 인수가 -1, 0, 1 사이이며, 삽입이나 삭제 시 균형을 맞추기 위해 회전 연산을 수행함
        
    - 레드-블랙 트리(Red-Black Tree)
        
        이진 탐색 트리의 종류로 각 노드에 색을 부여하여 트리의 균형을 유지하는 자가 균형 이진 탐색 트리임. AVL 트리보다 구현이 더 간단함
        



### 정렬

- 선택 정렬(Selection Sort)
    - 가장 작은 데이터를 찾아 가장 앞의 데이터와 교환하는 정렬 방식

```python
def selection(ds):
	for a in range(0, len(ds)-1):
		min_ind = a
		for b in range(a+1, len(ds)):
			if ds[b] < ds[min_ind]:
				min_ind = b
			ds[a], ds[min_ind] = ds[min_ind], ds[a]
```

- 삽입 정렬(Insertion Sort)
    - 정렬되지 않은 데이터를 정렬된 앞의 데이터의 적절한 위치에 삽입해가는 정렬 방식

```python
def insertion(ds):
    for a in range(1, len(ds)):
        key = ds[a]
        b = a-1
        while b>=0 and ds[b]>key:
            ds[b+1] = ds[b]
            b = b-1
        ds[b+1] = key
```

- 버블 정렬(Bubble Sort)
    - 서로 이웃한 데이터를 비교해 가장 큰 데이터를 뒤로 보내며 정렬하는 방식

```python
def bubble(ds):
    for a in range(0, len(ds)-1):
        for b in range(0, len(ds)-1-a):
            if ds[b] > ds[b+1]:
                ds[b], ds[b+1] = ds[b+1], ds[b]
```

- 퀵 정렬(Quick Sort)
    - 기준 키(pivot)를 중심으로 작거나 같은 값은 앞으로, 큰 값은 뒤로 가도록 하여 정렬하는 방식

```python
def quick(ds):
    if len(ds) < 2:
        return ds
    else:
        key = ds[0]
        left = [data for data in ds[1:] if data <= key]
        right = [data for data in ds[1:] if data > key]
        return quick(left) + [key] + quick(right)
```

- 병합 정렬(Merge Sort)
    - 배열을 반으로 나눠가며 정렬한 뒤, 이를 병합(merge)하여 최종적으로 정렬된 배열을 만드는 방식
- 힙 정렬(Heap Sort)
    - 힙 자료구조를 사용하여 마지막 노드를 루트 노드와 교환하며 정렬을 수행
- 이진 탐색(Binary Search)
    - 정렬된 데이터 집합을 반으로 쪼개 가면서 탐색하는 탐색 방법

```python
def binary(ds, key):
    low = 0
    high = len(ds)-1
    while low <= high:
        mid = (low+high)//2
        if key == ds[mid]:
            return mid
        elif key < ds[mid]:
            high = mid-1
        else:
            low = mid+1
    return
```



### 해시/재귀

- 해시 테이블(Hash Table)
    
    키를 사용하여 데이터를 빠르게 검색할 수 있도록 설계된 자료구조
    
    - 충돌
        - 체이닝
        - 개방 주소법
- 재귀 알고리즘
    
    자기 자신을 호출하는 방식으로 문제를 해결하는 알고리즘
    


### 탐욕 기법

매 순간 최적의 선택을 하는 방법으로 문제를 해결하는 알고리즘



### 최소 신장 트리(MST, Minimum Spanning Tree)

주어진 그래프에서 모든 노드를 연결할 수 있는 트리 구조를 찾고, 그 트리의 간선들의 가중치 합이 최소가 되도록 만드는 문제

- 크루스칼(Kruskal) 알고리즘
    
    그래프 간선들을 가중치가 작은 순서대로 선택하여 정렬하는 것
    
    - 사이클이 발생해서는 안됨
- 프림(Prim) 알고리즘
    
    임의 정점에서 시작하여 연결된 간선 중 가중치가 가장 작은 간선을 선택하고, 선택된 간선에 연결된 정점을 확장해 가면서 최소 신장 트리를 만듦
    
- 다익스트라(Dijkstra’s Algorithm)
    
    단일 출발지 최단 경로 문제를 해결하는 알고리즘
    



### 동적 프로그래밍(DP, Dynamic Programming)

복잡한 문제를 작은 문제로 나누어 해결하고, 그 결과를 저장하여 중복 계산을 방지하는 알고리즘 설계 기법

- 메모이제이션(Memoization)
    - 탑다운 방식
    - 재귀를 사용하여 문제를 풀고, 중간 계산 결과를 저장하여 중복 계산을 방지함
- 타뷸레이션(Tabulation)
    - 바텀업 방식
    - 반복문을 활용해 작은 부분 문제부터 차례대로 풀어 해결
