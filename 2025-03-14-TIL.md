## 구조체

- 서로 다른 형태의 데이터를 여러 개 모아서 하나의 자료구조로 표현
- 구조체로 만들어진 자료는 **연속된 메모리**에 할당됨

**선언 형식**

```c
struct 구조체이름 {
    자료형 식별자1;
    자료형 식별자2;
    ...
};

//사용
struct 구조체이름 구조체변수

or

struct 구조체이름 {
    자료형 식별자1;
    자료형 식별자2;
    ...
} 구조체변수1, 구조체변수2;
```

**typedef**

- **타입 정의(type definition)**를 위한 C 언어의 키워드
- 기존의 타입에 대해 새로운 이름을 부여하는 역할
- 이를 통해 코드의 가독성을 높이고, 코드에서 자주 사용하는 타입을 더 간결하고 의미 있게 표현할 수 있음
    - 모든 자료형을 사용자가 지정하는 자료형으로 가독성과 이식성 향상
    
    ```c
    typedef unsigned char BYTE;
    typedef struct student STU;
    
    typedef struct st_score {
        int id;
        int kor;
        double avg;
    } SCO;
    //typedef를 이용하여 구조체 전체를 SCO형으로 정의
    SCO student1, student2
    ```
    

- 구조체 문제 1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    typedef struct {
    	char name[20];
    	int studentID;
    	int score[3];
    	double average;
    } Student;
    
    int main() {
    //	1. 학생의 이름, 학번, 세과목 점수를 입력받고, 각 학생의 평균 점수를 계산하세요
    //	
    	Student students[2];
    
    	for (int i = 0; i < 2; i++) {
    		printf("학생 %d의 정보 입력 :\n", i + 1);
    		printf("이름 : ");
    		scanf("%s", students[i].name);
    		printf("학번 : ");
    		scanf("%d", &students[i].studentID);
    		for (int j = 0; j < 3; j++) {
    			printf("과목 %d 성적 : ", j + 1);
    			scanf("%d", &students[i].score[j]);
    		}
    		students[i].average = (students[i].score[0] + students[i].score[1] + students[i].score[2]) / 3.0;
    	}
    
    	printf("학생 성적 정보 :\n");
    	for (int i = 0; i < 2; i++) {
    		printf("이름 : %s, 학번 : %d, 평균 : %.2lf\n", students[i].name, students[i].studentID, students[i].average);
    	}
    	return 0;
    }
    
    ```
    
- 구조체 문제 2
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    struct Book {
    	char title[100];
    	char author[100];
    	int year;
    	char genre[100];
    };
    
    int main() {
    //	2. 도서관리 시스템을 구조체로 만들어 보세요.요구사항
    //		여러 도서의 제목, 저자, 출판년도, 장르 정보를 입력받고, 특정 장르의 책들만하시오.
    	struct Book books[100];
    	int n;
    	printf("도서의 개수를 입력하세요 : ");
    	scanf("%d", &n);
    
    	for (int i = 0; i < n; i++) {
    		printf("도서 %d의 정보 입력 :\n", i + 1);
    		printf("제목 : ");
    		scanf("%s", books[i].title);
    		printf("저자 : ");
    		scanf("%s", books[i].author);
    		printf("출판년도 : ");
    		scanf("%d", &books[i].year);
    		printf("장르 : ");
    		scanf("%s", books[i].genre);
    	}
    
    	char genre[100];
    	printf("장르를 입력하세요 : ");
    	scanf("%s", genre);
    
    	printf("'%s' 장르의 책들 :\n", genre);
    	for (int i = 0; i < n; i++) {
    		if (strcmp(books[i].genre, genre) == 0) {
    			printf("제목 : %s, 저자 : %s, 출판년도 : %d\n", books[i].title, books[i].author, books[i].year);
    		}
    	}
    
    	return 0;
    }
    
    ```
    
- 실습과제 28
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    struct game {
    	char name[7];
    	int R1, R2, R3;
    };
    
    int main() {
    	struct game player;
    	double avg;
    
    	printf("선수의 이름은? ");
    	scanf("%s", player.name);
    
    	printf("1, 2, 3라운드 점수는? ");
    	scanf("%d %d %d", &player.R1, &player.R2, &player.R3);
    
    	avg = (double)(player.R1 + player.R2 + player.R3) / 3;
    	printf("%s선수의 게임 결과 평균 %.1lf점", player.name, avg);
    	return 0;
    }
    
    ```
    
- 실습과제 29
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    int main() {
    	struct student {
    		char name[20];
    		char sex;
    		int stid;
    		int sub1;
    		int sub2;
    		int sub3;
    		double avg;
    	} st1 = { "kdhong", 'm', 1508001, 98, 89, 92, 0.0 }, st2;
    	st1.avg = (st1.sub1 + st1.sub2 + st1.sub3) / 3.0;
    	st2 = st1;
    	strcpy(st2.name, "yhkim"); //문자열같은 경우 strcpy 
    	st2.sex = 'f'; //나머지 원시타입의 경우 깡대입
    	st2.stid = 1608024;
    
    	printf("%s님(성별 : %s)의 학번은 %d이고,\n이번 시험 3과목의 평균은 %5.2lf입니다\n", st2.name, st2.sex == 'm' ? "남자" : "여자", st2.stid, st2.avg);
    	return 0;
    }
    
    ```
