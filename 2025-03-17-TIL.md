### 구조체 응용

- 구조체가 함수의 인수로 사용될 때 구조체 포인터, 구조체 배열, 구조체 리스트 등으로 활용된다.
- 함수와 구조체
    - 함수의 인수로 구조체를 전달할 수 있음
    - 함수의 인수로 구조체를 사용하려면 구조체를 전역 변수 선언 영역에 선언해야 함
    - 함수의 인수로 구조체가 전달되면 구조체의 모든 멤버가 복사되어 함수로 전달됨
    - 구조체는 하나의 형으로 취급되기 때문에 주소에 의한 호출이 아니라 값에 의한 호출로 처리됨
- 구조체 포인터
    - 함수의 매개변수로 구조체 주소를 이용한 구조체 포인터를 사용 가능
    - 구조체의 주소만 넘겨주면 되므로 멤버 복사에 시간이 걸리지 않음
- 일반 구조체, 구조체 포인터를 사용할때는 아래와 같은 차이
    - 일반 구조체의 멤버 접근 연산자 `.`
    - 구조체 포인터를 사용한 멤버의 접근에는 `->`

### 문제 풀이

- 실습과제 30
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    typedef struct student {
    
    	char name[20];
    	char sex;
    	int stid;
    	int	sub1, sub2, sub3;
    	double avg;
    } ST;
    
    int input(ST st);
    int main() {
    
    	ST st1 = { "kdhong", "m", 1508001, 0,0,0,0.0 };
    	input(st1);
    
    	printf("main()함수에서 출력된 세 과목의 점수는 %d, %d, %d입니다.\n", st1.sub1, st1.sub2, st1.sub3);
    
    	return 0;
    }
    
    int input(ST st) {
    	printf("%s 학생의 3과목 성적을 입력하세요(공란으로 구분) : ", st.name);
    	scanf("%d %d %d", &st.sub1, &st.sub2, &st.sub3);
    	printf("input()함수에서 출력된 세 과목의 점수는 %d, %d, %d입니다.\n", st.sub1, st.sub2, st.sub3);
    }
    ```
    
- 실습과제 31
    
    ```c
    **#define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    typedef struct student {
    
    	char name[20];
    	char sex;
    	int stid;
    	int	sub1, sub2, sub3;
    	double avg;
    } ST;
    
    int input(ST* st);
    int main() {
    
    	{
    	ST st1[3] = { { "kdhong", "m", 1508001, 0,0,0,0.0 } , { "kdhong", "m", 1508001, 0,0,2,0.0 } , { "kdhong", "m", 1508001, 0,0,0,0.1 } };
    	for (int i = 0; i < 3; i++) {
    		input(&st1[i]);
    		printf("main()함수에서 출력된 세 과목의 점수는 %d, %d, %d입니다.\n", st1[i].sub1, st1[i].sub2, st1[i].sub3);
    	}
    	}
    	return 0;
    }
    
    int input(ST * st) {
    	printf("%s 학생의 3과목 성적을 입력하세요(공란으로 구분) : ", st->name);
    	scanf("%d %d %d", &st->sub1, &st->sub2, &st->sub3);
    	printf("input()함수에서 출력된 세 과목의 점수는 %d, %d, %d입니다.\n", st->sub1, st->sub2, st->sub3);
    }**
    ```
    
- 응용문제 1
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    
    typedef struct {
    	char name[20];
    	int age;
    	int id;
    	double balance;
    } Customer;
    
    typedef struct {
    	int accountNumber;
    	double balance;
    } Account;
    
    int main() {
    //	1. 고객의 정보를 저장하고, 고객의 계좌를 추가, 삭제할 수 있는 고객 정보 관리 시스템을 구현하세요
    
    		Customer customer = { "홍길동", 20, 1234, 0 };
    		Account account[10];
    
    		int menu;
    		int accountNumber;
    		double balance;
    		int i = 0; // 계좌 수
    		int j = 0;
    
    		while (1) {
    			printf("1. 계좌 추가\n");
    			printf("2. 계좌 삭제\n");
    			printf("3. 고객 정보 보기\n");
    			printf("4. 종료\n");
    			printf("원하는 작업을 선택하세요 : ");
    			scanf("%d", &menu);
    			if (menu == 1) {
    				printf("추가할 계좌 번호를 입력하세요 : ");
    				scanf("%d", &accountNumber);
    				printf("계좌의 초기 잔액을 입력하세요 : ");
    				scanf("%lf", &balance);
    				account[i].accountNumber = accountNumber;
    				account[i].balance = balance;
    				i++;
    			}
    			else if (menu == 2) {
    				int deleteAccount;
    				printf("삭제할 계좌 번호를 입력하세요 : ");
    				scanf("%d", &deleteAccount);
    				for (j = 0; j < i; j++) {
    					if (account[j].accountNumber == deleteAccount) {
    						for (int k = j; k < i; k++) {
    							account[k] = account[k + 1];
    						}
    						i--;
    					}
    				}
    			}
    			else if (menu == 3) {
    				printf("고객 : %s\n", customer.name);
    				printf("계좌 수 : %d\n", i);
    				for (j = 0; j < i; j++) {
    					printf("계좌 %d : 잔액 %.2lf\n", account[j].accountNumber, account[j].balance);
    				}
    			}
    			else if (menu == 4) {
    				break;
    			}
    		}
    		return 0;
    }
    
    ```
    
- 응용문제 2
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    
    #define MAX_INVESTMENTS 100
    
    typedef struct {
        char name[20];
        double investment_amount;
        double return_rate;
    } Investment;
    
    typedef struct {
        char name[20];
        int num_investments;
        Investment investments[MAX_INVESTMENTS];
    } Customer;
    
    void add_investment(Customer* customer, char* name, double investment_amount, double return_rate) {
        strcpy(customer->investments[customer->num_investments].name, name);
        customer->investments[customer->num_investments].investment_amount = investment_amount;
        customer->investments[customer->num_investments].return_rate = return_rate;
        customer->num_investments++;
        printf("%s 투자 상품의 수익 : %.2f\n", name, investment_amount * return_rate);
    }
    
    double get_return(Customer* customer, char* name) {
        for (int i = 0; i < customer->num_investments; i++) {
            if (strcmp(customer->investments[i].name, name) == 0) {
                return customer->investments[i].investment_amount * customer->investments[i].return_rate;
            }
        }
        return -1;
    }
    
    int main() {
        // 고객의 투자 포트폴리오를 관리하는 시스템을 구현하시오.
        // 각 투자 상품은 종목 이름, 투자 금액, 수익률 등을 포함합니다.
        // 고객은 여러 개의 투자 상품을 가질 수 있습니다.
        // 각 투자 상품에 대한 수익을 계산하는 함수를 구현해야 하며,
        // 수익은 투자 금액에 수익률을 곱한 값으로 계산합니다.
    
        Customer customer;
        customer.num_investments = 0;
    
        char name[20];
        int num_investments;
        printf("고객 이름은 ? ");
        scanf("%s", customer.name);
        printf("투자 상품 개수는 ? ");
        scanf("%d", &num_investments);
    
        if (num_investments > MAX_INVESTMENTS) {
            printf("최대 투자 상품 개수는 %d개입니다.\n", MAX_INVESTMENTS);
            return 1;
        }
    
        for (int i = 0; i < num_investments; i++) {
            double investment_amount, return_rate;
            printf("투자 상품 %d을 입력하세요(회사명, 투자금액, 수익률) : ", i + 1);
            scanf("%s %lf %lf", name, &investment_amount, &return_rate);
            add_investment(&customer, name, investment_amount, return_rate/100);
        }
    
        printf("각 투자 상품의 수익 : \n");
        for (int i = 0; i < num_investments; i++) {
            printf("%s 투자 상품의 수익 : %.2f\n", customer.investments[i].name, get_return(&customer, customer.investments[i].name));
        }
    
        return 0;
    }
    
    ```
    
- 응용문제 3
    - 만기일 기준이지만 구조체 필드에는 만기일이 저장되어있지 않기 때문에 내부에서 임시 구조체 인스턴스를 만들어 해결
    - 만기 확인은 현재 시각을 `localtime()`을 활용하여 해결
    - 구조체 포인터를 활용할 때에는 매개변수가 배열로 넘어오는지, 단일 인스턴스로 넘어오는지에 따라 구조체 포인터를 사용하는지 멤버를 인덱스를 통해 확인하는지 달라짐
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    
    #define MAX_DEPOSITS 5
    
    typedef struct {
        char name[30];
        char account_number[10];
        struct tm start_date;
        int duration;
        double amount;
    } FixedDeposit;
    
    void sort_by_maturity_date(FixedDeposit* fds, int n) {
    	for (int i = 0; i < n - 1; i++) {
    		for (int j = 0; j < n - i - 1; j++) {
    			struct tm maturity_date1 = fds[j].start_date;
    			maturity_date1.tm_mon += fds[j].duration;
    			mktime(&maturity_date1); // 날짜 계산을 위해 mktime 호출
    			struct tm maturity_date2 = fds[j + 1].start_date;
    			maturity_date2.tm_mon += fds[j + 1].duration;
    			mktime(&maturity_date2); // 날짜 계산을 위해 mktime 호출
    
    			if (mktime(&maturity_date1) > mktime(&maturity_date2)) {
    				FixedDeposit temp = fds[j];
    				fds[j] = fds[j + 1];
    				fds[j + 1] = temp;
    			}
    		}
    	}
    }
    
    int check_maturity(FixedDeposit* fd) {
        struct tm maturity_date = fd->start_date;
        maturity_date.tm_mon += fd->duration;
        mktime(&maturity_date); // 날짜 계산을 위해 mktime 호출
        time_t now = time(NULL);
        struct tm* now_tm = localtime(&now);
        return mktime(now_tm) >= mktime(&maturity_date);
    }
    
    void display_deposits(FixedDeposit* fds, int n) {
        printf("[ 정기 예금 목록 (만기일 빠른 순 ) ]\n");
        for (int i = 0; i < n; i++) {
            struct tm maturity_date = fds[i].start_date;
            maturity_date.tm_mon += fds[i].duration;
            mktime(&maturity_date); // 날짜 계산을 위해 mktime 호출
    
            printf("%d. %s | 계좌: %s | 예금 시작: %d-%02d-%02d | 만기: %d-%02d-%02d | 금액: %.2lf원 | 만기 %s ? | \n",
                i + 1, fds[i].name, fds[i].account_number,
                fds[i].start_date.tm_year + 1900, fds[i].start_date.tm_mon + 1, fds[i].start_date.tm_mday,
                maturity_date.tm_year + 1900, maturity_date.tm_mon + 1, maturity_date.tm_mday,
    			fds[i].amount, check_maturity(&fds[i]) ? "도래" : "대기");
        }
    }
    
    int main() {
        FixedDeposit deposits[MAX_DEPOSITS] = {
            {"김철수", "1234567890", {0, 0, 0, 10, 0, 124}, 12, 5000000},
            {"이영희", "9876543210", {0, 0, 0, 1, 6, 123}, 24, 3000000},
            {"박민수", "5678901234", {0, 0, 0, 15, 2, 123}, 18, 2000000},
            {"조수빈", "3456789012", {0, 0, 0, 5, 8, 123}, 6, 1000000},
            {"정하나", "6789012345", {0, 0, 0, 20, 10, 123}, 12, 7000000}
        };
    
        sort_by_maturity_date(deposits, MAX_DEPOSITS);
        display_deposits(deposits, MAX_DEPOSITS);
    
        return 0;
    }
    
    ```
    
- 응용문제 4
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <time.h>
    #include <string.h>
    #include <math.h>
    #define STOCKS 3
    
    typedef struct Stock {
    	char name[20];
    	char ticker[20];
    	int stock_num;
    	double close[30];
    	double average;
    	double volatility;
    } Stock;
    
    void calculate_average(Stock* stock) {
    	double sum = 0;
    	for (int i = 0; i < 30; i++) {
    		sum += stock->close[i];
    	}
    	stock->average = sum / 30;
    }
    
    void calculate_volatility(Stock* stock) {
    	double sum = 0;
    	for (int i = 0; i < 30; i++) {
    		sum += (stock->close[i] - stock->average) * (stock->close[i] - stock->average);
    	}
    	stock->volatility = sqrt(sum / 30);
    }
    
    void categorize_risk(Stock* stock) {
    	if (stock->volatility <= 5) {
    		printf("낮음\n");
    	}
    	else if (stock->volatility <= 10) {
    		printf("보통\n");
    	}
    	else {
    		printf("높음\n");
    	}
    }
    
    int main() {
        //4. 주식 포트폴리오 위험 분석 시스템을 구축하시오
        //    관리자는 고객이 보유한 주식 포트폴리오의 변동성을 분석하여 위험 지수를 평가해야 합니다.각 주식 종목의 과거 30일 종가 데이터를 사용하여 변동성(표준편차를 계산한 후, 위험 수준
        //        을 낮음, 보통, 높음으로 평가하는 프로그램을 작성하세요.
    	Stock stocks[STOCKS] = {
    	 {"삼성전자", "005930", 100, {65400, 65200, 65000, 64800, 65500, 66000, 66200, 66100,
    	66300, 65900, 65550, 65800, 66050, 66350, 66700, 66800, 67000, 66900, 67200, 67300,
    	67100, 67050, 67250, 67400, 67600, 67500, 67450, 67650, 67800, 67900}},
    	 {"테슬라", "TSLA", 50, {210, 215, 220, 218, 222, 225, 230, 228, 232, 235, 238, 240,
    	245, 250, 248, 246, 244, 241, 238, 235, 233, 231, 228, 226, 224, 223, 220, 218, 215, 212}},
    	 {"애플", "AAPL", 80, {180, 182, 184, 183, 185, 186, 188, 187, 189, 191, 193, 195, 198,
    	200, 202, 203, 205, 207, 209, 210, 212, 213, 214, 215, 217, 218, 219, 220, 221, 222}}
    	};
    
    	printf("[ 고객 주식 포트폴리오 위험 분석 ]\n");
    	for (int i = 0; i < STOCKS; i++) {
    		calculate_average(&stocks[i]);
    		calculate_volatility(&stocks[i]);
    		printf("종목 : %s (%s) | 보유 주식 : %d | 변동성 %.2lf | 위험 수준 : ", stocks[i].name, stocks[i].ticker, stocks[i].stock_num, stocks[i].volatility);
    		categorize_risk(&stocks[i]);
    	}
        return 0;
    }
    
    ```
    
- 실습문제 34
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <time.h>
    
    int main() {
        time_t now;
        struct tm t;
    
        time(&now);
        printf("1970년 1월 1일부터 현재까지의 초는 %d초 입니다 \n", now);
        t = *localtime(&now);
        puts(asctime(&t));
    
        printf("현재의 연도 : %d\n", t.tm_year);
        printf("현재의 월 : %d\n", t.tm_mon);
        printf("현재의 일 : %d\n", t.tm_mday);
        printf("현재의 요일 : %d\n", t.tm_wday);
    
        return 0;
    }
    
    ```
    
- 실습문제 36
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <time.h>
    
    int main() {
        union year {
            int year;
            int grade;
        };
    
        typedef struct student {
            char name[20];
            char sex;
            int stid;
            union year y;
        } ST;
    
        ST st1 = { "kdhong", 'm', 1508001 };
        st1.y.grade = 4;
        printf("%s 학생은 %d학년입니다.\n", st1.name, st1.y);
        printf("%s 학생은 %d학년입니다.\n", st1.name, st1.y.grade);
        printf("%s 학생은 %d학년입니다.\n", st1.name, st1.y.year);
        st1.y.year = 1;
        printf("%s 학생은 %d학년입니다.\n", st1.name, st1.y);
        printf("%s 학생은 %d학년입니다.\n", st1.name, st1.y.grade);
        printf("%s 학생은 %d학년입니다.\n", st1.name, st1.y.year);
        return 0;
    }
    
    ```
    
- 실습문제 37
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <time.h>
    #include <string.h>
    #include <math.h>
    
    #define MAX 100
    #define MIN 0
    int main() {
    
    	typedef enum week { SUN, MON, TUE, WED, THU, FRI, SAT } WEEK;
    	typedef enum familiy_name {KIM = 100, LEE, PARK ,JUNG ,HONG} F_name;
    
    	F_name fn = JUNG;
    
    	printf("MAX = %d, MIN = %d\n", MAX, MIN);
    	printf("SUN = %d\n", SUN);
    	printf("MON = %d\n", MON);
    	printf("TUE = %d\n", TUE);
    	printf("WED = %d\n", WED);
    	printf("THU = %d\n", THU);
    	printf("FRI = %d\n", FRI);
    	printf("SAT = %d\n", SAT);
    	printf("JUNG은 %d번째 입니다", fn);
    	return 0;
    }
    
    ```
    

### 구조체 vs 공용체 vs 열거형 개요

- 구조체 (struct)
구조체는 여러 서로 다른 타입의 데이터를 하나의 단위로 묶을 수 있습니다. 구조체 내의
각 멤버는 서로 독립적으로 메모리 공간을 차지하므로, 여러 데이터 항목을 묶어 관리하고자
할 때 유용합니다.
- 공용체 (union)
공용체는 같은 메모리 공간을 공유하는 멤버들을 갖습니다. 즉, 여러 멤버 중에서 하나의
값만 유효하며, 메모리 공간을 절약할 수 있지만 동시에 값을 한 번에 하나만 저장할 수 있습
니다. 메모리 절약이 중요한 경우 사용됩니다.
- 열거형 (enum)
열거형은 관련된 상수 값들을 의미 있는 이름으로 정의하는 데 사용됩니다. 열거형을 사용
하면 코드에서 숫자 대신 의미 있는 이름을 사용할 수 있어 가독성이 높아집니다. 주로 상태
코드나, 설정 값 등을 정의할 때 유용합니다.
