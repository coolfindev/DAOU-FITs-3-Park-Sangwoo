### 문자열 문제

- strlen()
    - <string.h> strlen() 문자열의 길이
- tolower()
    - <ctype.h> tolower() 소문자, toupper() 대문자,
- itoa()
    - int to alpha _itoa( 값, 문자배열명, 변환할 진법)
- strncpy()
    - strncpy( s1, s2, strlent(s2) ); // s1의 첫 n개의 문자열을 s1에 복사한다.
- memmove( void *dest, const void *dest, size_t n );
    - src번지에 있는 데이터를 dest의 번지로 n, byte만큼 복사한다.
- memcpy(void *dest, const void *dest, size_t n);
    - src번지에 있는 데이터를 dest의 번지로 n_byte만큼 복사한다.
- strcat(d, s);
    - 문자열 d의 마지막 부분에 문자열 s를 연결
- strncat( d, s, 3);
    - 문자열 d의 마지막 부분에 s의 첫 n개의 문자열을 연결
- 5번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    
    int main()
    {/*
    	5. 대소문자 변환하기
    		소문자를 대문자로
    		대문자를 소문자로
    		‘e’-- > ‘E’
    		‘h’-- > ‘H’
    		힌트 tolower(문자), toupper(문자)*/
    	char ch = 'e';
    	printf("소문자 %c를 대문자로 바꾸면 %c입니다.\n", ch, toupper(ch));
    	ch = 'H';
    	printf("대문자 %c를 소문자로 바꾸면 %c입니다.\n", ch, tolower(ch));
    }
    ```
    
- 6번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    
    int main()
    {/*
     6. 다음 문자열에 대해서 대문자를 소문자로 소문자를 대문자로 변경하여 출력하세요
    ‘12ABCdeFG’
    힌트
    <string.h> strlen() 문자열의 길이, <ctype.h> tolower() 소문자, toupper() 대문자, 문자열 끝 - ‘\0’*/
    	char str[] = "12ABCdeFG";
    	int i;
    	for (i = 0; i < strlen(str); i++)
    	{
    		if (str[i] >= 'A' && str[i] <= 'Z')
    			printf("%c", tolower(str[i]));
    		else if (str[i] >= 'a' && str[i] <= 'z')
    			printf("%c", toupper(str[i]));
    		else
    			printf("%c", str[i]);
    	}
    	return 0;
     }
    
    ```
    
- 7번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    
    int main()
    {/*
    7. 다음 문자열을 역순으로 출력해 보세요.(배열 사용하기)
    결과화면
    영문 순방향 : swimming
    영문 역방향 : gnimmiws
    한글 순방향 : 가나다라마바
    한글 역방항 : 바마라다나가
    힌트
    한글인 경우는 영무자와 달리 2바이트 크기를 가지므로 영문자와 다르게 처리해야 합니다. for( I = strlen(st) - 2 ; i>=0 ; I-= 2 )
     printf(“%c%c”, s[i], s[i+1] );
    */
    	char s[] = "swimming";
    	char st[] = "가나다라마바";
    
    	printf("영문 순방향 : %s\n", s);
    	printf("영문 역방향 : ");
    	for (int i = strlen(s) - 1; i >= 0; i--)
    	{
    		printf("%c", s[i]);
    	}
    	printf("\n");
    
    	printf("한글 순방향 : %s\n", st);
    	printf("한글 역방향 : ");
    	for (int i = strlen(st) - 2; i >= 0; i -= 2)
    	{
    		printf("%c%c", st[i], st[i + 1]); //%c%c를 하는 이유는 한글이 2바이트 크기를 가지기 때문에 %c%c로 해야 한글이 제대로 출력된다.
    	}
    }
    
    ```
    
- 8번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    
    int main()
    {/*
    8. 문자열을 숫자로 변환하는 함수
    char* s1 = "47476"
    char* s2 = "98765432"
    char* s3 = "3456.7634“
    를 숫자로 바꿔서 출력하세요*/
    	char* s1 = "47476";
    	char* s2 = "98765432";
    	char* s3 = "3456.7634";
    	int n1 = atoi(s1);
    	int n2 = atoi(s2);
    	double n3 = atof(s3);
    	printf("%d %d %lf\n", n1, n2, n3);
    	return 0;
    }
    
    ```
    
- 9번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    
    int main()
    {/*
    9. 숫자를 문자열로 변환하는 함수
    결과화면
    정수 : 3198 문자열 : 3198
    힌트 _itoa( 값, 문자배열명, 변환할 진법)*/
    	int num = 3198;
    	char str[10];
    
    	_itoa(num, str, 10);
    	printf("정수 : %d 문자열 : %s\n", num, str);
    }
    
    ```
    
- 10번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    
    int main()
    {/*
    10. 문자열의 대체(replace)는 문자열의 특정 부분을 다른 문자열로 바꾸는 것입니다. char s1[] = “abcdefg”;
    char s2[] = “012”;
    일 때
    결과화면
    012defg
    힌트
    strncpy( s1, s2, strlent(s2) ); // s1의 첫 n개의 문자열을 s1에 복사한다.*/
    	char s1[] = "abcdefg";
    	char s2[] = "012";
    
    	strncpy(s1, s2, strlen(s2));
    
    	printf("%s\n", s1);
    }
    
    ```
    
- 11번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    
    int main()
    {/*
    11. 문자열의 대체(replace)는 문자열의 특정 부분을 다른 문자열로 바꾸는 것입니다. char s1[] = “abcdefg”;
    char s2[] = “012”;
    일 때
    결과화면
    abc012g
    힌트
    - memmove( void *dest, const void *dest, size_t n );
    // src번지에 있는 데이터를 dest의 번지로 n, byte만큼 복사한다. 
    - memcpy(void *dest, const void *dest, size_t n); -
    //src번지에 있는 데이터를 dest의 번지로 n_byte만큼 복사한다.
    */
    	char s1[] = "abcdefg";
    	char s2[] = "012";
    
    	memmove(s1 + 3, s2, 3);
    	printf("%s\n", s1);
    }
    
    ```
    
- 12번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    
    int main()
    {/*
    12. 문자열을 다른 문자열의 특정한 위치에 대체하세요. 실행결과
    st1 : 0123456789, length = 10
    st2 : abc, length = 3
    st2로 대체할 위치를 입력하세요 >> 4
    삽입위치 : 4
    st1 0123abc789, length = 10*/
    	char st1[11] = "0123456789";
    	char st2[4] = "abc";
    	
    	printf("st1 : %s, length = %d\n", st1, strlen(st1));
    	printf("st2 : %s, length = %d\n", st2, strlen(st2));
    
    	int pos;
    	printf("st2로 대체할 위치를 입력하세요 >> ");
    	scanf_s("%d", &pos);
    
    	_memccpy(st1 + pos, st2, 0, strlen(st2)); 
    	printf("삽입위치 : %d\n", pos);
    	printf("st1 : %s, length = %d\n", st1, strlen(st1));
    }
    
    ```
    
- 13번
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    
    int main()
    {/*
    13. 문자열 연결하기
    char d[15] = “love”;
    char s[6] = “holic”;
    결과화면
    strcat : loveholic
    strncat : loveholichol //holic에서 hol붙이기
    힌트
    strcat(d, s); //문자열 d의 마지막 부분에 문자열 s를 연결
    strncat( d, s, 3); //문자열 d의 마지막 부분에 s의 첫 n개의 문자열을 연결*/
    	char d[15] = "love";
    	char s[6] = "holic";
    	printf("strcat : %s\n", strcat(d, s));
    	printf("strncat : %s\n", strncat(d, s, 3));
    	return 0;
    }
    
    ```
    

### 변수

**지역변수(local variable), 자동변수(auto)** 

- 중괄호 내부, 함수의 매개변수에서 사용되는 변수이다.
- 지역변수의 지역은 함수의 내부, 중괄호 내부를 말한다
- 만든 곳을 벗어나면 사라진다.
- 변수의 메모리 공간이 소멸 된다.
- 지역변수는 초기화 하지 않으면 컴파일 에러가 나거나 쓰레기값이 저장된다

**전역변수(global)** 

- 중괄호 외부에 선언되는 변수이다
- 어느 지역에서든 참조할 수 있다.
- 전처리기 밑에 선언하고 반드시 상수로 초기화 해야 한다.
- 초기값을 지정하지 않으면 기본값이 0으로 자동 초기화 된다.
- 모든 곳에 접근 가능하고 프로그램 종료 전에 메모리가 소멸되지 않는다
- 잘못 이용하면 유지보수, 재사용등이 어려워 질 수 있다.
- 이것의 단점을 보안한 것이 정적변수이다

**정적변수(static)** 

- 프로그램 종료되기 전까지 메모리가 소멸되지 않는 변수이다
- 함수를 벗어나도 변수가 사리지지 않고 유지된다
- 초기화할 때 반드시 상수로 초기화 해야 한다
    - 초기화하지 않으면 디폴트값 0으로 자동 초기화 된다

**외부변수 (external Variable)** 

- 전역 변수중 하나로 여러 파일에서 공유할 수 있는 변수이다.
- extern 키워드를 사용하여 다른 파일에서 선언된 변수를 참조할 수 있다.
- 초기화된 변수는 선언된 파일에서만 메모리를 차지한다.
- 다른 파일에서는 extern을 통해 참조만 할 수 있다

- 문제
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <stdlib.h>
    
    void IDFind() {
    	static int count = 0;
    	count++;
    	printf("\n총 %d회째 ID찾기 요청합니다.\n\n", count);
    }
    
    void PWFind() {
    	static int count = 0;
    	count++;
    	printf("\n총 %d회째 PW찾기 요청합니다.\n\n", count);
    }
    int main() {
    	//문제) 정적 지역변수를 이용하여 비밀번호 찾기 프로그램을 작성하기
    
    	while (1) {
    		int num;
    		printf("1. ID찾기 2. PW찾기 3. 종료하기\n");
    		printf("원하시는 서비스 번호를 입력하세요(1~3) : ");
    		scanf_s("%d", &num);
    		if (num == 1) {
    			IDFind();
    		}
    		else if (num == 2) {
    			PWFind();
    		}
    		else if (num == 3) {
    			printf("프로그램을 종료합니다.\n");
    			break;
    		}
    		else {
    			printf("잘못된 입력입니다.\n");
    		}
    	}
    }
    ```
    
- 문제2
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    
    static int balance = 0;
    
    int check_PW() {
    	static int system_pw = 1111;
    	static int error_cnt = 0;
    	
    	int pw;
    	printf("시스템 비밀번호를 입력하세요 ? ");
    	scanf_s("%d", &pw);
    	
    	if (pw == system_pw) {
    		return 1;
    	}
    	else {
    		error_cnt++;
    	}
    
    	if (error_cnt < 3) {
    		printf("비밀번호가 틀렸습니다.총%d회 오류\n", error_cnt);
    		return 0;
    	}
    	else {
    		printf("비밀번호 오류 3회 발생\n");
    		printf("신분증을 가지고 서비스센터를 방문하세요.\n");
    		exit(0);
    	}
    }
    void check_balance() {
    	if (check_PW() == 1) {
    		printf("현재 잔액은 %d원\n", balance);
    	}
    }
    void deposit() {
    	int money;
    	printf("입금하실 금액은 ? ");
    	scanf_s("%d", &money);
    	balance += money;
    	printf("입금 후 현재 잔액은 %d원\n", balance);
    	
    }
    void withdraw() {
    	if (check_PW() == 1) {
    		int money;
    		printf("출금하실 금액은 ? ");
    		scanf_s("%d", &money);
    		if (balance < money) {
    			printf("잔액이 부족합니다.현재 잔액은 %d원\n", balance);
    		}
    		else {
    			balance -= money;
    			printf("출금 후 현재 잔액은 %d원\n", balance);
    		}
    	}
    }
    int main() {
    	printf("키움증권사에 방문해 주셔서 감사합니다.\n");
    
    	while (1) {
    		int menu;
    		printf("=================\n");
    		printf("1. 잔액조회\n");
    		printf("2. 입금\n");
    		printf("3. 출금\n");
    		printf("4. 나가기\n");
    		printf("=================\n");
    
    		printf("원하시는 서비스를 선택하세요 ? ");
    		scanf_s("%d", &menu);
    
    		if (menu == 1) {
    			check_balance();
    		}
    		else if (menu == 2) {
    			deposit();
    		}
    		else if (menu == 3) {
    			withdraw();
    		}
    		else if (menu == 4) {
    			break;
    		}
    
    		printf("계속 서비스를 원하시면 아무키나 누르세요.");
    		_getch();
    		 printf("\n\n");
    	}
    	return 0;
    }
    ```
    

### 포인터

- 포인터 개념
    1. 기계어로 번역되어 실행파일(exe)로 저장
    2. 로더에 의해 실행 파일이 메모리에 적재, 실행
    3. 모든 변수는 컴파일러에 의해 생성되는 심벌 테이블에 저장되며 심벌 테이블은 변수 명과 메모리의 주소가 저장됨
    4. 프로그램이 실행될 때 모든 변수는 심벌 테이블에 있는 주소에 의해 접근
- 심벌 테이블
    - 컴파일러와 같은 언어 변환기에 사용되는 자료구조
    - OS에 들어있음
- 실습 과제 22
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    int main (){
    	double d = 100.0;
    	double* dpoint = &d; // d의 주소	
    	printf("변수 d의 값 : %f\n", d);
    	printf("변수 d의 주소 값 : %p\n", dpoint);
    	printf("dpoint(포인터 변수)의 값 : %p\n", dpoint);
    	printf("*dpoint(포인터 변수가 가리키는 변수)의 값 : %f\n", *dpoint);
    	printf("변수 d의 크기 : %d\n", sizeof(d));
    	printf("변수 d의 주소의 크기 : %d\n", sizeof(&d));
    	printf("dpoint(포인터 변수)의 크기 : %d\n", sizeof(dpoint));
    	printf("*dpoint(포인터 변수가 가리키는 변수)의 크기 : %d\n", sizeof(*dpoint));
    
    	return 0;
    }
    ```
    
- 실습 과제 23
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    int main (){
    	int i, * ip = &i;
    	int sum = 0, * sump = &sum;
    	for (*ip = 1; *ip <= 100; (*ip)++) {
    		*sump += *ip;
    	}
    	printf("포인터 변수를 사용한 1~100까지의 합은 : %d\n", sum);
    	return 0;
    }
    
    ```
    
- 실습 과제 24
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    int main (){
    	int score = 100;
    	int* pscore, ** ppscore, *** pppscore;
    	pscore = &score;
    	ppscore = &pscore;
    	pppscore = &ppscore;
    
    	printf("포인터 변수 *pscore의 값은 %d\n", *pscore);
    	printf("이중 포인터 변수 **ppscore의 값은 %d\n", **ppscore);
    	printf("삼중 포인터 변수 ***pppscore의 값은 %d\n", ***pppscore);
    	printf("=================================================================\n");
    	printf("int 변수 score의 주소는 %p\n", &score);
    	printf("포인터 변수 pscore의 값(주소)은 %p\n", pscore);
    	printf("=================================================================\n");
    	printf("포인터 변수 pscore의 주소는 %p\n", &pscore);
    	printf("포인터 변수 ppscore의 값(주소)은 %p\n", ppscore);
    	printf("=================================================================\n");
    	printf("포인터 변수 ppscore의 주소는 %p\n", &ppscore);
    	printf("포인터 변수 pppscore의 값(주소)은 %p\n", pppscore);
    	return 0;
    }
    
    ```
    
- 실습 과제 25
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <time.h>
    int main (){
    	int score[6] = { 99, 89, 98, 88, 85 };
    	int* pscore, i, sum = 0;
    	pscore = score;
    
    	//아래 조건문이 가능한 이유는 배열의 이름은 배열의 첫 번째 요소의 주소를 가리키기 때문이다.
    	// 즉 pscre= score은 pscore = &score[0]과 같다.
    	if (pscore + 2 == score + 2 && pscore + 2 == &score[2]) {
    		printf("두 번째 요소의 주소가 모두 같다.\n");
    	}
    	if (*(pscore + 2) == *(score + 2) && *(pscore + 2) == score[2]) {
    		printf("두 번째 요소의 값도 모두 같다.\n");
    	}
    
    	printf("포인터 변수 pscore의 값(배열 주소) : %p\n", pscore);
    	printf("배열 이름 score의 값(배열 주소) : %p\n", score);
    	printf("포인터 변수 pscore의 값(배열 주소)의 주소 : %p\n", &pscore);
    	printf("배열 이름 score의 값(배열 주소)의 주소 : %p\n", &score);
    
    	sum = 0;
    	for (i = 0; i < 5; i++) {
    		sum += *pscore++;
    	}
    	printf("포인터 변수를 이용한 합계는 %d\n", sum);
    	return 0;
    }
    
    ```
